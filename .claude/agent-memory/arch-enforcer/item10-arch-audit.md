# Arch-Enforcer Audit Report: Item 10 (Reduce direct BesitoService composition in StoreService for PURCHASE debits/balances + high-value obs listener + 1-line/guard ports) (Item 10/28, second of new pool of 4)

**Date:** 2026-06-09 (PT)  
**Auditor:** arch-enforcer (Grok Build subagent; exact role per CLAUDE.md + AGENTS.md + rules.md + architecture.md + decisions.md + HARDENING_ROADMAP + telegram-bot-hardener pipeline)  
**Task:** Audit the just-executed Item 10 refactor (second of new pool of 4) for architectural violations. Produce report .claude/agent-memory/arch-enforcer/item10-arch-audit.md + update .claude/agent-memory/arch-enforcer/MEMORY.md. Pre GSD logs to .planning/quick/gsd-arch-enforcer-item10-store-besito.log (append + wc) before every read/write.

**Changes under audit (from executor self-check PASSED + 28-*-SUMMARY.md + PLAN.md + gsd-remaining-besito-store.log + item10 impact report + executor artifacts + 0 behavior/0 atomicity/0 prod change verified in golds):**  
- Reduction of direct/held BesitoService in StoreService for PURCHASE debit/balance sites (complete_order ~493 debit + ~488 recheck, direct_purchase ~366, create_order ~440).  
- Removed `self.besito_service = BesitoService(db)` from _init_services (kept package_service co-held untouched).  
- Locals `besito_service = BesitoService(db=self.db)  # local, on-demand; owns=False (db shared)` ONLY inside the exact debit/balance call sites with exact comments; outer purchase tx/commits/returns/stock/deliver/order logic 100% unchanged.  
- Added high-value observational listener `on_besitos_awarded_store_observer` at bottom (exact copy of story 670-694 / broadcast/reward templates: "Cross-domain event listeners" block, "MUST NOT credit/debit/mutate", best-effort, DESIRED CONTRACT, log "store | besitos_awarded_received", purely observational; "Item 10 / remaining store besito / arch-enforcer" comment).  
- Central explicit reg in bot.py (import + register call in on_startup block after game + extended logger.info + comment "+ Item 10 store").  
- 1-line/guard ports only in the two test files: tests/integration/test_cross_service_atomicity.py (class patch to services.besito_service.BesitoService for local intercept in cross atomicity complete_order paths + schedule_emit reuse + docstring "1-line/guard port post Item 10 (local besito in store complete_order per Item5/6 precedent; arch-enforcer)"); tests/unit/test_store_service.py (1-line/guard for service.besito_service.get_balance + comment + minimal import).  
- Minimal targeted appends to CLAUDEs (gamification + missions cross sections) + decisions.md (new Item10 entry after Item6, exact style of prior).  
- 0 other files touched. Executor self-check PASSED + SUMMARY produced; golds re-runs protected "credit survives deliver False" + "post-credit best effort (misiones + listeners)" + strict tx/deltas + source=PURCHASE + patch schedule_emit + DESIRED; re-verified. Pool phrase verbatim repeated.

**Reference rules audited (sourced from CLAUDE.md root + handlers + services + models + gamification/store/missions, rules.md, architecture.md, decisions.md, AGENTS.md, HARDENING_ROADMAP.md recent post-documentador sections with current new pool state + pool phrase + "Item 10/28 second of new pool of 4", precedents 23/24/25/26/27 SUMMARIES + gsd logs for pool language + "second of new pool", prior arch audits item6/item7/item9 + their GSD logs/MEMORY, gsd-remaining-besito-store.log key self-check + rules verif + GSD entries, item10 impact report, executor artifacts):**  
- Handlers: 0 direct other svc (store_user already get_service; no handler changes per tight scope).  
- Services: locals on-demand inside the atomic debit/balance sites only (no held in __init__ for this composer; package co-held untouched); observer is observational only ("MUST NOT credit/debit/mutate" + DESIRED CONTRACT + best-effort); 0 unnecessary coupling (thin, removable); 0 biz logic in wrong layer.  
- No DB access outside models (locals use self.db from service; tests use TestSession/file + guards).  
- Funcs <=50 LOC (no long funcs touched; 1-line/guard ports keep tests clean).  
- Logging: standard if added in paths; observer log follows "módulo | acción | user_id | resultado" style ("store | besitos_awarded_received").  
- Naming: locals use the established "local, on-demand" comment pattern; observer name follows precedent.  
- Callbacks: no new bare strings (none touched).  
- 3 crit systems: gamif (primary - purchase debits + atomicity contracts protected by golds; listener for wiring only); narrative 0 direct; channel/VIP 0 direct (orthogonal). Re-runs of cross atomicity + broader gamif protected.  
- EventBus: observer follows exact "MUST NOT" + DESIRED + best-effort + central reg precedent (Item5/6); no mutation risk with debit paths.  
- GSD/trace: pre every in executor; self-check PASSED; pool phrase; tight scope per PLAN (0 other files).  
- Other: get_service lifecycle respected elsewhere; no dumb closers; no scope creep; 0 behavior/0 atomicity (golds + re-verif); pool "second of new pool of 4".

## Methodology
- **Pre GSD logs (mandatory before reads/writes):** Multiple appends + wc to .planning/quick/gsd-arch-enforcer-item10-store-besito.log (init 7L, final pre-write 18L; documented role/task/pool phrase/plan/hand-off/Item 10/28 arch audit + every pre-read/pre-gate/pre-write step mirroring prior arch-enforcer item9/item7/item6 logs).  
- **Exploration (parallel list_dir + grep + read_file chunks + run_terminal for wc/grep/inspect + GSD pre every):**  
  - Changed sources: services/store_service.py (full + tail for observer placement + trailing methods), bot.py (imports + on_startup reg block), tests/integration/test_cross_service_atomicity.py (class patch + docstring + ports), tests/unit/test_store_service.py (1-line guard + import + comment).  
  - Planning/artifacts: .planning/phases/28-remaining-besito-store/PLAN.md (chunks), 28-remaining-besito-store-SUMMARY.md, .planning/quick/gsd-remaining-besito-store.log (self-check PASSED + pool + criticals + 80+ entries), .claude/agent-memory/impact-analyzer/item10-remaining-besito-store.md (full + pool + "second of new pool of 4").  
  - Precedents: .planning/phases/23-reward-besito-eventbus-decoupling/ (PLAN+SUMMARY+gsd), 24-remaining-besito-compositions/, 25-reward-handlers-1service-loc/, 26-store-admin-long-funcs/, 27-mission-admin-long-funcs/ (SUMMARIES + gsd logs for pool phrase/"first/second of new pool"/self-check/handoff/"Item X/2X closed"), gsd-impact-analyzer-item10-..., prior gsd-arch-enforcer-item*-*.log.  
  - Docs: CLAUDE.md (root + handlers + services + models + gamification/ + missions/ + store/ sub), rules.md, architecture.md, decisions.md (Item10 entry post-Item6), AGENTS.md, .planning/HARDENING_ROADMAP.md (recent post-doc sections + pool + "Item 10/28"), .claude/agent-memory/arch-enforcer/{item6,item7,item9}-arch-audit.md + MEMORY.md, .claude/agents/arch-enforcer.md.  
  - Patterns (grep project-wide + scoped post "refactor"): "self\.besito_service = ", "BesitoService\(db=self.db\)", "local, on-demand; owns=False", "on_besitos_awarded_store_observer", "store \| besitos_awarded_received", "MUST NOT credit/debit/mutate", "DESIRED CONTRACT", "1-line/guard port post Item 10", "Item 10 store", "second of new pool of 4", "Pool anterior de 4 cerrado...", helds in other svcs (story etc out of scope), no handler svc direct for store.  
  - LOC/structural: inspected via reads (no long touched; observer at module bottom post-last-method per PLAN; note latent pre-exist trailing methods check_stock_alert/notify_stock_alert now nested inside observer due to placement/prior indent -- explains documented AttributeError in unit complete low-stock path; pre-exist per SUMMARY "pre-exist in service (method called in complete but not defined in class from reads"; "latent not from 1-line/guard ports or F2 locals; documented non-reg").  
  - GSD discipline: run_terminal appends pre (this audit log) + wc tracked; matches executor (pre every in 28) + prior audits. No code mods (audit + persist report + MEMORY pointer only; writes GSD-pre-logged).  
- Evidence-based, concise, mirrors prior arch-enforcer reports (item9/item7/item6 structure: findings classified critical/medium/obs, 3sys impact, compliance checklist, verdict, handoff, pool verbatim, "Item 10/28 arch audit").

## Findings (Classified)
### Critical (Architecture-breaking, 0 found)
None. All changes follow PLAN/impact + precedents (Item5/23: locals inside _deliver + obs + reg + 1-line + DESIRED; Item6/24: locals in credit sites + 2 obs + "MUST NOT" + DESIRED + central reg + 1-line guards in cross/daily + daily hasattr; 25/26/27 pool/self-check/handoff) exactly. No layer violations (handlers untouched 0 direct svc introduced; services: locals only in the exact atomic sites for this composer + purely observational observer; no DB outside models; 0 biz logic moved wrong). No duplication/coupling added (thin local + removable observer; package co-held untouched per tight). Functions <=50 (no long funcs in scope; 1-line ports). Callbacks untouched. GSD pre-every + self-check PASSED + tight scope (4 source files + targeted docs + log + PLAN/SUMMARY per PLAN "0 other files (0 store_user -- already get_service; 0 package/reward delivery)"). 0 behavior/0 atomicity (golds + re-verif + "debit internal commit authoritative" + outer unchanged + "credit survives" + "post-credit best effort (misiones + listeners)" protected).

### Medium (Fragility / Maintenance / Pre-existing amplified, 2 findings — all pre-exist or out-of-scope per tight item; none critical or introduced by Item 10)
1. **Pre-existing structural/latent in store_service.py (notify_stock_alert / check_stock_alert called from complete_order on low stock but not defined on class instance; now appear nested inside observer due to indent/placement at "bottom"; causes AttributeError in unit test_complete_order_success low-stock path):**  
   - Desc: From read of services/store_service.py:650+ (class ends at update_low_stock ~666; Cross-domain block + observer @669-697; then indented "    def check_stock_alert(self... " @699 and notify @727 inside the async observer func). complete_order @558 does `await self.notify_stock_alert(...)`. SUMMARY explicitly: "Latent fail in unit store test_complete_order_success (AttributeError no notify_stock_alert inside complete on low stock path; pre-exist in service (method called in complete but not defined in class from reads; ... latent not from 1-line/guard ports or F2 locals; documented non-reg per PLAN". Pre-dates Item10 (the call was there; append-at-bottom per PLAN F2 did not fix pre-existing nesting/scope).  
   - Why "medium" (not critical): Explicitly documented non-reg in executor SUMMARY/PLAN "Riesgo: unrelated fails... document (precedent...); focus 0 attributable"; out of scope for this tight item per user prompt ("Note any pre-exist (e.g. other helds like package, latent test fails documented as non-reg) as out of scope for this tight item."). Item10 changes (locals + observer append) did not introduce the call or the defect. Cross atomicity golds + broader -k store/purchase did not hit the low-stock alert path in re-runs (or tolerated as pre).  
   - Recommendation (for test-guardian/quick if desired): No action for *this Item*. Future store touch (or dedicated quick) can move the two methods inside the StoreService class (or remove if dead). Test-guardian to run only planned criticals (not treat pre as reg from Item10). Out of scope here.  

2. **Pre-existing test debt / hygiene in golds (N806 TestSession in cross atomicity gold tolerated+doc per precedent; F841s some hygiene-fixed as _ in our ports, others pre; daily concurrent UNIQUE flake; RuntimeWarning coroutine emit; SA/MovedIn20/Deprecation warnings; latent daily !success assert pre-patches):**  
   - Desc: Documented in SUMMARY F4/F5 deviations + PLAN "pre-existing unrelated fails ... do not count as regression" + precedents 23-27/22/19/20. Our ports added 1 F841 (silenced _ hygiene 0 logic) + exercised the 1-line guard (now green where reached). Not caused by locals/observer/ports.  
   - Why "medium": Matches exact precedent handling ("do not count as regression"; N806 tol w/doc; ruff hygiene chore). Tight scope + "pre-exist only" per user prompt. 0 attributable to Item10 (unit complete besitos path now guarded; cross green 8/8).  
   - Recommendation: Leave (per PLAN explicit + precedents); test-guardian tolerate in re-runs of listed criticals. Flag for broader test debt pools per roadmap (~2-4 clusters remain).  

### Observations (Good / Minor / Adherence, many — selected key)
- **Locals on-demand + no held + atomicity fidelity (store_service.py: _init_services comments exact "Held direct BesitoService composition removed (Item 10 / remaining store debits unification)... PackageService remains held (scope: other composers untouched per Item 10 tight). # self.besito_service = ... REMOVED"; locals at direct_purchase:372, create_order:449, complete_order:500 with exact "local, on-demand; owns=False (db shared); ..."; post-debit comment "no schedule for debit; debit internal commit authoritative; outer stock/deliver/order COMPLETE + db.commit() unchanged"; outer logic/returns/commits/PURCHASE source/desc/ref=order.id/stock/deliver 100% identical):** Exact copy of Reward Item5 _deliver + bcast/game Item6 credit sites + atomicity gold + daily pattern per PLAN "Copia patrones al pie de la letra". Grep confirmed 0 active "self.besito_service = BesitoService" in store (only in comment); 3 locals present with comments. Preserves "debit internal commit authoritative" + caller tx control (0 atomicity impact). Package co-held untouched (per tight "0 other composers").  
- **Observer contract + wiring (store_service.py:669-698 "Cross-domain event listeners" block + "MUST NOT call back into credit/debit besitos (to avoid any re-entrancy with purchase debit paths..."; docstring "MUST NOT credit, debit, or mutate besitos state here", "DESIRED CONTRACT (copy of narrative precedent + Reward Item5 + broadcast Item6)", "observational best-effort for store domain", "no re-entrancy risk with purchase debit paths", "0 impact on purchase debit contracts / atomicity gold / partial failure"; log exact "store | besitos_awarded_received | user_id={uid} | ..."; final comment "No side effects that mutate besitos here (best effort...)"; + "Item 10 / remaining store besito / arch-enforcer" @677):** Exact precedent from story:670-694 + reward/broadcast expanded templates (per impact/PLAN F2). High-value for domain (wiring + future even on debit paths). Best-effort, 0 mutation risk.  
- **Central reg + bot (bot.py:76 import from services.store_service; on_startup @204 comment "Fase 3 of eventbus-poc + Item 5 + Item 6 + Item 10 store: ..."; @209 register after game; @210-211 logger.info extended to include ", store"):** Explicit, central, no import side-effects (precedent Item5/6). 5 listeners total now.  
- **1-line/guard ports + tests fidelity (cross: docstring @179 "1-line/guard port post Item 10 (local besito in store complete_order per Item5/6 precedent; arch-enforcer)"; class patch @199 `patch("services.besito_service.BesitoService") as _mock_besito_cls` (for local intercept when complete_order exercised) + schedule reuse + "optional no-held/uses_local/observer contract if fits tight"; unit store: import @15 comment, assert @143 exact guard `... if not hasattr(service, "besito_service") else ...` + comment "# 1-line/guard port post Item10 local (copy daily precedent in cross; arch-enforcer)"; keep exact asserts/deltas/tx/source=PURCHASE/"credit survives deliver False"/DESIRED/patch):** 1-line/guard only per PLAN/impact (no new tests/cases/files); daily precedent (hasattr + fallback); class patch for intercept (F841 silenced as _ hygiene). Re-runs exercised ports + patch + kept asserts.  
- **Docs + decisions fidelity (gamification/CLAUDE + missions/CLAUDE cross sections appended with Item10 note + "second of new pool of 4" + refs to PLAN/impact/gsd/golds/"0 other files"/"0 behavior/0 atomicity"; decisions.md @204-249 full Item10 entry after Item6 exact style: Motivo/Riesgos/Decisión/Resultado + pool phrase + "second of new pool of 4" + handoff to arch-enforcer/test-guardian/gsd-executor next; SUMMARY/gsd/impact/PLAN/CLAUDEs all repeat verbatim pool phrase + "Item 10/28 closed. Second of new pool of 4..."):** Minimal/targeted per PLAN "if precedent".  
- **GSD/trace/self-check + pool (executor gsd-remaining-besito-store.log 80+ entries with pre every + "Copia patrones al pie de la letra" + DoD per phase + F5 self-check "PASSED" + critical tests list + pool phrase verbatim + "Item 10/28 closed. Second of new pool of 4. Pool anterior de 4 cerrado (tests passing per user). Nuevo pool de 4 iniciado. Quedan ~2-4 clusters del análisis inicial después de este pool. Previous batch of 4 (Item 7/25 + Item 6/24 + Item9/27 as first of this pool) closed with tests passing per user. Ready for arch-enforcer re-scan (enfocado en store besito locals + no held + observer contract + atomicity golds) + test-guardian (correr los tests críticos listados) + gsd-executor del siguiente item del pool de 4"; this audit log mirrors with pre-reads/pre-writes + 18L; SUMMARY/impact/HARDENING/CLAUDEs/decisions repeat pool + "second of new pool of 4" + "Item 10/28"; HARDENING recent post-doc sections updated by documentador for new pool state post Item9 + "Item 10/28 ... executor next per handoff"):** Full traceability + discipline.  
- **Tight scope + 0/0/0/0 + 3sys + golds (per user prompt + PLAN/impact/SUMMARY: only listed files; 0 handlers; 0 other services/files; 0 prod/0 behavior/0 atomicity (purchase returns/deltas/tx/source=PURCHASE/history/order/stock/deliver/UI/Lucien identical; golds re-ran full w/ patch + strict + DESIRED + "credit survives deliver False" + "post-credit best effort (misiones + listeners)" + TestSession/file + 777 + gather + N806 tol w/doc); 3 crit: gamif primary (purchase debits + listener wiring; atomicity golds + broader -k store/purchase/complete_order/atomicity/besitos protected); narrative 0 direct; channel/VIP 0 direct (orthogonal); "0 impact on purchase debit contracts / atomicity gold"); pre-exist other helds (e.g. story) out of scope):** "second of new pool of 4" respected.  
- **Other positives:** Ruff limpio (hygiene only 0 logic); greps post-edit confirm (0 held active in store, locals+comments, observer+contract, ports comments, bot reg, docs); bot smoke reg+emit; no new bare cb strings; get_service lifecycle untouched (elsewhere); no dumb closers; Lucien voice 3rd preserved; comments/arch notes exact precedent. 

## Impact on 3 Critical Systems
- **Gamification (besitos source — reactions/daily/game + purchases + missions that award via deliver/increment):** Protected + improved compliance (no held). This item targets the last major purchase debit/balance composer in store (complete_order/direct/create for content buys via PURCHASE tx). Locals preserve exact atomicity ("debit internal commit authoritative" + outer unchanged). Observer is wiring-only (observational; purchases are debits so no emit from this path; "0 impact on purchase debit contracts"). Re-runs of cross atomicity full (happy/sad w/ patch schedule_emit + strict + DESIRED + "credit survives deliver False" + "post-credit best effort (misiones + listeners)" + TestSession/file + 777 + gather + N806 tol) + broader -k "store or purchase or complete_order or atomicity or besitos" + unit store + bot smoke protected "credit survives" + post best-effort (misiones + listeners) + emit still fires. 0 behavior on besitos tx/deltas/history/source.  
- **Missions/Rewards (dueños — deliver via deliver_reward + increment + claim):** Protected + 0 direct (via "post-credit best effort (misiones + listeners)" contract in golds; atomicity gold covers spend analog; listener best-effort only; 0 touch to reward/mission delivery paths per tight "0 package/reward delivery"). Continuation noted in missions/CLAUDE.  
- **Narrative (listeners + arquetipos + achievements):** 0 impact (untouched per precedent "narrative 0 direct"; story keeps its held; listener addition is store-domain only + best-effort; inverse credit paths re-run protected in F3).  

All 3 systems' contracts (atomicity golds, best-effort sides, "credit survives", no re-entrancy with "MUST NOT", tx sources) remain intact. "0 behavior/0 atomicity/0 UI change" + "3 critical systems protected" + "locals + observer contract + 1-line ports follow Item5/6 precedents exactly" per PLAN/executor self-check + re-runs + this audit.

## Compliance Checklist (vs audited rules)
- Handlers: Compliant (0 changes; store_user already uses get_service per impact/PLAN; no direct other svc introduced).  
- Services: StoreService owner for purchase flows; locals on-demand *only* inside the 3 exact atomic debit/balance sites (no held for this composer; package untouched); observer purely observational per "MUST NOT"/DESIRED/best-effort contract (no mutation, no biz logic, thin/removable, 0 unnecessary coupling); 0 dupe; no DB direct (uses self.db passed).  
- Layers/Cross: No DB outside models; get_service lifecycle respected (elsewhere; locals use direct for tx/owns explicit inside atomic per PLAN "NO migration to get_service for the local debits/balances"); EventBus central reg explicit (no side-effects); 0 new cb bare strings.  
- Functions/LOC/Naming: No long funcs touched (ports 1-line keep clean); naming/comments follow "local, on-demand" + observer precedent (verb+ctx via log "store | ...").  
- Anti-patterns/GSD: No prohibited added (no logic in handlers, no DB out, no dupe); GSD pre-every (executor 80+ + this audit 18L) + PLAN-driven + tight scope (0 creep) + self-check PASSED.  
- 3-systems/Atomicity/EventBus: 0 impact (gamif protected primary; others orthogonal); golds + re-runs + "MUST NOT" + "debit internal authoritative" + "credit survives"/"post-credit best effort" protected; observer follows exact precedent (no re-entrancy risk with debit paths).  
- Logging/voice: Observer log "store | besitos_awarded_received | user_id=... | ..." matches "módulo | acción | user_id | resultado"; purchase paths use standard; Lucien 3rd preserved (untouched).  
- Other (callbacks, get_service, no dumb closers, scope): All respected; pool "second of new pool of 4" + "Pool anterior..." verbatim; "Item 10/28" throughout.

## Overall Verdict
**PASS WITH NOTES (pre-exist only, like prior items 9/7/6).**  

0 critical violations. Tight scope respected exactly (per PLAN/impact/user prompt: 0 other files, 0 handlers, 0 package/reward, 0 behavior/0 atomicity/0 prod, 3 systems + golds safe). Locals + observer contract + 1-line ports + central reg + docs follow Item5/6 precedents *al pie de la letra* (copy story 670-694 / templates + daily guards + atomicity gold + pool/self-check/handoff language from 25/26/27). GSD discipline total (pre every + wc). Self-check PASSED in executor. Pre-exist notes (latent notify_stock_alert nesting/attr error in store complete low-stock + test hygiene/N806/F841/daily flakes etc) explicitly documented as non-reg/out-of-scope (do not count as regression from Item10; focus 0 attributable per PLAN + precedents). No new violations introduced. "no critical", "tight scope respected", "3 systems + atomicity golds safe", "locals + observer contract + 1-line ports follow Item5/6 precedents exactly".

**Pool phrase (verbatim, repeated throughout artifacts per mandate):** Pool anterior de 4 cerrado (tests passing per user). Nuevo pool de 4 iniciado. Quedan ~2-4 clusters del análisis inicial después de este pool.

**Item 10/28 arch audit**

**Handoff note:** Ready for test-guardian (re-run criticals from PLAN/impact: cross atomicity full w/ patch + DESIRED + 'credit survives' + 'post-credit best effort', broader gamif -k store/purchase/complete_order/atomicity/besitos, bot smoke reg+emit, unit store, ruff, greps for 0 held/locals/observer/'MUST NOT'/1-line ports) + gsd-executor of next pool item (or documentador if this closes the pool of 4).

Report persisted to .claude/agent-memory/arch-enforcer/item10-arch-audit.md + MEMORY.md updated (see sibling index). GSD log final wc tracked. All per task + "Copia patrones al pie de la letra".

**End of audit.** No fixes implemented (per instructions; only audit + persist).

Pool anterior de 4 cerrado (tests passing per user). Nuevo pool de 4 iniciado. Quedan ~2-4 clusters del análisis inicial después de este pool.
