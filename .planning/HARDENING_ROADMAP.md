# Lucien Bot - Hardening Roadmap (Telegram Bot Hardener Tirón)

**Date:** 2026-06-08  
**Context:** Post `/telegram-bot-hardener` invocation + multiple agent-pipeline "tirones" (batches of up to 4 items).  
**Goal:** Capture initial analysis, decisions, execution approach, completed work, gaps, and future roadmap. This serves as a living hoja de ruta for continued hardening while respecting project rules (CLAUDE.md, AGENTS.md, architecture.md, rules.md, GSD workflow, 3 critical systems: gamification, narrative, channel/VIP).

---

## 1. Initial Analysis (What Was Detected)

From the telegram-bot-hardener skill invocation (initial full codebase scan via analyze_codebase.py + manual deep dives into CLAUDEs, services/handlers/models, tests, middlewares, bot.py, atomicity golds, etc.):

### Critical / High Fragility (Rompen bajo carga/cambios o violan arquitectura)
- **Middlewares incomplete activation (alta prioridad):** 
  - ErrorHandlerMiddleware global (message + callback_query) — good, but only partial.
  - RateLimiterMiddleware (middlewares/) and legacy ThrottlingMiddleware (handlers/, aiolimiter + ADMIN_BYPASS) existed in parallel but **none wired globally** in bot.py (only Error was active). Idempotency was ad-hoc manual cache checks (only in 2 handlers: gamification reactions, rewards) + unit tests, not a global middleware.
  - Result: spam/minijuego protection dormant; duplicate callback re-execution risk; no consistent per-user throttling.
- **Session / resource management debt:** Mix of legacy `Service(); try: ... finally: svc.close()` (high leak risk — prior `fix_connection_leaks.py` existed) vs. modern `with get_service(XXX) as svc:` context manager (in services/__init__.py). Many "dumb closers" (Reward/Broadcast/Package/Game/User always closed even shared db=). Story/Reward held direct BesitoService composition in __init__ (cross-domain smell).
- **Cross-domain coupling (alta prioridad para 3 sistemas críticos):** Direct service instantiation (e.g., StoryService __init__ did `self.besito_service = BesitoService(self.db)`; same in Reward). No EventBus for notifications (gamif → narrative on besitos_awarded). Scheduler/streak used intentional bridge for APScheduler serialization (analyzer flagged as "circular" but documented as design).
- **Atomicity / state consistency risks (gamif + narrative + channel):** Besito had good FOR UPDATE + commit=False, but races possible in concurrent awards; narrative FSM/archetype/branching lacked explicit transition graphs or restart tests; VIP/channel grant/revoke had thin offline/edge coverage (user already left channel, ban propagation, multi-sub partial expire).
- **Callback answer() guarantees & error paths:** Many places did answer(), ErrorHandler helped on exceptions, but no global guard; early returns (e.g., idemp duplicates) sometimes silent.
- **Analyzer "críticos" (mostly false positives but highlighted debt):** Intentional bridges (streak/scheduler); long functions everywhere (bot.py main/on_startup, admin wizards, scripts).

### Fragility (Pueden romper bajo carga/cambios)
- **Testability & coverage gaps (media-alta):** Good structure (unit/integration/handlers + conftest with make_*, db_session expire_on_commit=False, TestSession/file SQLite for atomics). But missing explicit:
  - Gamif races/concurrent (no dup points, max limits, insufficient balance UX).
  - Narrative: invalid transitions/FSM restore on "restart", archetype once-only, branch graceful fallback.
  - Channel/VIP: pay→VIP+remove-free, expire-no-error-if-gone, ban-both-channels, offline startup recovery, multi/partial subs.
- **Long functions & handler bloat:** Dozens >50 LOC (project rule); admin wizards (store/mission/reward creation flows) had "process_*"/"confirm_*" multi-step logic flagged as "business logic in handlers" (though often routing + FSM).
- **Duplication & inconsistency:** Two rate limiting impls (aiolimiter legacy vs. simple time-based); manual idempotency vs. no global; mixed close patterns; some services composed Besito directly for credits.
- **In-mem rate/idemp (prod risk):** No Redis backing (unlike FSM); multi-instance (Railway) could allow dups/spam.
- **Logging & observability:** Inconsistent structured logging (módulo | acción | user_id | resultado); no health endpoint.

### Deuda Técnica (No rompe hoy, pero acumula)
- **Handler bloat in admin (store/mission/reward/promotion wizards):** Multi-step FSM (50+ LOC common); some still used legacy direct Service() + close.
- **Remaining direct Besito compositions:** Post-Item5 (Reward fixed), still in broadcast (reactions), game (minijuegos/streaks), daily_gift, store (debits), etc.
- **Docs drift:** handlers/CLAUDE.md described aiolimiter as active (but wasn't); security report claimed global rate (legacy was per-user).
- **Analyzer noise:** 360+ warnings mostly from strict 60-LOC + "logic in handler" length checks (project uses 50-LOC + "exactly 1 service call" rule).
- **Other:** Pre-existing test flakes (daily concurrent UNIQUE, some VIP flows); N806 in golds; deprecation utcnow; no full Redis for rate/idemp; health/structured logging missing.

**3 Critical Systems Always in Mind (per skill + CLAUDE.md):** 
- Channel admin (VIP/free grant/revoke/pending/expire/ban).
- Gamification (besitos, reactions, daily, minijuegos, broadcast).
- Narrative (story nodes, archetype quiz, achievements, progress, VIP-gated).

References: Initial hardener report (full 194-file scan), impact-analyzer outputs (e.g., item6-remaining-besito-compositions.md), CLAUDE.md (handlers 1-service, services owners, GSD, logging, 50 LOC), architecture.md, rules.md, testing-strategy.md from skill, gold tests (cross_service_atomicity, reaction_*_flow, invariants, besito unit, etc.).

---

## 2. Decisions Made

**Core Strategy Decisions:**
- **Start with middleware foundation (highest leverage/low risk):** Activate/unify rate limiting (choose canonical, wire global with ADMIN_BYPASS) + promote idempotency to real BaseMiddleware (global for all callbacks, silent answer on dupe). Unify session management to get_service (reduce leaks). Rationale: Directly addressed top fragilities from initial scan; protects *all* routers (incl. 3 critical systems); low blast radius (no business logic change).
- **Agent pipeline per item (strict sequence):** For each high-value item: impact-analyzer (map consumers, risks, affected tests, 3 systems) → gsd-planner (phased PLAN with DoD, tight scope, gold patterns, GSD pre-log) → gsd-executor (strict phases, pre-log every edit/gate, copy patterns al pie de la letra, self-check PASSED) → arch-enforcer (audit vs. CLAUDE/rules/architecture — PASS/PASS WITH NOTES/FAIL) → test-guardian (audit coverage, generate/update tests, re-run golds) → run tests (targeted + broad smoke with exact flags: -q --tb=line -p no:cov --override-ini="addopts=" + -k filters).
- **Tirones de hasta 4 items (chained automatically):** "Dos de un tirón" extended to max 4 per tirón. Automatic handoff (PLAN/SUMMARY/gsd-log + "BATCH: X items completed... Ready for next + arch-enforcer re-scan + test-guardian"). Focus on 3 critical systems + EventBus/get_service/atomicity precedents.
- **Principles (non-negotiable):** 
  - Tight scope per PLAN (0 behavior/0 atomicity change; 0 other composers unless planned; 0 new files except opt SUMMARY).
  - Preserve gold contracts (cross_service_atomicity, reaction_mission_flow, invariants, daily atomic, etc.): "credit survives deliver=False", "post-credit best effort (missions + listeners)", tx counts/deltas/strict asserts, TestSession/file DB, patch schedule_emit, DESIRED CONTRACT, fresh TG 777x, N806 tol w/ doc, try/finally close+dispose, gather+return_exceptions.
  - EventBus for *notifications only* (obs, "MUST NOT credit/debit", best-effort, central explicit reg in bot.py, removable).
  - GSD pre-log before *every* edit/gate (format + wc tracking).
  - Copy gold patterns al pie de la letra (Reward local inside _deliver + 1-line test; story listener block + "MUST NOT"; bot reg; atomicity golds; daily hasattr guard + fallback).
  - 3 systems + rules (handlers exactly 1 service + no logic/DB, services owners, <50 LOC, logging "módulo | acción | user_id | resultado", etc.).
- **Rationale for order/scope:** Middleware first (quick win, global protection). Then session (leak reduction). Then tests (protect what we have + fill skill-recommended gaps). Then Besito decoupling (use new EventBus + local pattern from Reward to reduce coupling without breaking atomicity). Prioritize high-volume user-facing (reactions, games, daily) + verifiable via existing golds.
- **No direct edits outside GSD/agent flow:** All via PLANs + pre-logs (per initial CLAUDE.md enforcement).
- **Max 4 per tirón + pause for doc:** As requested.

**Specific Item Decisions (per impact/planner reports):**
- Middleware: Port legacy aiolimiter logic to middlewares/ (canonical ThrottlingMiddleware + alias); make IdempotencyMiddleware; wire after Error; cleanup manual ifs; update docs.
- Session: Normalize dumb closers first (add _owns_session guards); convert Tier 1 handlers (gamif/store/broadcast) to with get_service; 1-line/guard test ports.
- Tests: ~4-6 per system using gold patterns; explicit races (gather + <=1), archetype once-only, invalid branches, pay+remove-free, expire-no-error, ban-both, offline recovery, FSM restart sim (Memory + note Redis), EventBus listener coverage.
- Besito decoupling (Item5 + Item6): Reward first (local inside _deliver + obs listener); then remaining in broadcast (reactions), game (play_* + streaks), daily (claim only; keep lazy prop for compat). Locals db= for atomicity; obs listeners "MUST NOT credit" (high-value for reactions/game awards); no daily listener (tight); 1-line fixes; central reg.

**Trade-offs Accepted:** Some pre-existing (daily concurrent flake, N806 in golds, >50 LOC in wizards, legacy direct in some handlers) documented as non-reg / out-of-scope for tight items. In-mem rate/idemp (future Redis). No full handler modernization in one go.

---

## 3. How We Are Proceeding (Execution Approach)

- **Per-item pipeline (chained automatically):** impact-analyzer (mapa de impacto, consumers, riesgos a 3 sistemas, tests críticos, scope recs) → gsd-planner (PLAN.md with 4-6 small phases, DoD checklists, exact files/cambios, tests gates, riesgos+mit, safe points, "Instrucciones para gsd-executor" + patterns to copy) → gsd-executor (strict phases; GSD pre-log before *every* edit/gate/ruff/pytest/grep/smoke/self-check; read PLAN + golds first; copy al pie de la letra; report brief per phase + full self-check PASSED in log + handoff) → arch-enforcer (audit vs. CLAUDE/rules/architecture; PASS / PASS WITH NOTES / FAIL; persist report) → test-guardian (coverage audit, generate/update tests, re-runs, veredict "suite protege adecuadamente"; persist report) → correr tests (targeted + broad smoke with exact flags from PLANs; 0 attributable regressions).
- **GSD Discipline:** Pre-log in per-item .planning/quick/gsd-*.log (timestamp | PHASE N | GSD pre-... - desc + refs DoD + patrones copiados; wc -l after). No edits without it. Task commits (specific git add + messages with scope/0/0/0/refs/BATCH).
- **Scope & Verification:** Tight per PLAN (0 behavior/0 atomicity/0 other/0 creep). Re-runs of golds + broader -k filtrado (atomicity, reaction chains, daily/game/broadcast units, besito emit, story inverse, event_bus, invariants, vip flows). ruff limpio (N806 pre gold tol + doc). Greps for rules (0 held, locals present, "MUST NOT", 1-line comments, bot reg, docs sections). Smokes (bot import, manual listener reg+emit, imports). Self-check PASSED + BATCH phrase at end of tirón.
- **Patterns & Precedents:** Always copy al pie de la letra (Reward local inside + 1-line test comment; story listener + "MUST NOT call back into credit/debit" + best-effort + DESIRED + domain log; bot reg block + comment; atomicity golds patch+DESIRED+file+TestSession+strict+"credit survives deliver False"+"post-credit best effort (misiones + listeners)"+N806+777+try/finally+gather; daily hasattr guard + fallback; EventBus schedule_emit). 3 systems in mind. EventBus for notifications (obs only). Locals db= for atomicity (shared, owns=False).
- **Batch/Tirón Model:** Up to 4 items chained (handoff via PLAN/SUMMARY/gsd-log + "BATCH: X items... Ready for next + arch-enforcer re-scan + test-guardian"). Max 4 per user. After batch: full report + pause for doc (this file).
- **Tools/Output:** Agents via spawn_subagent (with detailed prompts referencing prior reports/PLANs/golds). Persist in .planning/phases/*/PLAN.md + SUMMARY.md + gsd-*.log; .claude/agent-memory/{impact,arch,test-guardian}/*.md + MEMORY.md updates. Commits per protocol (not always executed in runs if PLAN excluded git).
- **Risk Mitigation:** Conservative (revert per safe points; pre-exist fails/warnings doc non-reg; stop on ambiguity + log). Focus verifiable (golds first).

---

## 4. What Has Been Done (Completed Work)

**Prior Foundational (pre this tirón, from initial middleware focus):**
- Middleware hardening (rate activation/unification to canonical in middlewares/ + alias; IdempotencyMiddleware global; cleanup manual ifs in gamif/reward handlers; tests ported; docs updated). ErrorHandler already global. Rate now with real ADMIN_BYPASS + aiolimiter (ported logic).
- EventBus PoC (Item 1 foundation): services/event_bus.py; emit in besito.credit post-commit; narrative listener (obs); central bot reg; tests + golds updated.

**This Tirón (4 items, max 4, chained automatically):**

**Item 1 (in batch context): Session/get_service unification**
- Fases: Prep/baseline (GSD, ruff, targeted pytest golds); normalize 5 dumb closers (Reward/Broadcast/etc. + _owns_session guards + composer subs); convert Tier 1 handlers (gamification_user, store_user, broadcast) to with get_service (multi-with for display paths; 1-service per entrypoint); 1-line/guard test ports (get_service + __enter__/__exit__ style from mission/reward ports); coverage for owns/exc/get_service in units; re-runs cross atomicity/reaction/vip/gamif; final ruff/greps/LOC/logging.
- Key files: services/* (5 normalized), handlers (3 converted), tests (ports + new lifecycle classes), docs.
- Outcomes: 0 leaks introduced; consistent lifecycle; tests 50-80+ passed per phase + broad 400+; 0 behavior change; arch PASS; self-check PASSED + handoff.
- Verification: Golds (atomicity + daily guards + reaction chains); GSD logs (dozens entries); greps 0 direct in converted handlers.

**Item 2: Critical systems tests**
- Fases: Prep (GSD, baseline, gold patterns confirm); gamif (races/concurrent/no-excede/insufficient + get_service lifecycle); narrative (archetype once-only + invalid branches/trans + FSM restore sim + EventBus listener); channel/VIP (pay+remove-free, expire-no-error, ban-both, offline recovery, multi/partial + lifecycle); cross + get_service coverage + re-runs golds; final verif.
- Key files: tests/unit (besito/daily/game/story/vip/channel + event_bus); integrations (cross atomicity + reaction_* + vip flows + free_entry); conftest updates (if needed).
- Outcomes: ~15-18 new/extended tests (races with gather+<=1; archetype immutability; invalid graceful; pay+remove-free; ban prop; offline; FSM+listener; get_service owns); re-runs 400+ passed (0 attributable); 1-2 preexist xfailed/doc non-reg; arch PASS WITH NOTES (preexist only); test-guardian veredict "suite protege adecuadamente"; self-check PASSED.
- Verification: Broad -k smoke 286+ passed; golds (cross + chains + daily atomic + besito race); listener/FSM/EventBus coverage; N806/TG/gather patterns.

**Item 3: Reduce direct Besito in RewardService via EventBus**
- Fases: Prep/baseline; refactor (held removed from __init__/close; local Besito(db=self.db) *only* inside _deliver_besitos for atomicity/MISSION tx + history + return msg; obs listener "MUST NOT credit/debit" + best-effort + DESIRED + "rewards | besitos_awarded_received"); central reg in bot.py; 1-line test fix (besito_service.get_balance → independent BesitoService(db=)); re-runs golds (reward unit + cross atomicity + mission/reward flows with patch schedule_emit); docs (missions/CLAUDE cross section + decisions Item5 entry).
- Key files: services/reward_service.py, bot.py, tests/unit/test_reward_service.py, services/missions/CLAUDE.md, decisions.md.
- Outcomes: 0 held active; listener + reg present with contract; 1-line fix; 0 behavior/0 atomicity (credit sync inside deliver; partials/golds hold); re-runs 17/8/44+ passed; arch PASS; test-guardian "suite protege"; self-check PASSED + "Item 5 closed. BATCH...".
- Verification: Golds with patch + "credit survives deliver False" + "post-credit best effort"; emit still scheduled; listener wiring smoke.

**Item 4 (final): Unify remaining direct Besito compositions (broadcast/game/daily_gift)**
- Fases (5 small, strict): F1 prep/GSD/baseline (ruff, targeted pytest golds 50-80p, greps composition + gold patterns al pie, GSD pre, "F1 safe point"); F2 broadcast (held removed; locals in register_reaction + check_and_register_reaction credit sites (atomic gold path); high-value listener on_besitos_awarded_broadcast_reaction_observer (copy story 670-694 + PLAN template: MUST NOT + DESIRED + "broadcast | besitos_awarded_received" + 0 impact on reaction credit contracts/atomicity gold/partials + best effort); 1 owns test deferred); F3 game (held removed; locals in 6 play_* credit blocks (win+streak bonuses; re-use local); has_sufficient local kept; high-value listener on_besitos_awarded_game_award_observer (game awards/streaks); 0 game test 1-lines (F1 grep 0 direct)); F4 daily (local inside claim_gift credit/get_balance only; @property kept for compat + hasattr guards (daily precedent); 0 change to other/close; no listener daily (tight)); F5 1-line fixes + re-runs golds + verif + self-check + BATCH (exactly 1-line/guard+comment in broadcast owns, daily 135/287/concurrent, cross 726/762 (class patch to hit local); imports; re-runs obligatorios golds+chains+broader (287p +1 pre daily concurrent doc non-reg +1 xfailed; patch schedule_emit in atomicity happy verified + DESIRED + strict + "credit survives" + "post-credit best effort"; 0 attributable); smokes (bot import, manual reg+emit 2 listeners OK); final ruff limpio; greps all criteria (0 held in b/g inits, locals w/ comments in credits, listeners + MUST NOT + domain logs, bot reg 4 + extended log + "+ Item 6", 1-line comments, 3 CLAUDEs cross sections + decisions Item6 + BATCH phrase); docs (broadcast/CLAUDE new full cross Item6 section at end; gamif/ append to Item1; missions/ append 1-2 to Item5; decisions full Item6 entry after Item5 mirror Item5 + BATCH + handoff; bot reg comment); big self-check PASSED in log (full struct + critical tests list + "Item 6/24 closed. BATCH: 4 items completed in this tirón (final of max 4)"); opt SUMMARY.
- Key files: services/broadcast_service.py (+ listener), game_service.py (+ listener), daily_gift_service.py (local in claim; prop kept), 3 test units (1-line + guards + import), test_cross_service_atomicity.py (1-line/guard + patch adjust), bot.py (reg for 2 new listeners), 3 CLAUDEs (new/append cross sections), decisions.md (Item6 entry + BATCH), gsd log (146+ entries), PLAN + SUMMARY.
- Outcomes: 0 held active in 3 inits (grep verified); locals present exactly in credit sites with comments; 2 observers + "MUST NOT" + domain logs + central reg (4 total listeners); 1-line fixes + daily guards (precedent); 0 behavior/0 atomicity (all returns, tx sources REACTION/GAME/TRIVIA/DAILY_GIFT, local besitos_awarded fields, Lucien voice, etc. identical; golds re-runs + patch schedule_emit confirm emit from *local* credits + "credit survives" + post best effort hold); ruff limpio (N806 pre gold tol + doc); re-runs 287p+ (broad -k) + targeted units 50-80p per phase + chains 8p+1x pre; 0 attributable regressions; arch PASS WITH NOTES (0 critical; medium pre-exist only: daily lazy kept + guards, >50 LOC preserved, guards exercised); test-guardian "suite protege adecuadamente" (new observers explicit coverage added; emit-from-local + no-held + guards + contracts unit-asserted + golds protect); self-check PASSED + "BATCH: 4 items completed in this tirón (final of max 4)"; GSD discipline total; batch complete.
- Verification: Golds (cross atomicity full w/ patch + DESIRED + TestSession + strict + "credit survives deliver False" + "post-credit misiones (best effort) + event listeners (best effort)"; reaction_mission_flow/full_chain/limit; daily atomic/concurrent w/ guards; game play/limits/streak/promo/concurrent; besito credit/emit/race; story listener/inverse; event_bus; invariants); broader smoke 286+; listener reg+emit smoke; greps (0 held, locals in credits, "MUST NOT" + logs, bot reg 4 + Item6 comment, 1-lines, docs sections, decisions Item6 + BATCH); ruff on touched; N806/TG/gather/try/finally patterns; 0 other composers.

**Overall Batch Outcomes:**
- Middleware + session + EventBus + Besito decoupling/unification across core gamif (reactions, games, daily, rewards).
- Tests strengthened for races/FSM/edges + coverage of new patterns (locals, observers, no-held, guards).
- 3 critical systems + atomicity/EventBus/get_service contracts protected/enhanced.
- Full traceability + docs (CLAUDEs cross-domain sections, decisions entries, PLANs/SUMMARIES/gsd logs).
- Arch: consistent PASS / PASS WITH NOTES (0 critical violations across tirón).
- Tests: hundreds passed (targeted + broad); 0 new attributable regressions; pre-exist documented.
- GSD/scope: total discipline; 4 items max; tight per PLANs.

**Files/Artifacts (key across tirón):** Multiple .planning/phases/*/PLAN.md + SUMMARY.md + gsd-*.log (per item); .claude/agent-memory/{impact-analyzer,arch-enforcer,test-guardian}/* (per item + MEMORY.md); services/ (3-5 per item: locals, listeners, normalization); handlers/ (conversions + 1-lines); tests/ (extensions + 1-lines + guards); bot.py (regs); CLAUDEs/decisions.md (cross sections + entries + BATCH); golds untouched but re-run/protected.

**Subsequent Tirón / New Pool of 4 (post foundational tirón above; Item 7/25 + Item 8/26 closed after tests passing per user; current new pool: Item 9/27 complete via full 6-step, Item 10/28 early stages):**

**Pool note (verbatim, per mandate in all recent artifacts):** Pool anterior de 4 cerrado (tests passing per user). Nuevo pool de 4 iniciado. Quedan ~2-4 clusters del análisis inicial después de este pool.

**Prior sub-pool (Item 7/25 + Item 8/26, closed):** 
- Item 7/25 (first of its new pool of 4, per 25-reward-handlers-1service-loc-SUMMARY.md + PLAN + gsd-reward-handlers-1service-loc.log + impact item7): reward_user_handlers consolidate to exactly 1 service (MissionService via get_service + rel mission.reward) + extract pure helpers (compute_reward_status_text, build_reward_detail_keyboard) for <=50 LOC (reward_detail 50->36); min support/confirm pure get_reward_emoji + 1-line delegate in reward_service (already present); test ports + new TestRewardUserPureHelpers (5+ import-inside); UI 1:1 identical (texts/emojis/bars/buttons/cb); logging standard; 0 behavior/0 delivery/0 atomicity/0 other handlers/0 mission_svc; self-check PASSED; handoff "Item 7/25 closed. First of new pool of 4. Previous batch of 4 ... closed with tests passing per 24-SUMMARY... Ready for arch-enforcer + test-guardian + siguiente item del pool"; 3 crit protected (read-only gamif/missions/rewards info; narr/VIP 0 tx). GSD 40+.
- Item 8/26 (second of its new pool of 4, per 26-store-admin-long-funcs-SUMMARY.md + PLAN + gsd-store-admin-long-funcs.log ~800+ + impact item8): store_admin_handlers long funcs (>50) to <=46 + exactly 1 service (StoreService via get_service + delegate get_available_packages_for_store for wizard pkgs); 6+ pure helpers (compute_stock_emoji_and_text, compute_restock_new_stock, build_*_text_and_buttons, build_product_detail_keyboard, build_product_confirmation_text_and_keyboard, build_delete_confirm_keyboard etc.; verb+context+result; "Función pura"); min support thin delegate + pure top + 1-line + arch comments in store_service; port 5 TestProcessProductDescription tests (from direct PackageService patch to get_service + mock_store delegate + assert on store); add TestStoreAdminPureHelpers (9 import-inside pure tests); UI 1:1 (emojis ♾️🚨⚠️📦, "📝 Reabastecer: name[:25]", "Resumen del producto", "Sin descripcion", "Paso X de 5", backs, trunc, wizard FSM, empty cases preserved in puros); logging standard; 0 behavior/0 CRUD/stock/delivery/gamif credit/0 other handlers/0 package_svc core; ruff (pre-exist hygiene only non-reg); self-check PASSED; handoff "Item 8/26 closed. Second of new pool of 4... Ready for arch-enforcer... + test-guardian + siguiente"; 3 crit protected (gamif read-only stats aggregates; narr/VIP 0; re-runs protect). GSD total.
- Pool close: "Pool anterior de 4 cerrado (tests passing per user). Nuevo pool de 4 iniciado. Quedan ~2-4 clusters del análisis inicial después de este pool." (per 25/26 SUMMARYs + user confirmation after tests passing).

**Current new pool of 4 (Item 9/27 + Item 10/28):**
- **Item 9/27 (first of this new pool of 4; FULL pipeline complete: impact->gsd-planner->gsd-executor self-check PASSED->arch-enforcer->test-guardian->tests green; per 27-mission-admin-long-funcs-SUMMARY.md + PLAN.md + gsd-mission-admin-long-funcs.log (79+) + .claude/agent-memory/impact-analyzer/item9-mission-admin-long-funcs.md + arch-enforcer/item9-arch-audit.md + test-guardian/item9-test-guardian-report.md):** Refactor mission_admin_handlers.py long funcs (>50 common in create wizard 6-step process_*/select_*/confirm + list/detail/delete/stats) to <=50 + exactly 1 service per handler (MissionService via `with get_service(MissionService) as ...` for all 9+ entrypoints incl all wizard message/cb steps; remove bare RewardService import + 2 instantiations at select_frequency/select_reward_for_mission ~302/362, replace with mission_service.get_all_rewards_for_mission_wizard() / get_reward_for_mission_wizard(reward_id) via thin delegates inside with). 10+ pure helpers extracted (compute_mission_wizard_step_text, build_mission_confirm_text_and_keyboard, build_reward_select_buttons, compute_reward_summary_for_confirm, build_mission_list_entry_and_button, build_mission_detail_text_and_keyboard (dedupe show), build_mission_delete_confirm_keyboard, build_mission_stats_text_and_buttons, compute_freq_text, compute_reward_text, build_mission_type_select_keyboard; verb+context+result; pure docstrings "Función pura (sin estado ni side-effects). Soporte para UI de admin missions (wizard/list/detail). 1:1 de lógica previamente inline (item9, arch-enforcer). Precedent item7/8."); all target long funcs slimmed (max 50 via inspect.getsourcelines post F3/F5: select_frequency 50, confirm_create 49 etc; puros max 49); min support ONLY in mission_service.py (thin delegates get_all_rewards_for_mission_wizard + get_reward_for_mission_wizard with exact "Thin delegate to RewardService.get_all_rewards(active_only=True). Added for item9: enables mission_admin_handlers reward select steps ... to call exactly 1 service (MissionService)... 0 behavior change. Precedent item8"; + arch comments "# Support added for mission_admin_handlers 1-service + pure extract (item9). Arch-enforcer long-funcs note addressed. Precedent item7 (reward) + item8 (store-admin)."; 0 core CRUD/deliver/increment/claim/atomic/held change); update ONLY tests/handlers/test_mission_admin_handlers.py (port ~8-12 RewardService patches in TestSelectFrequency (3) + TestSelectRewardForMission (4+) to @patch get_service + __enter__/__exit__ + mock_mission_svc delegate setups + assert on mission_svc not bare; setups post-assign for real puros; keep exact UI/data/state/"No hay recompensas"/"Resumen de la mision:"/"Paso X de 6"/"Una vez"/"Recurrente"/"Sin descripcion"/"Ninguna"/advances/clears/cb targets; update 2 class docstrings "Tests ported to 1-service pattern (get_service(MissionService) only + delegate for reward wizard steps). Arch-enforcer note addressed. Precedent from item7/8."; add TestMissionAdminPureHelpers at end (11 import-inside pure unit tests no @patch on helpers: wizard steps Paso1-6 + Lucien + example, confirm full/wo desc/reward "Resumen..."/"Sin descripcion"/"Ninguna", reward select kbs len/rows/texts/cb ids/back "admin_missions", list entry status+name[:30], detail rel/None "Sin recompensa" + 3row kb, delete, stats, freq, edges/empty; all assert exact pins); full 55p + pure 11p green); logging estándar "mission_admin_handlers | <action> | user_id=... | resultado=..." inside withs post-success (list + confirm); UI render 1:1 identical (ALL pins from impact/PLAN preserved exactly in puros + handlers: "Paso 1 de 6: Nombre de la mision...", menu "➕ Crear mision"/"🎁 Crear recompensa"/"📋 Ver misiones"/"📊 Estadisticas", "Resumen de la mision:", reward none "Ninguna"/"Sin recompensa", "Sin descripcion", "Una vez"/"Recurrente", "No hay misiones registradas."/"No hay recompensas configuradas...", "Estas seguro de eliminar", "✅ Mision eliminada correctamente."/"Mision creada exitosamente", backs "admin_missions"/"list_missions", trunc [:30], cb packing (Mission*Callback, SelectRewardMissionCallback), empty/error, Lucien 3rd person, wizard FSM 6 steps/states/transitions, freq/status); ruff (pre-exist hygiene E402 bottom import in test per 26 precedent, non-reg); rules verif (0 bare RewardService active in handler (grep), LOCs all <=50 via inspect, logging in main withs, 9 withs + delegates + 10+ puros + Test* 11, 1svc+<=50+logging+pure+no biz); GSD pre every (79+ entries total); self-check PASSED + critical tests list (handler full 55, pure -k subset, cross -k "mission_admin or ... or TestMissionAdminPureHelpers or build_ or compute_ or select_reward or select_frequency", reward get_all/get_reward gold 4p, bot smoke, ruff+greps+LOC); handoff "Item 9/27 closed. First of new pool of 4. Previous pool closed with tests passing per user. Ready for arch-enforcer re-scan (enfocado en mission_admin_handlers: exactly 1 service + <=50L + no direct RewardService + puros + ports + UI1:1 + logging + comments) + test-guardian (correr los tests críticos listados) + gsd-executor del siguiente item del pool de 4."
  - Arch-enforcer (item9-arch-audit.md): **PASS WITH NOTES** (0 critical violations); 9 with get_service(MissionService) confirmed (grep); 0 RewardService in handler; 11 puros + LOC verified via terminal inspect (max 50 entry/49 puro); delegates exact + comments; UI 1:1 + Lucien preserved; logging/UI/cbs preexist CallbackData; 3 crit protected (gamif: admin mission/reward config read+admin-mutate only, orthogonal to credit/increment/deliver/atomic golds + re-runs of cross/reaction_mission/reward gold protect indirectly; "admin create is orthogonal to user progress/claim" per prompt/SUMMARY; narrative 0 direct; channel/VIP 0 direct); medium notes = pre-exist/out-of-scope per tight (min logging coverage for some withs, other wizards long/legacy debt e.g. reward_admin, ruff E402 hygiene in test per 26 precedent "do not count as regression", handler CLAUDE legacy examples); strong traceability (GSD 79+ executor + 34+ this audit with pool/BATCH/self-check, impact, 27-SUMMARY/PLAN, code comments, test docstrings); no new long/0 creep/0 beh/0 atomic/0 delivery/0 other/0 prod chg. "Pool anterior de 4 cerrado (tests passing per user). Nuevo pool de 4 iniciado. Quedan ~2-4 clusters del análisis inicial después de este pool." Ready for test-guardian + next pool item.
  - Test-guardian (item9-test-guardian-report.md): **suite protege adecuadamente**; re-runs exact per PLAN F5/SUMMARY critical list (all green, pre-exist only non-attrib per 25/26/24 precedents): full handler `pytest ... test_mission_admin_handlers.py` 55 passed (1 pre warn MovedIn20); pure subset -k "TestMissionAdminPureHelpers or compute_mission_wizard or build_mission_confirm or ..." 11 passed; broader cross -k "mission_admin or admin_missions or TestMissionAdmin or mission or reward or TestCross or atomicity" 179 passed +1 pre xfail +15 pre warns (MovedIn20, Runtime AsyncMock/emit never awaited, deprec, SA, unraisable etc from priors/m w-hardening); reward gold spot -k "get_all_rewards or get_reward or active_only" 4 passed (exercises real delegates passthrough); bot smoke + delegates (has get_all_rewards_for_mission_wizard/get_reward_for_mission_wizard True) + ruff on 3 (E501s in comments/pures/style, 1 pre E402 bottom states import per 26 file conv for inline class, documented non-reg "do not count as regression"; 0 attributable logic); final greps/inspect: 0 bare RewardService (handler+test), 9 with get_service(MissionService), 55 tests in file, puros defs + exact pure docstrings, logs in list/confirm, delegates in svc + arch comment, LOCs <=50, pure tests cover 1:1 UI/calc pins ("Paso X de 6"/"Resumen de la mision:"/"Sin descripcion"/"Ninguna"/"Una vez"/"Recurrente", reward buttons len/rows/texts/cb ids/back, list entry status+trunc30, detail rel/None "Sin recompensa" + 3row kb "Desactivar"/"🗑️ Eliminar"/"🔙 Volver", delete, stats, freq, edges/empty, Lucien); coverage: 11 pure direct exercise real puros (import inside per conv, post-assign MagicMock .name=/.value= for real exec, no @patch on helpers); ports faithful (get_service + __enter__/__exit__ + delegate mocks + mission_svc asserts + __exit__ closes + docstrings "ported..."); 3 crit safe (gamif orthogonal admin config only + re-runs protect; narr/channel 0 direct per impact/PLAN/SUMMARY); pre-exist handling verbatim "non-regression" "do not count as attributable to Item 9" "pre-exist per 25/26/24 precedents". GSD pre every (40+ this log + 80+ executor); no new files/gaps (tight: existing + 11 added in F4 sufficient per PLAN/precedents 26/25); veredict "suite protege adecuadamente". "Pool anterior de 4 cerrado (tests passing per user). Nuevo pool de 4 iniciado. Quedan ~2-4 clusters del análisis inicial después de este pool." Handoff to close Item9 + next in pool.
  - Outcomes (from SUMMARY self-check + reports): 0 behavior/0 atomicity/0 delivery/0 other handlers/0 reward_svc core/0 prod chg (UI identical, tests pin exact strings/emojis/cbs/math, cross re-runs protect); 0 mission/reward CRUD/deliver/increment/claim/atomic/held change (delegates transparent passthrough; core untouched); scope tight (only 3 files + log + SUMMARY per impact/PLAN); GSD total; 3 critical systems protected (gamif: admin mission/reward config orthogonal to credit/debit/increment/deliver/atomic golds + "post-credit best effort (misiones + listeners)"; re-runs of cross_service_atomicity + reaction_mission_flow + reward gold protect indirectly; narrative 0 direct; channel/VIP 0 direct); arch PASS WITH NOTES (0 critical); test-guardian "suite protege adecuadamente"; self-check PASSED + BATCH/POOL note; handoff explicit. "Item 9/27 closed. First of new pool of 4."
- **Item 10/28 (second of this new pool of 4; impact + gsd-planner complete, executor next; per .planning/phases/28-remaining-besito-store/PLAN.md + .claude/agent-memory/impact-analyzer/item10-remaining-besito-store.md + gsd-impact-analyzer-item10...log):** Reduce direct BesitoService composition in StoreService (debits in complete_order/direct_purchase + balance checks in purchase flows). Use local on-demand `BesitoService(db=self.db)` *only inside the debit/balance call sites* (complete_order ~493 debit + rechecks ~488, direct_purchase ~366, create_order ~440; copy Reward Item5 _deliver + broadcast/game Item6 credit sites al pie de la letra for atomicity + history + PURCHASE source + tx row); remove held `self.besito_service = BesitoService(db)` from _init_services (package_service co-held untouched); optional high-value observational EventBus listener `on_besitos_awarded_store_observer` at module bottom (copy story_service 670-694 + reward/broadcast expanded templates exact: "Cross-domain event listeners" block + "MUST NOT credit/debit/mutate besitos state here" + best-effort + DESIRED CONTRACT + domain log "store | besitos_awarded_received"; purely obs, 0 mutation, 0 re-entrancy with purchase debit paths); central explicit reg in bot.py on_startup (extend cross-domain listeners block after narrative+rewards+broadcast+game from Item5/6; add import + register + extend log line + comment "+ Item 10 store"); 1-line/guard test ports only (no new tests/cases/files: cross atomicity.py + store unit test_store_service.py e.g. "bal = BesitoService(db=...) if not hasattr... else ..." or class patch("services.besito_service.BesitoService") for local intercept + comment "# 1-line/guard port post Item10 (local besito in store complete_order per Item5/6 precedent; arch-enforcer)"; targeted docs appends (services/gamification/CLAUDE.md or store/CLAUDE.md + missions/CLAUDE.md + decisions.md new Item10 entry mirroring Item5/6 style + BATCH/POOL)); 5 small phases (F1 prep/GSD/baseline/greps/golds/LOC/UI pins/patterns read + 24/25/26/27 precedents + impact exact; F2 store_service locals in 3 purchase sites + remove held + comments + opt observer + arch; F3 bot.py reg if listener + comment; F4 1-line/guard ports in tests; F5 re-runs golds + rules verif + self-check + handoff); 0 behavior/0 atomicity change (purchase paths return identical; debit internal commit + PURCHASE tx + order/stock/deliver outer commit unchanged; "credit survives deliver False" + "post-credit best effort (misiones + listeners)" protected even for debit analog by golds with patch schedule_emit + DESIRED + TestSession/file + strict + 777 TG + gather; listeners best-effort only); 0 other files (0 store_user -- already get_service; 0 package/reward delivery/backpack/streak; 0 story deliberate keep); 0 change to core StoreService CRUD/purchase except the composition sites; 3 crit protected (gamif primary: store purchase debits + possible future listener; narr 0 direct per precedent; channel/VIP 0); risks low (atomicity golds precedent-proven); gates: cross atomicity full happy/sad + patch + DESIRED + strict tx/delta/balance/"credit survives"; broader -k "store or purchase or complete_order or atomicity or besitos"; unit store targeted; bot smoke reg+emit; ruff; greps (0 held post, locals w/ comments, observer + "MUST NOT" + DESIRED + "store |", bot reg "+ Item 10 store", 1-line/guard in tests); self-check PASSED + pool phrase + "Item 10/28 closed. Second of new pool of 4...". (Impact + planner done; executor next per handoff.)
- Overall for this pool (2/4 done): 3 crit + atomicity/EventBus/get_service contracts protected (gamif for mission admin config + store purchase debits orthogonal/read+mutate only + golds protected "credit survives deliver False" + "post-credit best effort (misiones + listeners)"; narr/VIP 0 direct; EventBus observer "MUST NOT" + DESIRED CONTRACT); GSD discipline; scope tight per PLANs; arch PASS WITH NOTES (0 crit for Item9 + Item10); test veredict "suite protege adecuadamente" for both; UI/behavior identical; traceability full (SUMMARYs self-checks + agent reports + gsd logs + PLANs + code comments + test docstrings + decisions/CLAUDE appends); BATCH/pool notes + verbatim phrases. "Pool anterior de 4 cerrado (tests passing per user). Nuevo pool de 4 iniciado. Quedan ~2-4 clusters del análisis inicial después de este pool." "Item 10/28 closed. Second of new pool of 4."

**Note on documentador agent:** New agent (per .claude/agents/documentador.md role, created/ launched for post-tirón/pool HARDENING_ROADMAP updates + tirón learnings persistence + own .claude/agent-memory/documentador/ reports + GSD pre-log discipline mirroring impact/arch/test-guardian). Invoked automatically at end of pool (after last item test-guardian + tests green + self-check); this invocation (for Item 10 close, second of new pool of 4 after Item 9/27 first closed full 6-step) updated ROADMAP (this sec + sec5 + metrics + pool/BATCH notes), persisted tiron report + MEMORY pointer. No code changes. Source of truth: the SUMMARYs + self-checks + gsd logs + agent reports of the tirón (esp. 28-remaining-besito-store-SUMMARY.md self-check PASSED + 27/26/25/24 precedents + item10 impact/arch/test-guardian reports + gsd-*-item10 + 28-PLAN + prior ROADMAP post-Item9 update). "Pool anterior de 4 cerrado (tests passing per user). Nuevo pool de 4 iniciado. Quedan ~2-4 clusters del análisis inicial después de este pool." "Item 10/28 closed. Second of new pool of 4."

**Documentation sync / claude-md-sync meta-work (2026-06-11, executed by documentador agent per .claude/agents/documentador.md + claude-md-sync.md role to "dejar ya bien sentada toda la información"):** 
- Objective: Comprehensive audit of root CLAUDE.md + services/CLAUDE.md + handlers/CLAUDE.md + models/CLAUDE.md + decisions.md + AGENTS.md + rules.md + HARDENING_ROADMAP.md + recent tirón artifacts (27/28/29 *-SUMMARY.md + PLAN.md) + agent-memory reports (impact/arch/test-guardian/documentador) + code evidence (bot.py wiring/listeners/health, health_service.py) + GSD pre-logs to dedicated gsd-documentador-claude-sync-*.log + wc -l before reads/writes. Codify hardener (pools max 4 chained, exact 6-agent seq per item impact→planner→executor (GSD pre inside + self-check PASSED) → arch-enforcer (PASS WITH NOTES 0 crit) → test-guardian ("suite protege adecuadamente" + golds re-runs) + explicit documentador at pool close for ROADMAP/learnings + agent-memory report + MEMORY pointer) as the agile standard/preferred for telegram-bot-hardener (tight 0/0/0, 3 crit gamif/narrativa/canales-VIP + atomicity/EventBus/get_service contracts protected, core rules intact, lighter than full /gsd:execute-phase for this scope).
- Files/scope (tight): Updated/verified root CLAUDE (Hardener Workflow section + "Workflow para Hardening (resumen accionable)" 5 steps + pool phrase + Health/Observability in domains table + full Documentos refs incl HARDENING_ROADMAP + documentador.md + claude-md-sync.md + .claude/agents hardener + services/CLAUDE cross); services/CLAUDE (Health row + Observability + hardener precedent for new/cross services + refs 29-SUMMARY + claude-md-sync); handlers/CLAUDE (new "Patrón Probado en Hardener (tirones 25-29 / Items 7-11)" for 1-service via get_service + puros for long admin <=50 LOC + delegates + Test*PureHelpers + arch/test veredicts + UI 1:1 + refs 27/26/25 SUMMARYs + root CLAUDE); decisions.md (full adoption entry after Item10, exact Motivo/Riesgos/Decisión/Resultado style with evidence from tirones 25-29/Items5-11 esp 27-29 SUMMARYs + arch "PASS WITH NOTES 0 critical" + test "suite protege adecuadamente" + self-checks + golds + 0/0/0 + pool phrase + BATCH + handoff + claude-md-sync note); light AGENTS.md (HealthService in services list + changelog note on hardener standard + "current details in CLAUDEs/ROADMAP"); this ROADMAP meta note (documentador role); persisted/updated .claude/agent-memory/documentador/claude-md-sync-hardener-standard-2026-06-11.md (full audit + verification pass + citations) + MEMORY.md pointer.
- Outcomes + verif: Pattern seated (no material drift; all requested codif present and matching primary sources SUMMARYs/PLANs 27-29 + agent reports + gsd logs + code); greps confirmed pool phrase verbatim (CLAUDE.md + decisions.md + cross CLAUDEs), HealthService (services/ + CLAUDE table + hardener pattern section), get_service 1-call in handlers, hardener/Item 9/10/11/tirón/pool terms in docs; code reality matches (bot.py has health on_startup + listeners from Item10 + _BOT_START_TIME; health_service.py exact read-only/Analytics/ logging / <=50 / 3 crit note / arch comment); GSD pre + wc total (47+ in dedicated log); 3 crit + contracts + core rules (1svc/handler, <=50, verb+ctx+res, "módulo | acción | user_id | resultado" logging, is_admin, get_service, EventBus MUST NOT mutate, atomicity golds) preserved/enforced; pool phrase + "Item X/2X closed. Nth of new pool of 4" + self-check PASSED + arch/test veredicts + documentador handoff in all sources.
- BATCH: Documentation sync / claude-md-sync verification + seating of hardener standard (1 meta "item" in pause). 
Pool anterior de 4 cerrado (tests passing per user). Nuevo pool de 4 iniciado. Quedan ~2-4 clusters del análisis inicial después de este pool.
"Documentación del tirón/sincronización actualizada. HARDENING_ROADMAP y CLAUDEs listas para el siguiente tirón o pausa."

(End of current pool section + meta sync note. For next tirón: follow now-documented hardener flow + launch documentador at close per .claude/agents/documentador.md.)

**Phase 30 — Channel Admin Hardening (2026-06-15; feature-advisor scope #1/#2/#4/#5/#6; implement-review loop, 0 open issues post re-review):**

- **Objetivo:** Fortalecer módulo admin de canales (sistema crítico #3): guards Custodio en todos los callbacks, wait time custom 1–1440 min, aprobación masiva con grant real en Telegram, editor mensajes approval/welcome, gestión individual pendientes con paginación 8/página y status `rejected`.
- **Key files:** `services/channel_grant.py` (nuevo — orquestación TG+BD compartida scheduler/admin), `services/channel_service.py` (approve/reject individual+bulk, mensajes, `get_valid_pending_request` IDOR guard), `services/scheduler_service.py` (delega loop a grant helper), `handlers/channel_handlers.py` (seguridad + FSM + UI admin), `handlers/free_channel_handlers.py` (welcome custom), `keyboards/callback_data.py`, `utils/lucien_voice.py`, tests unit/integration/handlers nuevos o extendidos, `services/channels/CLAUDE.md`.
- **Cambio de contrato explícito:** `approve_all_pending_now` **sí** llama `approve_chat_join_request` + welcome; test `test_free_entry_flow.py` invertido (antes esperaba solo BD).
- **Outcomes:** `is_admin` en todos entrypoints admin canales; 1 service `ChannelService` via `get_service`; puros para ≤50 LOC; grant centralizado preserva comportamiento scheduler gold; mensajes custom wired en scheduler + free entry; IDOR guard en approve/reject individual; HTML escape en previews; errores parciales en bulk approve transparentes; 0 impact gamif/narrativa; 3 crit channel-VIP protegido (pending/approve/welcome/scheduler).
- **Verification:** `pytest tests/unit/test_channel_service.py tests/unit/test_channel_grant.py` green; `pytest tests/integration/test_free_entry_flow.py -k "Scheduler or approve_all"` green; `pytest tests/handlers/test_channel_admin_handlers.py` green; smoke broader 114p (1 pre daily concurrent flake doc non-reg); implement re-review 0 open issues.
- **Docs:** `30-channel-admin-hardening-SUMMARY.md`, `services/channels/CLAUDE.md`, `decisions.md` Item 12, `handlers/CLAUDE.md` patrón channel admin, este ROADMAP.
- **Handoff:** "Phase 30 channel admin hardening closed (tests passing). Ready for next hardening cluster or feature work."

**Phase / Pool 2026-06-23 — Broadcast: default reacciones + botones extra tg links (hardener-agile effort=5; items via quick/20260623-broadcast-link-buttons-item1 + item2; "definir primero, máx 1 opcional en flujo"):**

- **Pool context:** Hardener-agile (tight scope, 6-agent seq per item + explicit documentador at close). Pool name in artifacts: "broadcast-default-reactions-and-extra-link-buttons". Item1: catalog foundation only (model + mig + service). Item2: wizard integration (max 1 optional). ITEM3 default reactions + minimal admin "definir primero" referenced in pool/g sd logs (out of scope for these SUMMARYs/PLANs). 0 behavior/0 atomicity/0 impact on protected paths. Source of truth: item1/2 PLAN.md + *-SUMMARY.md (self-check PASSED) + impacts + arch + test-guardian + gsd logs (pre every + self-check).
- **Item 1 (broadcast-link-buttons-item1, catalog only):** BroadcastButton model (id/label/url/desc/is_active/created) + nullable extra_button_id FK on BroadcastMessage (same mig) + BroadcastService 6 CRUD (create_broadcast_button etc; copy ReactionEmoji verbatim: get-then-mutate, active_only, toggle semantics, logging "broadcast_service | create... | label=... | url=... | id=..."; <=15 LOC each) + TestBroadcastButton (6 tests CRUD+filter+toggle+partial) + models export + new alembic/20260623_add... (down_revision correct, downgrade impl, batch for SQLite) + heads + upgrade/downgrade cycle + full gold re-runs (alembic 4p, broadcast svc 20p, reaction_flow 22p, cross_atomicity 10p gold, reaction_* 2+1+3+4p, callback 24p, gamif reaction 26p, smoke). Scope locked (0 handlers/wizard/markup/create sig change/default reactions). GSD pre (log 31 lines). Ruff clean. Fixes round post-review (0 open). self-check PASSED. Arch: **PASS** (0 critical). Test-guardian: **"suite protege adecuadamente"**. 0 impact 3 crits (gamif reactions untouched: check_and_register_reaction etc; nullable FK inert on fixtures); atomicity/EventBus/get_service protected.
- **Item 2 (broadcast-link-buttons-item2, wizard integration):** 2 new FSM states (waiting_extra_button_decision, selecting_extra_button) after reactions + ToggleExtraButtonCallback(prefix="bc_extra") + 4 pure helpers extracted (build_broadcast_send_markup "Función pura..." 31LOC, persist_broadcast_from_state 20LOC, build_broadcast_preview_text, build_extra_button_selection_keyboard; import-inside TestBroadcastPureHelpers 4 cases) so confirm_and_send_broadcast 166 LOC (delta -8 <=0); wizard flow: ask_for_extra (get_service load, auto-skip empty catalog, decision kb) → show_extra (single choice replace, "⏭️ Ninguno" id=0, ✅ exclusive, continue) → protection; backs wired; preview shows "Botón extra: label (url)" / "❌"; step nums "Paso X de 7" (extra=4, protection=5); create sig + extra_button_id=None param + store; send builds combined markup (reactions row if + optional url row) post message_id edit; refresh_reaction_markup_counts preserves (uses stable reactions_keyboard_with_counts for react row + manual append for url; getattr(broadcast,"extra_button_id",None) + isinstance(int) guard for mocks; never mutates reactions_keyboard). 8 new tests (service create extra with+default; cb prefix+collision len=4; 4 pure; refresh preserve 2rows+url). Golds re-ran exact per PLAN (71p+ baseline all green post incl reaction golds + new; 0 attributable reg). Scope locked (0 default reactions, 0 admin UI, 0 >1 button, reactions_keyboard sig/body untouched, "ninguno" default=None). GSD pre (log 50 lines + self-check). Ruff clean. self-check PASSED. Arch: **PASS 0 critical**. Test-guardian: **"SUITE PROTEGE ADECUADAMENTE"**. 0 impact on reaction credit/atomicity (golds hold; explicit preserve test + pure combined: row0 react: cb, row1 url); EventBus/get_service/3 crit protected (markup post-send best-effort orthogonal).
- **Key verifs (from SUMMARYs + agent reports):** All golds per exact PLAN cmds + flags (-q --tb=line -p no:cov --override-ini="addopts=") green: alembic_heads, broadcast_*, cross atomicity (gold), reaction full_chain/invariants(-k)/limit/mission, callbackdata (29p post), gamif_user (-k reaction 27p), smoke. New coverage: CRUD, puros (reactions_only/extra_only/combined/none), bc_extra unique, create extra, refresh preserve. 3 crit + contracts: gamif reactions (check_*/credit/observers untouched; atomic golds "credit survives" + best-effort hold); 0 mutation. UI: single choice enforced (replace; 0 or 1); combined markup (reactions first callbacks, extra url second); preview accurate; empty→auto-skip.
- **Outcomes:** Feature slice complete per scope (catalog first + max1 optional in flow). "definir primero" enabled (catalog service + minimal admin note from item3 gsd). Review limpio (item1 0 open post fixes). self-check PASSED both. 0 beh/0 atomicity/0 prod change.
- **Pool close:** "Pool anterior de 4 cerrado (tests passing per user). Nuevo pool de 4 iniciado. Quedan ~2-4 clusters del análisis inicial después de este pool."
- **Handoff:** "Pool cerrado, tests passing, review limpio, feature completa." Ready for review or next cluster. GSD total + documentador per role. Full traceability in SUMMARYs + .grok/.claude agent-memory + this ROADMAP.

---

## 5. What Is Missing / Roadmap (Gaps + Proposed Next)

**Remaining from Initial Analysis (not fully closed by this tirón; prioritized):**
- **Long functions / admin bloat (deuda media-alta, flagged in initial + arch notes):** Store/mission/reward/promotion admin wizards (process_*/confirm_* >50 LOC common; some still multi-service orchestration or "lógica en handlers"). Reward handlers still noted for 2 services in some paths (pre-exist but visible post-cleanup). Game/broadcast some >50 preserved (tight scope). **Channel admin (Phase 30 closed):** guards, grant real, mensajes, pendientes individual — ver sec 4 Phase 30.
- **Remaining direct Besito compositions:** Store (debits in complete_order — critical atomic but out-of-scope in Item6 tight); story (deliberately kept for debit commit=False + _grant credit); some handlers (direct for admin/anon queries — legacy/out-of-scope); backpack/streak (locals already good precedent).
- **Test / coverage gaps (media, per skill + impact recs):** More explicit max limits/global caps in gamif; full FSM restart with real Redis sim; more invalid narrative + EventBus desbloqueo tests; deeper channel/VIP (multi-tariff edges, free pending after VIP expire); full handler E2E for "mensaje correcto" (Lucien voice on insufficient); property-based or more concurrent in other flows; health/observability (no /health yet); full Redis for rate/idemp (in-mem still).
- **Other fragility/deuda (from initial + arch/test-guardian notes):** Pre-exist daily concurrent flake + some VIP flows (doc non-reg); N806 in golds (tolerated); deprecation utcnow (game/streak tests); inconsistent logging in some paths; no full rate/idemp Redis; health endpoint; broader docs drift (some CLAUDEs still reference old patterns); analyzer noise reduction; full handler modernization (legacy direct Service() in some admin).
- **Cross-cutting (future):** Expand EventBus (more listeners for streaks/promo/hooks); more get_service in remaining legacy; timeout on external (DB/Telegram); rate per-action or smarter (beyond per-user); structured logging everywhere; health check (DB, bot, channels, bus, scheduler).

**Proposed Next (if new tirón, max 4; high-value, builds on prior + handoffs; use same pipeline):**
Completed by pool 34 (see detailed section above + SUMMARYs/gsd):
1. Remaining user flows test reality hardening (story/backpack/mission int real; scaled; golds green).
2. Continue long admin wizards modernization (reward_admin 1svc + 11+ puros + LOC; delegates; tests).
3. Observability + health spike + docs hygiene (logging align + /health verify + CLAUDE sync to 1svc/puros/get_service).
4. Test gaps/hygiene clusters (explicit caps, E2E insuff Lucien, FSM restart, VIP edges; golds protected).
After pool 34: clusters from initial mapeo / pool33 Proposed largely addressed (tests reality + wizard puros + hygiene/obs + gaps). Remaining ~0-2 from initial analysis (e.g. full Redis rate/idemp, additional wizard paths if surfaced, broader EventBus, more edges). 
Proposed Next (max 4 if requested): 1. Additional admin wizard modernization (if bloat remains post reward); 2. Full Redis backing for rate/idemp (builds middleware); 3. Expand EventBus listeners + structured logging coverage; 4. Deeper channel/VIP or gamif cap property tests + FSM real Redis sim. Prioritization: 3 crit + contracts; tight 0/0/0; GSD/PLAN; arch/testg + documentador.

**Prioritization Criteria (for future):** High impact on 3 critical systems (gamif/narrative/channel-VIP) + atomicity/EventBus/get_service contracts; tight scope (verifiable via golds + self-checks); GSD/PLAN-driven; low risk (0 behavior/0 atomicity); builds on completed (1svc+puros pattern from Item7/8/9, locals+obs from Item5/6/10, middleware/get_service/EventBus); addresses initial "fragility" or "deuda" with clear metrics (e.g., 0 held, tests pass, <50 LOC, arch PASS WITH NOTES, test-guardian "suite protege adecuadamente").

**Metrics of Success (this tirón + ongoing):**
- 0 critical arch violations (achieved: all PASS/PASS WITH NOTES across tirones; Item9 specifically PASS WITH NOTES 0 critical per arch-enforcer/item9-arch-audit.md; broadcast items1/2: PASS 0 critical per their arch reports; pool 33 Items 1-3: PASS WITH NOTES 0 crit per 33-item*-arch-audit.md).
- 0 attributable regressions in golds/chains (achieved; pre-exist only, documented non-reg per 25/26/24/27 precedents in SUMMARYs/test-guardian; broadcast: all 69→71p+ baseline golds + reaction chains green post; pool 33: atomic 25p id, cross 10p, reactions 9p, daily 19p, vip 37p, broader 778p+8xf + new int/E2E all green 0 attr reg).
- Scope tight + GSD discipline (achieved per logs/self-checks 40+/79+/800+ entries + broadcast 31/50 lines; 3 files max per item + exact PLAN lists; pool 33: GSD pre 50+/item, wc 371 main log + 6+ docu log, tight per mapeo/PLAN In/Out).
- 3 systems + contracts protected/enhanced (achieved; gamif orthogonal admin config + store purchase debits protected via re-runs + golds "credit survives" + "post-credit best effort"; narr/VIP 0 direct; EventBus/get_service/atomicity upheld; broadcast: 0 mutation on check_and_register_reaction/credit/observers; reactions_keyboard stable; markup best-effort orthogonal; pool 33: store/promotion orthogonal, atomic gold verbatim untouched, 1-line/guard + real DB for debit paths, get_service 1 call unchanged).
- Documentation/traceability (this ROADMAP + per-item PLANs/SUMMARIES/gsd-logs/agent-memory + cross CLAUDEs/decisions planned; cites to 27-SUMMARY etc.; broadcast: explicit SUMMARY self-checks + arch/test veredicts + pool phrase; pool 33: full SUMMARY/gsd/arch/testg/self-check + documentador report + ROADMAP append + MEMORY pointer).
- Pool/tirón success: Item9/27 full 6-step (executor self-check PASSED 55/55+11 pure, arch PASS WITH NOTES, test-guardian "suite protege adecuadamente" with 55+11+179+4 green + coverage on puros/ports/delegates); Item10/28 impact+planner done per PLAN; multiple verbatim pool phrase instances; BATCH notes; new documentador agent used for post-pool update. **Broadcast pool 2026-06-23 effort=5 (item1 catalog + item2 max1 optional integration):** arch PASS 0 crit x2, test-guardian "suite protege adecuadamente", golds 0 reg (71p+ incl explicit extra+reaction preserve), puros pattern applied, single-choice UI + "ninguno" default, 0 impact reactions atomicity/3crits/contracts, GSD pre+self-check, pool phrase, handoff "Pool cerrado, tests passing, review limpio, feature completa."; documentador closed per role. **Pool 33-test-reality-user-flows-store (effort=4, 4 items, tests-only):** 10+4+1 new integration tests (store purchase paths + E2E cap/tier/discount/complete + promo me interesa); mock reduction in store svc non-gold + promo unit (1 path real); 1-line/guard ports exact; golds protected 0 attributable regressions (atomic gold verbatim 25p id across); arch PASS WITH NOTES 0 crit (1-3) + Item4 self-check; testg "suite protege adecuadamente"; self-checks PASSED all + final pool close; GSD pre every (50+/item, 371 lines); review 0 open; 0/0/0 (0 prod/0 beh/0 atomicity); 3 crit + contracts 0 impact; UI 1:1; documentador used (ROADMAP + report + MEMORY); pool phrase verbatim x many + "Item X/33 closed. Nth of new pool of 4".
- Roadmap ready for next tirón (if user requests).

**Next Steps (post-pausa):**
- User reviews this doc (persist as .planning/HARDENING_ROADMAP.md or similar).
- If new tirón: user prompt with items (or "tú decide up to 4 high-value from roadmap"); we chain pipeline again (impact/gsd-planner/executor etc). Include remaining clusters from 33 mapeo (story/backpack/mission user real flows) or other high-value.
- Optional: arch-enforcer re-scan whole batch or specific (e.g. post pool 33); test-guardian re-run listed críticos from 27/28/33 + broadcast reaction golds; manual smoke of affected flows (store purchase complete/direct now with real int, E2E atomic, promo me interesa).
- Persist/update this file + MEMORY.md as living doc. (documentador agent used for this purpose per role; see .grok/agent-memory/documentador/33-test-reality-pool-close.md + MEMORY pointer + prior tiron-*.md reports).
- "Pool anterior de 4 cerrado (tests passing per user). Nuevo pool de 4 iniciado. Quedan ~2-4 clusters del análisis inicial después de este pool."

This document draws from initial hardener analysis, all impact/planner/executor/arch/test-guardian reports, PLANs/SUMMARIES/gsd logs, gold tests, CLAUDEs, and execution traces (esp. 25/26/27-SUMMARYs + item9 arch/test reports + item10 PLAN/impact + 20260623-broadcast item1/2 PLAN/SUMMARY + impacts/arch/testg for recent + pool 33 PLAN + 33-SUMMARY + 33 gsd 371 + 33-item* arch/testg + new tests). It is actionable as hoja de ruta. BATCH: prior + broadcast pool (item1+item2 effort=5, closed via documentador) + pool 33 (4 items effort=4 tests-only, closed via documentador) documented here. Pool phrase verbatim at every close.

**Fin del tirón / pool. Listo para pausa + review o nuevo tirón.** 🎩

**Handoff final (broadcast pool close):** "Pool cerrado, tests passing, review limpio, feature completa."

**Handoff final (pool 33 close):** "Pool anterior de 4 cerrado (tests passing per user). Nuevo pool de 4 iniciado. Quedan ~2-4 clusters del análisis inicial después de este pool." Report: .grok/agent-memory/documentador/33-test-reality-pool-close.md. ROADMAP + MEMORY updated. Ready for next.

**Pool 33-test-reality-user-flows-store (2026-06-26; hardener-agile effort=4 --hardening, tests-only per user request on mock fragility in user flows / tienda priority; 4 items completed; max pool 4 chained; explicit documentador at close):**

- Per impact mapeo (source of truth: .grok/agent-memory/impact-analyzer/33-test-reality-user-flows-mapeo.md full): ALTO risk for tienda (252 get_service + ~380 total mocks in test_store_user_handlers.py; pure isolation MagicMock for product/balance/effective_price/tier/cap/stock; bug in complete_order/debit/discount not detected). Map of user flows (store purchase paths highest priority as flujo económico crítico); promotion "me interesa" (66-80/174 mocks); others lower. Precautions: Item10 locals → 1-line/guard ports exact; complete_order post-commit (patch external only, assert DB state); rich fixtures (tiers/privileges/caps, telegram_id balance contract); no touch 3 crit golds (only re-run); get_service class patch pattern (not ctx); UI 1:1. Golds list exact (store atomic gold TestStorePurchaseAtomicGold + complete_order; cross_service_atomicity full; invariants I8; reaction_full_chain + reaction_mission_flow + reaction_limit; daily atomic; vip flows 3; broader -k "store or atomicity or mission or reaction or daily or vip"). Pool phrase: "Pool anterior de 4 cerrado (tests passing per user). Nuevo pool de 4 iniciado. Quedan ~2-4 clusters del análisis inicial después de este pool."
- PLAN + SUMMARY source: .planning/phases/33-test-reality-user-flows-store/{PLAN.md, 33-test-reality-user-flows-store-SUMMARY.md} (full F1-F6 per item + Item2/3/4 notes + review-fix round + self-checks PASSED + handoffs; GSD pre every; copy gamif_integration + atomic gold + 1-line/guard al pie de la letra; 0 prod/0 beh/0 atomicity; UI 1:1 Lucien; 3 crit + contracts protected).
- gsd log: .planning/quick/gsd-33-test-reality-user-flows-store.log (371 lines; GSD pre before every edit/gate/verif/ruff/pytest/grep/smoke/self-check; safe points + DoD marked; self-checks + pool phrase repeated; final Item4/pool close).
- Item 1/33 (store handler purchase paths to integration style; first of pool; new tests/handlers/test_store_user_handlers_integration.py): 10 tests (8 initial + review +2 cap/tier error branches); mirrors gamif al pie (pytestmark=integration, real_svc=StoreService(db_session), patch("handlers.store_user_handlers.StoreService") return real, handler→real svc→DB→UI 1:1 exact Lucien strings/keywords/emojis preserved); covers direct_buy (suff/insuff), confirm_direct_buy (success → COMPLETE + PURCHASE tx + delta via 1-line/guard; insuff after effective), product_detail (discount/effective), purchase_history (real order UI), + dedicated success/insuff per mapeo. Patch external only (PackageService.deliver); TestSession/file + expire_on_commit=False + full FK privilege chain seeds for complete visibility. 1-line/guard exact comment. Review fixes: 4 nits (history assert "adquisiciones"/"Adquisición #", discount "Precio de lista"/"ventaja activa", added TestCapTierErrorBranchesIntegration real svc cap/tier error branches, PLAN wording align). 10/10 pass. Spot golds green. GSD 50+ (wc50 F5). Self-check PASSED + phrase + handoff "Item 1/33 closed. First of new pool of 4. ... Ready for arch-enforcer re-scan (enfocado en store handler integration + service unit mocks + 1-line ports + 0 impact on 3 crit) + test-guardian (correr golds) + documentador + Item 2".
- Item 2/33 (reduce mocks in non-gold purchase paths test_store_service.py; second): ONLY TestRaceConditions (non-gold) edited: removed MagicMock query/spy_query/fs_inst/Fulfillment heavy; real db_session + real query + external-only PackageService.deliver patch (gold precedent). 1-line/guard port exact comment added. Docstring reality update. Atomic gold TestStorePurchaseAtomicGold 100% untouched (verbatim class/contract/25p). Review fixes: test renamed test_complete_order_real_path + doc "Exercises real complete_order path... contract protected in gold"; real post-state asserts (COMPLETE, bal via 1-line/guard, PURCHASE tx, stock) + clean unused _=. Re-runs atomic 25p identical + spot cross. 0 gold change. GSD ~20+ (wc96+). Self-check PASSED + phrase + handoff "Item 2/33 closed. Second...".
- Item 3/33 (add dedicated E2E int for complete_order/fulfillment + discount/tier/cap using TestSession/file; third): New tests/integration/test_store_purchase_integration.py (gold atomic pattern EXACT: TestSession/file + N806 tol+doc, 777 tg, explicit models User/Balance/Package/Product/Order/Tx/Tier/Fulfillment/Privilege, try/finally reopen/re-query, external patch ONLY PackageService.deliver, 1-line/guard exact comment + noqa E501, real DB asserts + seeds full FK chain). 4 tests: success (debit PURCHASE, COMPLETE, stock, tx, post best-effort), insuff after effective (79<80), monthly cap exhausted (completed dummy in month), tier locked (prior <2 REQUIRED_PREV). 4/4 + atomic 25p id + cross 10p. Side effects (STORE_PURCHASE best-effort) protected (cross/reaction_mission green; new E2E external patch only). Atomic gold 100% untouched. GSD 40+ (wc235+). Self-check PASSED + phrase "Item 3/33 closed. Third of new pool of 4...".
- Item 4/33 (promotion_user "me interesa" tight optional cluster, fourth/last; activated by user): Unit reduction test_promotion_user_handlers.py (key express_interest path test_calls_express_interest_with_user_data → real PromotionService(db_session) + seed promo + DB PromotionInterest verify; removed heavy MagicMock for blocked/has/express/get in path; still external patch get_service ctx + notify). New tests/handlers/test_promotion_user_handlers_integration.py (1 test tight): pytestmark=integration, real_svc=PromotionService(db_session), patch class "handlers.promotion_user_handlers.PromotionService" return real, patch notify external, sample fixtures, handler→real svc→DB row→UI 1:1 ("Diana ha sido notificada de su curiosidad" + promo.name; "Interes registrado"). 29p (28+1). Broader -k promotion 156p. Greps/ smoke OK. GSD ~30+ (wc323+). Self-check PASSED + phrase "Item 4/33 closed. Fourth of new pool of 4. ... Ready for arch-enforcer + test-guardian + documentador (update ROADMAP + learnings + final pool close)".
- Arch audits (1-3): PASS WITH NOTES (0 critical violations) each. Citations: scope tight (tests only + listed files); 0 prod (git/greps no writes to handlers/store_*.py or services/*.py); precedents copied al pie (gamif int + atomic gold full + 1-line/guard exact + TestSession + DESIRED + external only); UI 1:1; GSD pre + wc + self-check; 3 crit + atomicity/EventBus/get_service 0 impact (golds re-runs + 0 writes in crit paths; atomic gold verbatim; get_service 1 call/handler unchanged); notes pre-exist only (N806 tol per gold, long guards, etc). Item4 via executor self-check.
- Test-guardian (1-3): "suite protege adecuadamente". Re-runs exact golds list (atomic 25p id, cross 10p, inv 2p, reactions 9p, daily 19p, vip 37p, broader 778p+8xf preexist; + new int/E2E); new coverage (store purchase real vs isolation); 0 attributable reg; atomic untouched; 1-line exact; external only. Item4 self-check + broader promotion.
- Executor self-checks (all items + pool close): PASSED full (phases/DoD/gates/archivos/tests passed; reglas: GSD pre every, scope tight per PLAN/mapeo, 3 crit protected via re-runs/greps, precedents al pie, UI 1:1, 0/0/0, get_service 1 call unchanged); pool phrase verbatim; handoffs explicit to arch/testg/documentador + next.
- Metrics (from SUMMARYs/gsd/arch/testg + wc/git/collect/pytest --collect): Files touched ~5 test files (new: store int 658LOC/10 tests, E2E int 476LOC/4 tests, promo int 59LOC/1 test; edited: promo unit reduction 1 path/28p post, store svc unit non-gold reduction + review real asserts) + gsd log 371 lines + PLAN/SUMMARY + agent reports + documentador log. Tests added: ~15 (10 store handler +4 E2E +1 promo); golds re-runs: multiple full per item (F3/F5/Item3 F3: atomic/cross/inv/reaction/daily/vip/broader exact flags; review re-runs); GSD pre counts: 50+/item (F1-Fx), total 371 main + 6+ doc pre; arch/testg verdicts: PASS WITH NOTES 0 crit (1-3), "suite protege adecuadamente"; review 0 open (Item1 4 nits + Item2 fixes addressed). Other: ruff clean (pre N806 tol); bot smoke OK; 0 prod writes; scope tight; pool phrase.
- Pool/BATCH notes + phrase: "Pool anterior de 4 cerrado (tests passing per user). Nuevo pool de 4 iniciado. Quedan ~2-4 clusters del análisis inicial después de este pool." (repeated verbatim in PLAN/SUMMARY/gsd/self-checks/arch/testg/hand offs + this ROADMAP). BATCH: 4 items completed in this tirón (tests-only hardening; store purchase paths priority per user; gamif int precedent + TestSession + real DB + 1-line ports applied; golds protected; 0/0/0; arch/testg gates + documentador). GSD discipline total. Source of truth only. 3 crit (gamif atomic/cross/reaction/daily protected via re-runs + orthogonal store/promotion; narr/VIP 0 direct; atomicity/EventBus/get_service contracts 0 mutation).
- Learnings (extracted from SUMMARYs/gsd/agent reports): Gamif integration precedent (real service + class patch + full flow + UI 1:1) scaled to store (tienda critical) + promotion (high volume UX). TestSession/file gold pattern (N806 tol, 777, explicit models, try/finally, external patch only, survives + post-credit best effort, DESIRED) proven for E2E reality (debit/discount/cap/tier). Real DB (vs isolation) boosts confidence in debit/discount paths (effective price, COMPLETE/PURCHASE tx, stock, bal delta via guard) without breaking atomic golds/contracts. 1-line/guard ports (exact copy post-Item10) keep tests resilient. Tight pools of 4 effective for focused test-hardening (user-driven, no creep). GSD pre + self-check + arch/testg + documentador enforce quality/traceability. Review 0-open loops + UI 1:1 tighten improve resilience.
- Handoff: Pool 33 closed (4 items, tests passing per user per self-checks + golds + arch/testg). New pool of 4 initiated. Quedan ~2-4 clusters del análisis inicial después de este pool. ROADMAP updated + tirón report persisted in .grok/agent-memory/documentador/33-test-reality-pool-close.md + MEMORY.md pointer. Ready for next clusters (e.g. remaining user flows from mapeo: story user quiz, backpack fulfillment, mission user claim) or documentador.

(End of pool 33 entry. Documentador used per role at close.)

**Pool 34 after pool 33 (hardener-agile effort=4; 4 items chained; explicit documentador at close; 2026-06-26; "Item X/34 closed. Nth of new pool of 4"):**

- Per impact mapeo context from pool33 (source of truth: PLANs + 34-*-SUMMARY.md + gsd-34-*.log + arch gsd logs; remaining clusters: story_user/narrativa CRIT#2 quiz/archetype/advance + backpack fulfillment + mission_user claim + reward_admin long funcs + test gaps/hygiene + observability/health/docs hygiene). Precautions: copy gamif int + pool33 store int/E2E + atomic gold + 1-line/guard al pie; TestSession where atomic visible; class patch real_svc; UI 1:1; external only; no touch 3 crit golds; GSD pre every; self-check + pool phrase; arch/testg gates. 0 prod/0 beh/0 atomicity. 3 crit + atomicity/EventBus/get_service 0 impact.
- **PLAN + SUMMARY + gsd sources:** .planning/phases/34-*/{PLAN.md, *-SUMMARY.md} + .planning/quick/gsd-34-*.log (item1 100+ wc, reward ~106l 40+pre, testgaps ~66l 42+ entries, obs 124l) + gsd-arch-enforcer-34-item*.log + gsd-test-guardian-34-item2.log + gsd-documentador-34-*.log.
- **Item 1/34 (34-remaining-user-flows-reality, first):** New int tests story primary (narrative_menu not/started+arch, start/continue real progress, archetype quiz once-only+calc+assign+FSM clear+immut re-complete, view ach, advance/make_choice success+invalid graceful no partial + cost E2E atomic TestSession/file + 1-line/guard exact x2, VIP deny real); tight backpack (fulfillment_retry real, resend_vip, read_chapter); mission (show_my real bars, detail graceful, claim real deliver). 1-line/guard ports. UI 1:1 ("Fragmentos...", "Bienvenido de vuelta", "Capitulo X", "Explorador", "Diana ha sido notificada...", bars, alerts). Review 8 nits fixed 0 open (1-line full, strict E2E, VIP test, ach seed, fsm data, etc). GSD 100+. self-check PASSED + "Item 1/34 closed. First of new pool of 4." Arch: **PASS WITH NOTES 0 critical** (gsd-arch-enforcer-34-item1; 0 prod git/rg, style copied al pie pytestmark/real_svc/patch class/UI1:1/fixtures, 1-line present, GSD+self+phrase, 3crit 0 impact, get_service 1 unchanged). Test-guardian: golds re-runs (story 43p + cross 10p + reaction + daily + vip + invariants + broader 799p+) green 0 attr reg; "suite protege adecuadamente". 0/0/0. Pool phrase. Handoff to arch/testg/docu/Item2.
- **Item 2/34 (34-reward-admin-wizard, second):** reward_admin_handlers exactly 1 service (Reward via with get_service(RewardService) as for all wizard cb/message/list/detail/toggle/delete); 11+ puros extracted (build_reward_confirm_text_and_keyboard, build_package_selection_text_and_buttons, build_tariff_selection_buttons, build_pkg_confirmation_text_and_keyboard, compute_reward_type_text, build_reward_list_entry_and_button, build_reward_detail_text_and_keyboard, build_reward_delete_confirm_keyboard, build_back_only_keyboard, build_reward_created_text, build_reward_error_text; verb+ctx+res "Función pura..."; inspect <=50 post: confirm 48/49, list 34, delete 40). Thin delegates in reward_service (get_available_packages_for_rewards, get_all_tariffs, get_tariff, get_package + create_package_for_reward_wizard passthrough + arch comments "Added for item34... Precedent item8/9"). New test_reward_admin_handlers.py TestRewardAdminPureHelpers 13+ import-inside (cover texts/buttons/summaries/list/detail/edges/empty; @patch get_service + delegate mocks + assert on reward_svc only; doc "ported to 1-service..."). UI 1:1 exact ( "Paso X de 5", "Resumen de la recompensa"/"paquete", "name (N archivos, stock: ∞/X)", "name (D dias)", "✅/❌ name (type)", detail bullets + toggle "Activar/Desactivar", "🗑️ Eliminar", "🔙 Volver", empty "No hay paquetes..."/"No hay tarifas...", truncation, Lucien). Logging inside withs. GSD pre. self-check PASSED + "Item 2/34 closed. Second of new pool of 4." Arch: **PASS WITH NOTES 0 critical** (0 cross svc, 9+ withs, 11 puros, LOCs<=49 via inspect, delegates+comments, UI1:1, logging, 3crit orthogonal). Golds: reward 31p + broader 158p +2 pre xf (0 attr); pure direct real exec. 0/0/0 (0 beh/0 CRUD/0 claim/atomic/0 other handlers/0 pkg/vip core). Patterns al pie item7/8/9. 3crit protected (re-runs). Pool phrase. Handoff Item3.
- **Item 3/34 (34-test-gaps-hygiene, third):** explicit caps (TestGamifDailyCapsExplicit once+block real DailyGift+credit +1-line/guard; TestGamifTriviaCapsExplicit DEFAULTS pins dice/vip); full handler E2E "mensaje correcto" Lucien insuff (store int direct_buy "Moneda especial insuficiente." +show_alert; gamif int game protection "Besitos insuficientes para la proteccion." real Streak); FSM restart sim (TestFSMRestartSim fresh MemoryStorage per bot.py + real svc + DB StreakSession survive); VIP/channel edges (TestVIPChannelEdges expire-no-error-if-gone, multi-tariff real+DB). Tests added/ext in unit/int (daily, trivia, store/gamif int, streak_fsm, vip_flows). GSD ~66l 42+ entries. self-check PASSED + "Item 3/34 closed. Third of new pool of 4." Arch: **PASS WITH NOTES 0 critical** (caps explicit, E2E insuff exact Lucien UI1:1, FSM Memory, VIP edges real, class patch/get_service, 1-line exact, external, 0 prod, 0/0/0, 3crit 0 impact). Golds re-runs (gamif 51p+4xf, cross/reac19p, story43p, vip140p+7xf, inv14p, broader1201p+9xf pre) 0 attr. UI1:1, precedents al pie, scope tight. Deviations pre-only (tol, flakes, N806). Pool phrase. Handoff Item4 + arch/testg/docu.
- **Item 4/34 (34-observability-health-docs, fourth/last):** structured logging hygiene align "module | action | user_id=... | result=..." (copy health_service al pie): rate_limiter (5), idempotency (2), besito_service (credit/debit alongside 0 tx chg), health (channels/critical status=). /health verify: scripts/health_check --json/--verbose (core 7 checks db/bot/channels/scheduler/event_bus/critical_sanity/backup + overall; read-only 0 commit/add; best-effort; bot smoke + get_service(Health) + on_startup sim); script OK. CLAUDEs/docs: handlers/CLAUDE.md (get_session example -> "with get_service(BesitoService) as svc: # exactly 1 service"; Reglas 1svc/get_service ONLY + hardener puros/integration/Items7-11/pool33 + logging); decisions.md (Item 4/34 entry + refs + phrase + handoff); services/CLAUDE (opt cross). GSD 124l. self-check PASSED full + "Item 4/34 closed. Fourth of new pool of 4." Golds: health13p + cross10p + reac/daily/inv57p + 3crit~599p (1 pre flake re clean non-attrib) + broader1002p+13xf pre; 0 attr reg. Arch: **PASS WITH NOTES 0 critical** (logging hygiene, /health core, Health precedent al pie, get_service 1call x2 +is_admin analytics, 0 get_session active, 0 writes, read-only, 0/0/0, 3crit 0 impact, GSD/self/phrase). 0/0/0 tight (listed files only). Precedents Item11/29 al pie. Pool phrase. Final pool handoff to arch/testg/documentador.
- **Arch (all 4):** PASS WITH NOTES 0 critical (detailed in gsd-arch-enforcer-34-item*.log; .grok/agent-memory/arch-enforcer/34-itemN-arch-audit.md referenced; 1svc/puros/integration/hygiene/UI1:1/0prod/3crit+contracts/ GSD+self+phrase; notes pre/hygiene only).
- **Test-guardian:** golds re-runs + new coverage ( "suite protege adecuadamente" per handoffs/gsd mentions/precedents; item2 explicit gsd-test-guardian-34-item2.log; broader green 0 attr reg).
- **Self-checks + review:** PASSED all items + pool (full DoD/gates/phases/archivos/tests/rules/GSD pre/scope/3crit/UI1:1/0/0/0/precedents/phrase); review 0 open (item1 fixes done).
- **Metrics (this pool):** Tests added/ext ~20+ (story int ~13 incl E2E, backpack 3, mission 3, daily/trivia caps 2, store/gamif insuff E2E 2, fsm 1, vip edges 2, pure helpers tests 13+, extensions); GSD pre counts (100+/item1, 106l 40+pre item2, ~66l 42+ item3, 124l item4 + arch gsd logs  + docu pre 4+); golds re-runs (multiple per item exact flags -q --tb=line -p no:cov --override-ini="addopts=" + targeted -k + broader smoke 1000p+ per; atomic/cross/reaction/daily/vip/story/inv protected); files touched ~ 3 new int handler tests + extensions in 4+ test files + 4 py hygiene (rate/idemp/health/besito) + 4 PLAN/SUMMARY + 4 gsd main + 4 arch gsd + 1 docu gsd + decisions/handlers/CLAUDE; pool phrase instances (verbatim x many in SUMMARY/gsd/self/hand off/ROADMAP); 0 attributable reg; ruff clean (pre tol); bot smoke + terminal health OK; 0 prod writes; scope tight per PLANs; GSD discipline total (pre every edit/gate/verif/ruff/pytest/grep/smoke/self + wc).
- **Pool/BATCH notes + phrase (verbatim):** "Pool anterior de 4 cerrado (tests passing per user). Nuevo pool de 4 iniciado. Quedan ~2-4 clusters del análisis inicial después de este pool." (repeated in all SUMMARYs/gsd/self-checks/arch logs/hand offs + this + final). BATCH: 4 items completed (tests reality scale + 1svc+puros wizard + hygiene/obs/docs); source SUMMARYs/gsd/impact context from 33 + PLANs + agent reports; 3 crit (gamif atomic/reaction/daily/mission orthogonal protected via re-runs/golds; narr CRIT#2 real coverage positive; channel/VIP edges added; atomicity/EventBus/get_service contracts 0 mutation); GSD pre + arch (PWN 0c) + testg ("suite protege") + documentador at close. Precedents copied al pie (gamif int, store int/E2E/TestSession, item7-11 1svc+puros+LOC+Health, daily guards, atomic gold). UI 1:1 Lucien. Review 0 open.
- **Learnings (from SUMMARYs/gsd/agent):** Test reality scaling (story CRIT#2 + backpack/mission) with gamif/store precedent + TestSession E2E + 1-line/guard + real DB boosts debit/assign/cost/price confidence (effective, COMPLETE/PURCHASE tx, stock, bal via guard) w/o breaking golds. 1svc+puros for wizards (reward_admin): 11+ puros + thin delegates + Test*PureHelpers + inspect LOC + get_service boundary + UI1:1; delegates transparent. Hygiene/obs leverages item11/29 Health (logging format, read-only, 1svc+is_admin, best-effort /health); docs sync prevents drift (handlers/CLAUDE get_service 1svc now). Tight pools of 4 work for focused clusters (mapeo-driven, 0 creep). 3 crit + contracts always protected (orthogonal/re-runs). GSD pre + self + gates + phrase + 0-open review enforce. Pool phrase discipline + traceability (SUMMARYs primary).
- **Handoff:** Pool 34 closed (4 items, tests passing per user per self-checks + golds + arch/testg verdicts). ROADMAP updated + report .grok/agent-memory/documentador/34-remaining-clusters-pool-close.md + MEMORY pointer. Ready for next (or pause). "Pool anterior de 4 cerrado (tests passing per user). Nuevo pool de 4 iniciado. Quedan ~2-4 clusters del análisis inicial después de este pool."

**Pool 35 (new pool of 4; 2026-06-26; "Item X/35 closed. Nth of new pool of 4"; 3/4 items completed with full 6-agent seq + arch PWN 0c + testg "suite protege adecuadamente" + golds 0 attr reg + self-checks + phrases; explicit documentador at 3/4):**

- Per impact mapeo / prior roadmap (source: prior pool34 + HARDENING + gsd-35 logs + arch/testg reports; clusters surfaced incl full Redis for rate/idemp (middleware debt), promotion admin wizard (long funcs 1svc+puros), EventBus + logging expansion (streak obs + hygiene); remaining ~1 cluster post this). Precautions: copy 1svc+puros (27/34/9/2), listener "MUST NOT"/DESIRED/central reg (Item5/6/10/3), optional ctor+fallback (new for mw), GSD pre every + self-check + phrase; 0 prod/0 beh/0 atomicity; 3 crit + atomicity/EventBus/get_service 0 impact; UI1:1 when applicable; external only or pre-handler. No touch 3 crit golds (re-run only).
- **PLAN + gsd sources:** .planning/phases/35-*/PLAN.md (3) + .planning/quick/gsd-35-*.log (item1 ~36l, promo 157+l, eventbus 30+l) + gsd-arch-enforcer-35-*.log + gsd-test-guardian-35-*.log + gsd-documentador-35-pool-progress.log (this + pre + wc).
- **Item 1/35 (35-full-redis-rate-idemp-middleware, first):** middlewares/rate_limiter.py (optional redis: Redis|None=None ctor + ZSET sliding window parity for rate + exact else in-mem path; TYPE_CHECKING; arch "# Item 1/35..."), idempotency.py (redis=None + async SET NX EX check_and_mark + exact fallback; skip guarantee "critical: guarantees skip before any handler/credit"; global compat), bot.py (create_storage returns (storage, redis_client|None); shared wiring Idemp before Throttle + order comments + "guarantees skip before credit on dupe CB across instances" + Item 1/35), tests/unit/*_middleware.py (in-mem 100% untouched + minimal redis opt tests). GSD pre 36+ lines + self-check PASSED. Golds re-runs exact: mw+atomic 48p, reaction/daily/inv 57p, store atomic 533p(8xf pre), broader 1002p(13xf) — 0 attributable. Arch: **PASS WITH NOTES (0 critical)** (optional ctor+TYPE+if/else+ZSET/SETNX parity exact, in-mem 100%, shared client, order, Item comments, scope listed only, 3crit gamif protected via idemp skip+rate, contracts untouched). Test-guardian: **"suite protege adecuadamente"** (in-mem untouched, redis mocks min, no gold mutation, dupe guarantee exercised indirect via golds, new opt coverage). Bot smoke (no-arg ctors + fallback logs) OK. 3 crit + contracts: gamif (idemp/rate pre-credit protection across inst; golds "credit survives" + "post-credit best effort" hold); narr/chan 0; atomic/EventBus/get_service 0 mutation. Verbatim phrase + "Item 1/35 closed. First of new pool of 4." Handoff arch/testg/docu.
- **Item 2/35 (35-promotion-admin-wizard, second):** handlers/promotion_admin_handlers.py (18 with get_service(PromotionService) uniform for menu/select/confirm/list/detail/toggle/delete/pending/interests/mark/block/stats + wizard; 0 bare PackageService post; 13 puros extract verb+ctx+res "Función pura..." 1:1 UI "Paso X de 5"/Lucien/"✅ Forjar experiencia"/"El Gabinete esta vacio..."/price cents/file texts/empty/cbs; slimmed long funcs <=50 via inspect post e.g. confirm 32L/select 37L), services/promotion_service.py (thin delegate get_available_packages_for_promo_wizard + "Thin delegate... Added for item 2/35... Precedent item 8/9/34. Not core CRUD. 0 behavior change."), tests/handlers/test_promotion_admin_handlers.py (TestSelectPackageSource ported to get_service + promo delegate + __enter__ + asserts; + TestPromotionAdminPureHelpers 12+ import-inside no @patch on puros covering all UI pins). GSD 157+ pre-every. self-check PASSED. Golds: 67p full handler + 12p pure subset + 526p broader (0 attr). Arch: **PASS WITH NOTES (0 critical)** (rg 18 1svc/0 Package, 13 puros exact doc + LOC<=50 inspect, delegates+arch comments, UI1:1, logging, GSD, 3crit orthogonal). Test-guardian: **"suite protege adecuadamente"** (ports faithful, no @patch puros, import-inside, golds untouched, UI1:1 + prior behavior covered direct). ruff pre-tol test only. 3 crit protected (promo admin config orthogonal to gamif credit/atomic/reaction/daily; golds protect; narr/chan 0). Verbatim phrase + "Item 2/35 closed. Second of new pool of 4." Handoff.
- **Item 3/35 (35-eventbus-logging-expansion, third):** services/streak_promotion_service.py (high-value obs listener at bottom: exact Cross-domain block + "MUST NOT credit, debit, or mutate besitos state here." + full DESIRED CONTRACT + "streak | besitos_awarded_received | user_id=... | ..." + best-effort + "0 impact on contracts or gamif atomicity golds" + "Item 3/35"; F1 safe: streak debit-only), services/besito_service.py (hygiene credit/debit/_schedule: "besito_service | credit_besitos | user_id=... | amount=... source=... result=credited" + debit/emit_failed + Item 3/35 comment copy health/pool34), bot.py (import + reg 6th in on_startup cross-domain block after store; extended comment "# ... + Item 3/35 eventbus logging expansion" + log "... store, streak; ...); + Item 3/35"), tests/unit/test_event_bus.py (new test_streak_promotion_listener_is_invoked_and_logs_per_item3_35: fresh InternalEventBus + import inside + register real + emit + caplog + assert "streak | ..."; mirrors narrative/broadcast/game), decisions.md (full Item 3/35 entry mirror style + BATCH/pool + handoff). GSD 30+ . self-check PASSED. Golds: 24p event_bus/cross (patch schedule), 57p reaction/daily/inv, 474p besito/health/listener, 1003p broader — 0 attr (9/13 xf pre). Patch+DESIRED+ "credit survives deliver False" + "post-credit best effort (misiones + listeners)" exercised. Arch: **PASS WITH NOTES (0 critical)** (listener template exact incl MUST NOT/DESIRED/domain log, logging format aligned, central reg only + comments, "MUST NOT", scope tight, 3crit safe). Test-guardian: **"suite protege adecuadamente"** (new streak caplog/"MUST NOT"/wiring; structured logs; bot reg 6 + health listeners=6; golds/contracts hold). 3 crit: gamif (obs-only + MUST NOT + F1 safe + golds protect credit/reac/atomic; structured on emitter); narr/chan 0. Verbatim phrase + "Item 3/35 closed. Third of new pool of 4." Ready for item4 or full close + documentador.
- **Arch (all 3):** PASS WITH NOTES 0 critical (detailed gsd-arch-enforcer-35-*.log + .claude/agent-memory/arch-enforcer/35-*-arch-audit.md; optional ctor/fallback, puros+1svc/delegates, listener template/"MUST NOT", GSD/precedents/phrase, 3crit+contracts 0 impact, scope listed files only; notes pre-exist hygiene only).
- **Test-guardian (all 3):** "suite protege adecuadamente" (new coverage for opts/puros/obs+caplog; hygiene audit pass: in-mem untouched, no @patch puros, ports faithful, golds untouched; re-runs exact PLAN cmds 0 attr reg; 3crit safe).
- **Self-checks + review:** PASSED per item (full DoD/gates/phases/tests/rules/GSD pre/scope/3crit/UI1:1/0/0/0/precedents/phrase); review n/a (tight, no open surfaced in handoffs).
- **Metrics (this pool):** GSD pre counts (36l item1, 157+l item2, 30+l item3 + arch/testg/docu pres + wc); golds re-runs (multiple per item exact flags + broader smoke ~1000p+; atomic/cross/reaction/daily/vip/store protected); files touched ~5-6 py + 3 PLAN + 3 gsd main + 3 arch gsd + 3 testg gsd + 1 docu gsd + decisions; pool phrase instances (verbatim x many in gsd/self/arch/testg/ROADMAP/hand offs); 0 attributable reg; ruff clean (pre tol); bot/manual smoke OK (ctors, reg+emit, health count+1); 0 prod writes; scope tight per PLANs; GSD total; arch PWN 0c x3; testg suite x3.
- **Pool/BATCH notes + phrase (verbatim):** "Pool anterior de 4 cerrado (tests passing per user). Nuevo pool de 4 iniciado. Quedan ~2-4 clusters del análisis inicial después de este pool." (repeated verbatim in all gsd/self-checks/arch/testg/hand offs + this ROADMAP + agent reports + PLANs). BATCH: 4/4 items completed in this tirón (pool 35 of 4; redis middleware + promo wizard puros + eventbus/logging + deeper edges tests-only; source gsd-35-*.log + 35-*-arch/test reports + PLANs + agent memory + 35-full-pool-close.md); 3 crit (gamif protected via idemp/rate skip+parity + obs MUST NOT + golds "credit survives deliver False" + "post-credit best effort"; narr/chan 0 direct; atomicity/EventBus/get_service 0 mutation). GSD pre + arch (PWN 0c x4) + testg ("suite protege adecuadamente" x4) + documentador at close. Precedents (redis optional ctor+fallback+shared, 1svc+puros+delegates+Test*PureHelpers, listener "MUST NOT"/DESIRED + hygiene, TestSession/gather+external patch+Memory fallback per bot + real svc + 1-line/guard) copied al pie. UI 1:1 where touched. 0/0/0. 4/4 closed.
- **Item 4/35 (deeper edges tests-only, fourth/last):** tests/unit/test_besito_service.py (TestGamifBesitoCapsRacesExplicit: concurrent gather <=1 + repeated caps no exceed + real Besito + file TestSession + N806+doc+777+try/finally+external schedule patch ONLY + DESIRED + "credit survives" + daily precedent); tests/integration/test_vip_flows.py (TestVIPChannelDeeperEdges: pay→VIP+remove-free, expire-no-error-if-gone no crash, ban-both, multi/partial, offline, free pending after expire, real TG grant sim; real VIP/ChannelService + DB asserts); tests/test_streak_fsm.py (TestFSMRestartSimRealStorage: MemoryStorage roundtrip per bot.py fallback + archetype + real StoryService + progress survives + DESIRED + story gold copy). GSD 102l. self-check PASSED + "Item 4/35 closed. Fourth/last of new pool of 4." + phrase. Golds (exact PLAN list): VIP/channel 130p/7xf, free 15p, cross atomic 10p (patch), reactions 9p, story 43p, inv 11p, broader 928p/8xf + spots; 0 attr reg. Arch: **PASS WITH NOTES (0 critical)** (ultra-tight: only 3 tests+log; rg 0 prod; 0/0/0; precedents al pie; integration real+TestSession+class patch; 3crit+contracts protected via re-runs+0 writes crit paths; phrase x15+). Test-guardian: **"suite protege adecuadamente"** (deeper coverage explicit + golds protected; hygiene 1:1; 0 attr). 
- **Full Pool 35 Outcomes:** All 4 items 6-seq complete; golds 1000p+ broader per item green 0 attributable (pre flakes/xf only non-reg); arch PWN 0c x4; testg suite x4; self-checks PASSED; GSD total (36l/157+l/30+l/102l + docu); pool phrase verbatim xN; 3 crit + contracts protected 0 impact (gamif via rate/idemp/obs + golds; narr/chan orthogonal; atomic/EventBus/get_service upheld); learnings documented in 35-full-pool-close.md.
- **Learnings (from gsd/arch/testg/SUMMARY equiv + reports):** Optional redis ctor + exact fallback + shared client (public 0-arg preserved; ZSET/SETNX parity for gamif rate/idemp dist safety; in-mem 100% + min opt tests). Puros for wizard +1svc (13 puros + delegate thin + Test*PureHelpers import-inside; <=50 inspect; UI1:1 Lucien/Gabinete/price/file/empty). Safe obs listener + hygiene (exact "MUST NOT credit/debit/mutate" + DESIRED + domain log; structured "módulo | ... | user_id=... | resultado=..." on emitter; central reg + health; 0 reentrancy/0 mutation). Deeper edges via real DB + TestSession + gather + bot Memory fallback (class patch + external patch ONLY + 1-line/guard refs + story/atomic/daily + N806+doc+777+try/finally+gather return_exceptions; explicit caps/races concurrent, channel/VIP pay-remove/expire/ban/multi/offline/pending, FSM sim roundtrip). Tight pools of 4 effective (focused from clusters; 0 creep; 6-seq + gates + phrase + self + docu enforce quality/trace). 3 crit + contracts always protected (orthogonal/re-runs/golds).
- **Handoff:** Pool 35 closed (4/4 items, tests passing per user). ROADMAP updated + report .claude/agent-memory/documentador/35-full-pool-close.md + MEMORY pointer. Ready for next pool or pause. "Pool anterior de 4 cerrado (tests passing per user). Nuevo pool de 4 iniciado. Quedan ~2-4 clusters del análisis inicial después de este pool."

(End of pool 35 4/4 entry. Documentador used per role at full close.)

**Pool 36 (new pool of 4; 2026-06-28; item 1/1 ad-hoc store UX fix via /hardener-agile):**

- **Item 1/36 (34-store-catalog-tier-nav, first/only):** Unificar navegación tienda visitante: "Recorrer las estanterías" y callback legacy `store_categories` muestran **tiers del catálogo Kinky** (IMPULSO→MÍTICO, `docs/catalogo.md`), no categorías de paquetes (`Category`). `StoreService.get_tiers_for_shop()` (patrón `get_categories_for_shop`); `shop_menu` apunta a `store_tiers` (eliminado botón duplicado "Ver por niveles"); `store_categories` thin delegate → `store_tiers_menu`; botones secundarios `store_tiers`; `store_category_products` conservado para callbacks antiguos. GSD 34-store-catalog-tier-nav.log. self-check PASSED. Arch: **PASS WITH NOTES 0 critical**. Test-guardian: **suite protege adecuadamente** (108p store + 89p gold smoke 0 attr). 0/0/0 (0 compra/atomicity/fulfillment). 3 crit orthogonal. Pool phrase verbatim.
- **Handoff:** Pool 36 item 1 closed (tests passing per user). "Pool anterior de 4 cerrado (tests passing per user). Nuevo pool de 4 iniciado. Quedan ~2-4 clusters del análisis inicial después de este pool."

---

## 5. What Is Missing / Roadmap (Gaps + Proposed Next)

**Remaining from Initial Analysis + post-pool35 (not fully closed; prioritized):**
- **Long functions / admin bloat (deuda media-alta):** Remaining admin wizards if any post pool35 promo (reward/mission/store already addressed in prior pools 25-35 + item2; channel closed). Pre-existing long in besito debit etc out of scope per tights.
- **Test / coverage gaps (media):** Full Redis rate/idemp addressed in pool35 Item1; deeper FSM real Redis sim (beyond Memory sim in item4); more gamif caps/property explicit; invalid narrative + EventBus; channel/VIP multi edges (partial in item4 tests); health broader; broader EventBus listeners (streak added pool35 item3).
- **Other fragility/deuda:** Pre-exist daily concurrent flake + VIP (doc non-reg); N806 tol golds; deprecations; inconsistent logs (hygiene advanced pool34+35); analyzer noise.
- **Cross-cutting (future):** Expand EventBus (more listeners if high value surfaced); rate smarter (per-action); health enhancements; docs drift prevention via claude-md-sync.
- Post pool35 surfaced: patterns adoption (optional+fallback, puros, safe obs); obs hygiene for racha/streaks/promo partial via Item3.

**Proposed Next (if new tirón, max 4; high-value, builds on prior + handoffs; use same pipeline):**
Completed by pool 35 4/4 (see detailed section 4 + gsd/arch/test reports + 35-full-pool-close.md): 1. Full Redis rate/idemp (middleware; optional ctor + fallback + shared + ZSET/SETNX parity; protects gamif dupe/spam). 2. Promotion admin wizard (1svc + 13 puros + delegate + <=50 + UI1:1). 3. EventBus + logging expansion (streak obs safe + "MUST NOT"/DESIRED + besito hygiene + central reg + health count + caplog). 4. Deeper edges tests (channel/VIP/gamif caps/races + FSM real Redis sim via Memory + narrative edges; TestSession/gather/real svc). After pool35: clusters from initial mapeo / prior pools largely addressed (middleware, high-volume wizards, EventBus/logging, test edges for 3 crit). Remaining ~2-4 clusters (e.g. further obs/listeners for streak/promo/backpack; test gaps like deeper gamif caps/property + full FSM Redis sim; any surfaced long wizards; health extensions or rate smarter).
Proposed Next (max 4 if requested): 1. Expand EventBus + structured logging to remaining high-value (streak full, promo, backpack). 2. Test gaps/hygiene (FSM restart real Redis, explicit global caps/property in gamif, VIP/channel edges more, E2E Lucien insuff more). 3. Further admin wizard modernization if bloat remains. 4. Health/observability extensions or rate smarter. Prioritization: 3 crit + contracts; tight 0/0/0; GSD/PLAN; arch/testg + documentador.

**Prioritization Criteria (for future):** High impact on 3 critical systems (gamif/narrative/channel-VIP) + atomicity/EventBus/get_service contracts; tight scope (verifiable via golds + self-checks); GSD/PLAN-driven; low risk (0 behavior/0 atomicity); builds on completed (1svc+puros from 7-9/2/34/35, locals+obs from 5/6/10/3, middleware/get_service/EventBus + optional from 35 item1, hygiene, realDB+TestSession+Memory from item4); addresses initial "fragility" or "deuda" with clear metrics (e.g., 0 held, tests pass, <50 LOC, arch PASS WITH NOTES, test-guardian "suite protege adecuadamente", idemp skip guarantee, listener "MUST NOT", redis fallback parity).

**Metrics of Success (ongoing + pool35 4/4 update):**
- 0 critical arch violations (achieved: ... + pool35: arch PWN 0c x4 per 35-item*-arch-audit.md).
- 0 attributable regressions in golds/chains (achieved; ... + pool35: golds ~48/57/533/1002 (item1), 67/12/526 (item2), 24/57/474/1003 (item3), 130/15/10/9/43/11/928+ (item4) all green 0 attr).
- Scope tight + GSD discipline (achieved; ... + pool35: 36l/157+l/30+l/102l per item + docu pre + wc tracked; ~3-6 files max/item).
- 3 systems + contracts protected/enhanced (achieved; ... + pool35 4/4: gamif via idemp/rate skip+parity + obs MUST NOT + golds "credit survives" + "post best effort"; narr/chan 0; atomic/EventBus/get_service upheld; optional fallback + listener hygiene + TestSession/gather in tests).
- Documentation/traceability (... + pool35: full gsd/self/arch/testg + 35-full-pool-close.md + ROADMAP append + MEMORY + phrases xN).
- Pool/tirón success: Pool 35 4/4 items (Item1 redis rate/idemp, Item2 promo wizard 1svc+puros, Item3 eventbus+logging, Item4 deeper edges tests); arch PWN 0c x4; testg "suite protege adecuadamente" x4; self-checks PASSED + pool phrase verbatim; GSD; 3crit+contracts protected; learnings (redis ctor fallback, puros wizard, safe obs+hygiene, deeper via real DB+TestSession+gather+bot Memory); handoff "Pool 35 closed (4/4 items, tests passing per user)."

**Next Steps (post-pausa):**
- User reviews this doc + .claude/agent-memory/documentador/35-full-pool-close.md.
- If new tirón: user prompt; chain pipeline (impact etc). Include remaining ~2-4 clusters.
- Persist/update this + MEMORY as living. (documentador used per role).
- "Pool anterior de 4 cerrado (tests passing per user). Nuevo pool de 4 iniciado. Quedan ~2-4 clusters del análisis inicial después de este pool."

This document draws from ... + pool35 gsd-35-*.log + 35-*-arch-audit.md + *-test-report.md + 35-PLANs + 35-full-pool-close.md + 35-pool-progress. ... BATCH: ... + pool35 (4 items, 4/4 of new pool of 4, closed via documentador). Pool phrase verbatim at every close.

**Fin del pool 35 3/4. Listo para item 4 o close.** 🎩

**Handoff final (pool 35 3/4):** "Pool 35 3/4 items closed (tests passing per user)."

**Pool anterior de 4 cerrado (tests passing per user). Nuevo pool de 4 iniciado. Quedan ~2-4 clusters del análisis inicial después de este pool.**