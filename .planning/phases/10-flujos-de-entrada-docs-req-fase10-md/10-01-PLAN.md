---
phase: 10-flujos-de-entrada-docs-req-fase10-md
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - models/models.py
  - utils/lucien_voice.py
  - keyboards/inline_keyboards.py
  - migrations/versions/ (alembic autogenerated)
autonomous: true
requirements:
  - FREE-01
  - VIP-01
must_haves:
  truths:
    - User model has vip_entry_status and vip_entry_stage columns
    - LucienVoice contains exact ritual texts from PRD for Free and VIP entry
    - Inline keyboards expose social links, "Continuar", and "Estoy listo" buttons
  artifacts:
    - path: models/models.py
      provides: User columns for VIP entry state
      contains: vip_entry_status, vip_entry_stage
    - path: utils/lucien_voice.py
      provides: Ritual message templates
      min_lines: 20
    - path: keyboards/inline_keyboards.py
      provides: Entry flow keyboards
      exports: social_links_keyboard, vip_entry_continue_keyboard, vip_entry_ready_keyboard
  key_links:
    - from: keyboards/inline_keyboards.py
      to: utils/lucien_voice.py
      via: pairing in handlers (used together)
---

<objective>
Create the foundational artifacts for Phase 10: add database columns for VIP entry tracking, add ritual message templates to LucienVoice, and add inline keyboards for both Free and VIP entry flows.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@models/models.py
@utils/lucien_voice.py
@keyboards/inline_keyboards.py
@docs/req_fase10.md
@.planning/phases/10-flujos-de-entrada-docs-req-fase10-md/10-CONTEXT.md

Source of truth for message texts: 10-CONTEXT.md "specifics" section.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add vip_entry_status and vip_entry_stage to User model</name>
  <files>models/models.py</files>
  <read_first>
    - models/models.py (read the User class fully)
    - docs/req_fase10.md (confirm field names and types)
  </read_first>
  <action>
    In the `User` class in `models/models.py`, add two new columns after `updated_at`:
    - `vip_entry_status = Column(String(20), nullable=True)`  # values: "pending_entry", "active"
    - `vip_entry_stage = Column(Integer, nullable=True)`       # values: 1, 2, 3
    Do not add default values (let them be None by default).
    Then run Alembic autogenerate: `alembic revision --autogenerate -m "add vip entry status and stage to users"`.
    Verify the generated migration file appears under `migrations/versions/` and that it contains `op.add_column('users', sa.Column('vip_entry_status', sa.String(length=20), nullable=True))` and the equivalent for `vip_entry_stage`.
  </action>
  <verify>
    <automated>grep -n "vip_entry_status\|vip_entry_stage" models/models.py && ls migrations/versions/*add_vip_entry_status_and_stage*</automated>
  </verify>
  <acceptance_criteria>
    - models/models.py contains `vip_entry_status = Column(String(20), nullable=True)` inside class User
    - models/models.py contains `vip_entry_stage = Column(Integer, nullable=True)` inside class User
    - A migration file exists matching `migrations/versions/*add_vip_entry_status_and_stage_to_users*.py`
    - The migration file contains `op.add_column('users',` for both columns
  </acceptance_criteria>
  <done>User model has the two new columns and an Alembic migration file exists.</done>
</task>

<task type="auto">
  <name>Task 2: Add LucienVoice methods for Free and VIP entry rituals</name>
  <files>utils/lucien_voice.py</files>
  <read_first>
    - utils/lucien_voice.py (read existing methods up to free_access_approved)
    - docs/req_fase10.md (message texts for all 6 new messages)
    - .planning/phases/10-flujos-de-entrada-docs-req-fase10-md/10-CONTEXT.md
  </read_first>
  <action>
    Add the following @staticmethod methods to `LucienVoice` in `utils/lucien_voice.py`, returning `str` with parse_mode="HTML" formatting already baked in (use <b>, <i>, 👇, 🎩). Copy the exact texts from the PRD/CONTEXT.md.

    Free channel:
    - `free_entry_ritual()` — returns the exact Message 1 text including HTML tags, ending with "Mientras tanto… aquí puede seguir su rastro 👇"
    - `free_entry_impatient()` — returns the exact Message 2 text
    - `free_entry_welcome(channel_name: Optional[str] = None)` — returns the exact Message 3 text. The caller will send the invite link in a follow-up message, so this message should end with "👇".
    - `free_entry_expired()` — message if subscription expires during VIP ritual (reused in VIP flow). Text: "🎩 <b>Lucien:</b>\n\n<i>Su acceso ha expirado antes de completar el ritual.</i>\n\nDiana cierra las puertas cuando el tiempo se agota. Si desea regresar, deberá obtener un nuevo enlace.</i>"

    VIP channel:
    - `vip_entry_stage_1()` — exact Fase 1 text + "Antes de entregarle la entrada, hay algo que debe saber…"
    - `vip_entry_stage_2()` — exact Fase 2 text ending with "…entonces sí."
    - `vip_entry_stage_3()` — exact Fase 3 text ending with "Entre con intención.\n👇"

    Ensure no changes to existing methods unless explicitly required.
  </action>
  <verify>
    <automated>grep -c "free_entry_ritual\|free_entry_impatient\|free_entry_welcome\|vip_entry_stage_1\|vip_entry_stage_2\|vip_entry_stage_3" utils/lucien_voice.py</automated>
  </verify>
  <acceptance_criteria>
    - utils/lucien_voice.py contains `def free_entry_ritual(`
    - utils/lucien_voice.py contains `def free_entry_impatient(`
    - utils/lucien_voice.py contains `def free_entry_welcome(`
    - utils/lucien_voice.py contains `def vip_entry_stage_1(`
    - utils/lucien_voice.py contains `def vip_entry_stage_2(`
    - utils/lucien_voice.py contains `def vip_entry_stage_3(`
    - Each method returns a string containing "🎩 <b>Lucien:</b>"
  </acceptance_criteria>
  <done>All six message templates are present with exact PRD wording.</done>
</task>

<task type="auto">
  <name>Task 3: Add inline keyboards for Free and VIP entry flows</name>
  <files>keyboards/inline_keyboards.py</files>
  <read_first>
    - keyboards/inline_keyboards.py (read existing patterns)
    - docs/req_fase10.md (social URLs and button labels)
  </read_first>
  <action>
    Add three new functions to `keyboards/inline_keyboards.py`:

    1. `social_links_keyboard() -> InlineKeyboardMarkup`
       Returns a single-row keyboard with three `url` buttons (not callback_data):
       - Instagram: text="Instagram", url="https://www.instagram.com/srta.kinky"
       - TikTok: text="TikTok", url="https://www.tiktok.com/@srtakinky"
       - X: text="X", url="https://x.com/srtakinky"

    2. `vip_entry_continue_keyboard() -> InlineKeyboardMarkup`
       Returns a single button: text="Continuar", callback_data="vip_entry_continue"

    3. `vip_entry_ready_keyboard() -> InlineKeyboardMarkup`
       Returns a single button: text="Estoy listo", callback_data="vip_entry_ready"
  </action>
  <verify>
    <automated>grep -n "social_links_keyboard\|vip_entry_continue_keyboard\|vip_entry_ready_keyboard" keyboards/inline_keyboards.py</automated>
  </verify>
  <acceptance_criteria>
    - keyboards/inline_keyboards.py contains `def social_links_keyboard(` returning InlineKeyboardMarkup with url buttons for Instagram, TikTok, and X
    - keyboards/inline_keyboards.py contains `def vip_entry_continue_keyboard(` with callback_data="vip_entry_continue"
    - keyboards/inline_keyboards.py contains `def vip_entry_ready_keyboard(` with callback_data="vip_entry_ready"
    - social buttons use `url=` not `callback_data=`
  </acceptance_criteria>
  <done>Three new keyboard functions exist and follow project conventions.</done>
</task>

</tasks>

<verification>
- Run `pytest tests/unit/test_channel_service.py tests/unit/test_vip_service.py -q` to ensure existing tests still pass after model changes.
- Verify migration file is generated and contains both column additions.
</verification>

<success_criteria>
- User model has `vip_entry_status` and `vip_entry_stage` columns
- Alembic migration exists under `migrations/versions/`
- LucienVoice has all ritual message methods with exact PRD text
- Keyboards have `social_links_keyboard`, `vip_entry_continue_keyboard`, and `vip_entry_ready_keyboard`
- No regression in existing unit tests
</success_criteria>

<output>
After completion, create `.planning/phases/10-flujos-de-entrada-docs-req-fase10-md/10-01-SUMMARY.md`
</output>
