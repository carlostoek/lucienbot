---
phase: 10-flujos-de-entrada-docs-req-fase10-md
plan: 02
type: execute
wave: 2
depends_on:
  - 10-01
files_modified:
  - services/scheduler_service.py
  - handlers/free_channel_handlers.py
autonomous: true
requirements:
  - FREE-01
  - SCHED-01
must_haves:
  truths:
    - Free channel join request schedules a 30-second delayed welcome message via APScheduler DateTrigger
    - Repeated join requests while pending send the impatience message immediately
    - Approved free requests send the ritual welcome message with the invite link
    - Pending request approvals run every 30 seconds instead of once daily at 09:00
  artifacts:
    - path: services/scheduler_service.py
      provides: IntervalTrigger for approvals, DateTrigger job handler for free welcome
      exports: "_send_free_welcome_job"
    - path: handlers/free_channel_handlers.py
      provides: Delayed scheduling and impatience logic
      contains: "schedule_free_welcome" or scheduler.add_job call
  key_links:
    - from: handlers/free_channel_handlers.py
      to: services/scheduler_service.py
      via: "DateTrigger job for free_welcome"
    - from: services/scheduler_service.py
      to: handlers/free_channel_handlers.py / pending requests
      via: IntervalTrigger replacing cron
---

<objective>
Implement the ritualized Free channel entry flow: 30-second delayed welcome message, immediate impatience message on repeated requests, and a corrected real-time approval loop using IntervalTrigger.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@services/scheduler_service.py
@handlers/free_channel_handlers.py
@utils/lucien_voice.py
@keyboards/inline_keyboards.py
@.planning/phases/10-flujos-de-entrada-docs-req-fase10-md/10-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix scheduler bug and add free welcome DateTrigger job handler</name>
  <files>services/scheduler_service.py</files>
  <read_first>
    - services/scheduler_service.py (read _process_pending_requests, SchedulerService.start, imports)
    - utils/lucien_voice.py (confirm free_entry_ritual and free_entry_welcome exist)
    - keyboards/inline_keyboards.py (confirm social_links_keyboard exists)
  </read_first>
  <action>
    1. Change the `_process_pending_requests` trigger in `SchedulerService.start()` from:
       `trigger="cron", hour=9, minute=0`
       to:
       `trigger=IntervalTrigger(seconds=30)`
       (add `from apscheduler.triggers.interval import IntervalTrigger` at the top of `SchedulerService.start` or module-level if already present — check imports).

    2. Update `_process_pending_requests` message sending: replace `LucienVoice.free_access_approved(channel.channel_name)` with `LucienVoice.free_entry_welcome(channel.channel_name or channel.channel_id)` and import `free_entry_welcome`. The invite link should be sent in a separate `bot.send_message` after the welcome text. Use the channel's `invite_link` if available.

    3. Add a new module-level async job function `_send_free_welcome_job(user_id: int, channel_id: int)` above `SchedulerService`. It must:
       - Create a `SessionLocal()`
       - Instantiate `ChannelService(db)`
       - Get the channel by `channel_id`
       - Send `LucienVoice.free_entry_ritual()` + `social_links_keyboard()` via the lazy `_get_bot()`
       - Log the action
       - Close the DB session
       Use the exact same "module-level function + lazy bot" pattern as `_process_pending_requests`.

    4. Expose a public method on `SchedulerService` named `schedule_free_welcome(user_id: int, channel_id: int)` that adds a one-time job:
       ```python
       from datetime import datetime, timedelta
       from apscheduler.triggers.date import DateTrigger
       job_id = f"free_welcome_{user_id}_{channel_id}"
       run_date = datetime.utcnow() + timedelta(seconds=30)
       self._scheduler.add_job(
           _send_free_welcome_job,
           trigger=DateTrigger(run_date=run_date),
           id=job_id,
           replace_existing=True,
           kwargs={"user_id": user_id, "channel_id": channel_id},
       )
       ```
  </action>
  <verify>
    <automated>grep -n "IntervalTrigger" services/scheduler_service.py && grep -n "_send_free_welcome_job" services/scheduler_service.py && grep -n "schedule_free_welcome" services/scheduler_service.py</automated>
  </verify>
  <acceptance_criteria>
    - services/scheduler_service.py contains `trigger=IntervalTrigger(seconds=30)` for job id "approve_join_requests"
    - services/scheduler_service.py contains async def `_send_free_welcome_job(user_id: int, channel_id: int)` as a module-level function
    - services/scheduler_service.py contains `def schedule_free_welcome(self, user_id: int, channel_id: int)` inside SchedulerService
    - `schedule_free_welcome` uses `DateTrigger(run_date=datetime.utcnow() + timedelta(seconds=30))` with `replace_existing=True`
    - `_process_pending_requests` sends `LucienVoice.free_entry_welcome` instead of `free_access_approved`
  </acceptance_criteria>
  <done>Scheduler fixed to 30s interval and free welcome delay job handler registered.</done>
</task>

<task type="auto">
  <name>Task 2: Modify free channel handler for delay, impatience, and approval wiring</name>
  <files>handlers/free_channel_handlers.py</files>
  <read_first>
    - handlers/free_channel_handlers.py (current handle_join_request and handle_member_join)
    - services/scheduler_service.py (schedule_free_welcome signature added in Task 1)
    - utils/lucien_voice.py (free_entry_impatient exists)
    - services/channel_service.py (get_pending_request, create_pending_request signatures)
  </read_first>
  <action>
    Modify `handlers/free_channel_handlers.py`:

    1. Import `LucienVoice` additions and `social_links_keyboard` from keyboards.
    2. Import `get_scheduler` from `services.scheduler_service` if not already imported.
    3. In `handle_join_request()`:
       - After checking `existing_request`, if it exists and is still pending, immediately send `LucienVoice.free_entry_impatient()` via `join_request.bot.send_message(chat_id=user.id, text=..., parse_mode="HTML")`, then `return`.
       - If no existing request, after creating the `PendingRequest`, do NOT call `LucienVoice.free_request_received()` immediately. Instead, schedule the delayed welcome using the scheduler:
         ```python
         from services.scheduler_service import get_scheduler
         scheduler = get_scheduler()
         if scheduler:
             scheduler.schedule_free_welcome(user.id, channel.id)
         ```
       - Remove the old immediate `send_message` with `free_request_received`.
    4. In `handle_member_join()`:
       - After marking the request as approved, send the welcome ritual message with `LucienVoice.free_entry_welcome(channel.channel_name)` followed by the invite link from `channel.invite_link` in a separate message.
       - Ensure no reference to the old `free_access_approved` message remains.
  </action>
  <verify>
    <automated>grep -n "free_entry_impatient\|schedule_free_welcome\|free_entry_welcome" handlers/free_channel_handlers.py</automated>
  </verify>
  <acceptance_criteria>
    - handlers/free_channel_handlers.py sends `LucienVoice.free_entry_impatient()` when `existing_request` is truthy
    - handlers/free_channel_handlers.py no longer sends `LucienVoice.free_request_received` immediately
    - handlers/free_channel_handlers.py calls `scheduler.schedule_free_welcome(user.id, channel.id)` after creating the pending request
    - handlers/free_channel_handlers.py sends `LucienVoice.free_entry_welcome` and `channel.invite_link` in `handle_member_join`
    - `free_access_approved` is no longer used in handlers/free_channel_handlers.py
  </acceptance_criteria>
  <done>Free handler implements 30s delay, impatience message, and ritual welcome on approval.</done>
</task>

</tasks>

<verification>
- Run existing unit tests: `pytest tests/unit/test_channel_service.py -q`
- Run bot startup locally (or at least import scheduler_service) to confirm no import errors.
</verification>

<success_criteria>
- `_process_pending_requests` runs every 30 seconds (IntervalTrigger)
- Free join request schedules a 30-second delayed ritual message with social buttons
- Repeated free requests send the impatience message immediately
- Approval sends the ritual welcome message followed by the invite link
- No regression in existing tests
</success_criteria>

<output>
After completion, create `.planning/phases/10-flujos-de-entrada-docs-req-fase10-md/10-02-SUMMARY.md`
</output>
