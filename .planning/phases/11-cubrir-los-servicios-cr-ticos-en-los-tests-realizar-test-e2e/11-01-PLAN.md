---
phase: 11-critical-services-tests
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/conftest.py
  - pyproject.toml
  - tests/unit/test_store_service.py
  - tests/unit/test_promotion_service.py
  - tests/unit/test_broadcast_service.py
  - tests/unit/test_story_service.py
  - tests/unit/test_analytics_service.py
  - tests/unit/test_user_service.py
  - tests/unit/test_daily_gift_service.py
  - tests/unit/test_reward_service.py
  - tests/unit/test_package_service.py
  - tests/integration/test_free_entry_flow.py
  - tests/integration/test_vip_ritual_flow.py
  - tests/e2e/test_lucien_voice.py
  - tests/integration/test_cross_service_atomicity.py
autonomous: true
requirements:
  - REQ-11-01
  - REQ-11-02
  - REQ-11-03
  - REQ-11-04
  - REQ-11-05
  - REQ-11-06
  - REQ-11-07
  - REQ-11-08
  - REQ-11-09
  - REQ-11-10
  - REQ-11-11
  - REQ-11-12
  - REQ-11-13
  - REQ-11-14
must_haves:
  truths:
    - All required test files exist as valid Python modules
    - conftest.py provides fixtures for all models tested in Phase 11
    - pytest recognizes e2e marker without strict-markers failure
  artifacts:
    - path: tests/conftest.py
      provides: Fixtures for StoreProduct, Package, Promotion, StoryNode, Archetype, DailyGiftClaim
    - path: pyproject.toml
      provides: e2e marker registered
    - path: tests/unit/test_store_service.py
      provides: StoreService test stub
    - path: tests/unit/test_promotion_service.py
      provides: PromotionService test stub
    - path: tests/unit/test_broadcast_service.py
      provides: BroadcastService test stub
    - path: tests/unit/test_story_service.py
      provides: StoryService extended test stub
    - path: tests/unit/test_analytics_service.py
      provides: AnalyticsService test stub
    - path: tests/unit/test_user_service.py
      provides: UserService test stub
    - path: tests/unit/test_daily_gift_service.py
      provides: DailyGiftService test stub
    - path: tests/unit/test_reward_service.py
      provides: RewardService test stub
    - path: tests/unit/test_package_service.py
      provides: PackageService test stub
    - path: tests/integration/test_free_entry_flow.py
      provides: Free entry E2E stub
    - path: tests/integration/test_vip_ritual_flow.py
      provides: VIP ritual E2E stub
    - path: tests/e2e/test_lucien_voice.py
      provides: LucienVoice E2E stub
    - path: tests/integration/test_cross_service_atomicity.py
      provides: Cross-service atomicity stub
  key_links:
    - from: tests/conftest.py
      to: all unit test files
      via: fixture imports
      pattern: "def sample_.*\\(db_session"
---

<objective>
Set up test infrastructure for Phase 11: add missing fixtures to conftest.py, register the e2e pytest marker, and create stub test files for all new test modules.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@tests/conftest.py
@pyproject.toml
@models/models.py

The existing conftest.py lacks fixtures for StoreProduct, Package, Promotion, StoryNode, StoryChoice, Archetype, DailyGiftConfig/Claim, BroadcastMessage, ReactionEmoji, Order, CartItem. pyproject.toml has --strict-markers but no e2e marker. All stub files must be valid Python that pytest can collect (imports + empty class with pass is fine).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add e2e marker to pyproject.toml</name>
  <files>pyproject.toml</files>
  <read_first>pyproject.toml</read_first>
  <action>
    In pyproject.toml [tool.pytest.ini_options] markers list, add exactly this entry after the "slow" marker line:
    `e2e: E2E tests`
    Ensure the list remains a valid TOML array of strings. Do NOT remove unit, integration, or slow markers.
  </action>
  <verify>
    <automated>python -m pytest --markers | grep -q "e2e: E2E tests"</automated>
  </verify>
  <acceptance_criteria>
    - pyproject.toml contains the exact line `e2e: E2E tests` inside the markers list
    - `python -m pytest --markers` output contains "e2e: E2E tests"
  </acceptance_criteria>
  <done>e2e marker registered and pytest recognizes it</done>
</task>

<task type="auto">
  <name>Task 2: Add missing fixtures to conftest.py</name>
  <files>tests/conftest.py</files>
  <read_first>tests/conftest.py, models/models.py</read_first>
  <action>
    Append the following fixtures to tests/conftest.py before the mock fixtures section. Each fixture follows the existing pattern: create instance, db_session.add, commit, refresh, return instance.

    1. sample_package(db_session) -> Package(name="Test Package", description="A test package", store_stock=10, reward_stock=5, is_active=True)
    2. sample_store_product(db_session, sample_package) -> StoreProduct(name="Test Product", description="A product", package_id=sample_package.id, price=100, stock=10, is_active=True)
    3. sample_promotion(db_session, sample_package) -> Promotion(name="Test Promo", description="A promo", package_id=sample_package.id, price_mxn=99900, is_active=True, status=PromotionStatus.ACTIVE)
    4. sample_reaction_emoji(db_session) -> ReactionEmoji(emoji="💋", name="besito", besito_value=1, is_active=True)
    5. sample_broadcast_message(db_session, sample_free_channel, sample_admin) -> BroadcastMessage(message_id=1001, channel_id=sample_free_channel.channel_id, admin_id=sample_admin.telegram_id, text="Test broadcast", has_reactions=True)
    6. sample_story_node(db_session) -> StoryNode(title="Test Node", content="Test content", node_type=NodeType.NARRATIVE, chapter=1, is_active=True, is_starting_node=True)
    7. sample_story_choice(db_session, sample_story_node) -> StoryChoice(node_id=sample_story_node.id, text="Go forward", next_node_id=None, archetype_points=0)
    8. sample_archetype(db_session) -> Archetype(archetype_type=ArchetypeType.EXPLORADOR, name="El Explorador", description="Curioso y aventurero", traits='["curiosidad","aventura"]')
    9. sample_daily_gift_config(db_session) -> DailyGiftConfig(besito_amount=10, is_active=True)
    10. sample_cart_item(db_session, sample_user, sample_store_product) -> CartItem(user_id=sample_user.id, product_id=sample_store_product.id, quantity=1)
    11. sample_order(db_session, sample_user) -> Order(user_id=sample_user.id, total_items=1, total_price=100, status=OrderStatus.PENDING)
    12. sample_reward_besitos(db_session) -> Reward(name="Test Reward", description="A reward", reward_type=RewardType.BESITOS, besito_amount=50, is_active=True)

    Import any missing models at the top of conftest.py: StoreProduct, Package, Promotion, PromotionStatus, ReactionEmoji, BroadcastMessage, StoryNode, StoryChoice, NodeType, Archetype, ArchetypeType, DailyGiftConfig, DailyGiftClaim, CartItem, Order, OrderStatus, Reward, RewardType.
  </action>
  <verify>
    <automated>python -c "import tests.conftest"</automated>
  </verify>
  <acceptance_criteria>
    - conftest.py imports all 16 added models without ImportError
    - conftest.py defines fixtures named: sample_package, sample_store_product, sample_promotion, sample_reaction_emoji, sample_broadcast_message, sample_story_node, sample_story_choice, sample_archetype, sample_daily_gift_config, sample_cart_item, sample_order, sample_reward_besitos
    - `python -c "import tests.conftest"` exits with code 0
  </acceptance_criteria>
  <done>All required fixtures added and importable</done>
</task>

<task type="auto">
  <name>Task 3: Create stub test files</name>
  <files>
    tests/unit/test_store_service.py
    tests/unit/test_promotion_service.py
    tests/unit/test_broadcast_service.py
    tests/unit/test_story_service.py
    tests/unit/test_analytics_service.py
    tests/unit/test_user_service.py
    tests/unit/test_daily_gift_service.py
    tests/unit/test_reward_service.py
    tests/unit/test_package_service.py
    tests/integration/test_free_entry_flow.py
    tests/integration/test_vip_ritual_flow.py
    tests/e2e/test_lucien_voice.py
    tests/integration/test_cross_service_atomicity.py
  </files>
  <read_first>tests/unit/test_vip_service.py, tests/integration/test_vip_flow.py</read_first>
  <action>
    Create each of the 13 stub test files with valid Python syntax and pytest-discoverable structure. Each file must contain at minimum:
    - Appropriate imports (pytest, the service under test, relevant models)
    - One class decorated with @pytest.mark.unit / @pytest.mark.integration / @pytest.mark.e2e
    - One empty test method so pytest collection succeeds

    Specific stub contents:

    tests/unit/test_store_service.py:
    ```python
    import pytest
    from services.store_service import StoreService
    from models.models import StoreProduct, CartItem, Order, OrderStatus, Package
    
    @pytest.mark.unit
    class TestStoreService:
        def test_stub(self, db_session):
            pass
    
    @pytest.mark.unit
    class TestRaceConditions:
        def test_stub(self, db_session):
            pass
    ```

    tests/unit/test_promotion_service.py:
    ```python
    import pytest
    from services.promotion_service import PromotionService
    from models.models import Promotion, PromotionStatus, PromotionInterest, InterestStatus, BlockedPromotionUser
    
    @pytest.mark.unit
    class TestPromotionService:
        def test_stub(self, db_session):
            pass
    ```

    tests/unit/test_broadcast_service.py:
    ```python
    import pytest
    from services.broadcast_service import BroadcastService
    from models.models import BroadcastMessage, BroadcastReaction, ReactionEmoji
    
    @pytest.mark.unit
    class TestBroadcastService:
        def test_stub(self, db_session):
            pass
    ```

    tests/unit/test_story_service.py:
    ```python
    import pytest
    from services.story_service import StoryService
    from models.models import StoryNode, StoryChoice, UserStoryProgress, ArchetypeType, NodeType
    
    @pytest.mark.unit
    class TestStoryServiceCRUD:
        def test_stub(self, db_session):
            pass
    ```

    tests/unit/test_analytics_service.py:
    ```python
    import pytest
    from services.analytics_service import AnalyticsService
    
    @pytest.mark.unit
    class TestAnalyticsService:
        def test_stub(self, db_session):
            pass
    ```

    tests/unit/test_user_service.py:
    ```python
    import pytest
    from services.user_service import UserService
    from models.models import User, UserRole
    
    @pytest.mark.unit
    class TestUserService:
        def test_stub(self, db_session):
            pass
    ```

    tests/unit/test_daily_gift_service.py:
    ```python
    import pytest
    from services.daily_gift_service import DailyGiftService
    from models.models import DailyGiftConfig, DailyGiftClaim
    
    @pytest.mark.unit
    class TestDailyGiftService:
        def test_stub(self, db_session):
            pass
    ```

    tests/unit/test_reward_service.py:
    ```python
    import pytest
    from services.reward_service import RewardService
    from models.models import Reward, RewardType, UserRewardHistory
    
    @pytest.mark.unit
    class TestRewardService:
        def test_stub(self, db_session):
            pass
    ```

    tests/unit/test_package_service.py:
    ```python
    import pytest
    from services.package_service import PackageService
    from models.models import Package, PackageFile
    
    @pytest.mark.unit
    class TestPackageService:
        def test_stub(self, db_session):
            pass
    ```

    tests/integration/test_free_entry_flow.py:
    ```python
    import pytest
    from services.channel_service import ChannelService
    
    @pytest.mark.integration
    class TestFreeEntryFlow:
        def test_stub(self, db_session):
            pass
    ```

    tests/integration/test_vip_ritual_flow.py:
    ```python
    import pytest
    from services.vip_service import VIPService
    
    @pytest.mark.integration
    class TestVIPRitualFlow:
        def test_stub(self, db_session):
            pass
    ```

    tests/e2e/test_lucien_voice.py:
    ```python
    import pytest
    
    @pytest.mark.e2e
    class TestLucienVoiceConsistency:
        def test_stub(self):
            pass
    ```

    tests/integration/test_cross_service_atomicity.py:
    ```python
    import pytest
    
    @pytest.mark.integration
    class TestCrossServiceAtomicity:
        def test_stub(self, db_session):
            pass
    ```
  </action>
  <verify>
    <automated>python -m pytest tests/ --collect-only -q</automated>
  </verify>
  <acceptance_criteria>
    - All 13 stub files exist
    - `python -m pytest tests/ --collect-only -q` discovers tests without ImportError or collection errors
    - Output shows at least 13 newly discovered test classes
  </acceptance_criteria>
  <done>All stub test files created and pytest collection succeeds</done>
</task>

</tasks>

<verification>
- Run `python -m pytest tests/ --collect-only` — must show no errors
- Verify `python -m pytest --markers` shows e2e marker
</verification>

<success_criteria>
Wave 0 complete when pytest can discover all 13 new test files without collection errors, and conftest.py provides fixtures for all Phase 11 models.
</success_criteria>

<output>
After completion, create `.planning/phases/11-cubrir-los-servicios-cr-ticos-en-los-tests-realizar-test-e2e/11-01-SUMMARY.md`
</output>
