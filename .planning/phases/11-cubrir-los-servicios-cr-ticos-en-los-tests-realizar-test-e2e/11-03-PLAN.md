---
phase: 11-critical-services-tests
plan: 03
type: execute
wave: 1
depends_on:
  - 11-01
files_modified:
  - tests/unit/test_promotion_service.py
  - tests/unit/test_broadcast_service.py
  - services/promotion_service.py
  - services/broadcast_service.py
autonomous: true
requirements:
  - REQ-11-02
  - REQ-11-07
must_haves:
  truths:
    - PromotionService.create_promotion creates an active promotion with price in centavos MXN
    - PromotionService.express_interest rejects blocked users and duplicate interests
    - PromotionService.express_interest uses SELECT FOR UPDATE to prevent duplicate interest race conditions
    - BroadcastService.register_reaction awards besitos and records the reaction
    - BroadcastService.register_reaction uses SELECT FOR UPDATE to prevent double-reaction race conditions
  artifacts:
    - path: tests/unit/test_promotion_service.py
      provides: Full PromotionService unit test coverage
      min_lines: 150
    - path: tests/unit/test_broadcast_service.py
      provides: Full BroadcastService unit test coverage
      min_lines: 150
    - path: services/promotion_service.py
      provides: Race condition fix in express_interest
      contains: with_for_update
    - path: services/broadcast_service.py
      provides: Race condition fix in register_reaction
      contains: with_for_update
  key_links:
    - from: services/promotion_service.py
      to: models.models.PromotionInterest
      via: with_for_update query
      pattern: with_for_update
    - from: services/broadcast_service.py
      to: models.models.BroadcastReaction
      via: with_for_update query
      pattern: with_for_update
---

<objective>
Implement comprehensive unit tests for PromotionService and BroadcastService, and fix the confirmed race conditions in express_interest and register_reaction by adding SELECT FOR UPDATE locking.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@services/promotion_service.py
@services/broadcast_service.py
@tests/unit/test_promotion_service.py
@tests/unit/test_broadcast_service.py
@tests/unit/test_vip_service.py
@tests/conftest.py
@models/models.py

**Race conditions to fix:**
1. `PromotionService.express_interest` - no locking on the `has_user_expressed_interest` check + insert sequence. Fix by querying `PromotionInterest` with `with_for_update()` before the check, or by relying on the DB unique constraint on `(user_id, promotion_id)` and wrapping the insert in a try/except. The preferred fix per project patterns: add `with_for_update()` to the interest query inside `express_interest`.
2. `BroadcastService.register_reaction` - `has_user_reacted` check and insert are not atomic. Fix by querying `BroadcastReaction` with `with_for_update()` before checking.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix race condition in PromotionService.express_interest</name>
  <files>services/promotion_service.py</files>
  <read_first>services/promotion_service.py</read_first>
  <action>
    In `services/promotion_service.py`, modify `express_interest` to lock the interest check query with `with_for_update()`.

    Replace the existing `has_user_expressed_interest` call logic inside `express_interest` with an inline query that uses locking. Specifically, after validating the promotion is available, add:
    ```python
    existing = db.query(PromotionInterest).filter(
        PromotionInterest.user_id == user_id,
        PromotionInterest.promotion_id == promotion_id
    ).with_for_update().first()
    if existing:
        return False, "Ya has expresado interes en esta promocion", None
    ```
    Remove or bypass the old `self.has_user_expressed_interest(user_id, promotion_id)` call in this method. Ensure `PromotionInterest` is already imported (it is).
  </action>
  <verify>
    <automated>grep -n "with_for_update" services/promotion_service.py</automated>
  </verify>
  <acceptance_criteria>
    - services/promotion_service.py contains `with_for_update()` inside the `express_interest` method
    - The method no longer relies solely on `has_user_expressed_interest` for the critical check (may still call it for non-critical paths, but the guarded check uses with_for_update)
  </acceptance_criteria>
  <done>Race condition in express_interest protected by SELECT FOR UPDATE</done>
</task>

<task type="auto">
  <name>Task 2: Fix race condition in BroadcastService.register_reaction</name>
  <files>services/broadcast_service.py</files>
  <read_first>services/broadcast_service.py</read_first>
  <action>
    In `services/broadcast_service.py`, modify `register_reaction` to lock the reaction existence check with `with_for_update()`.

    Replace the early `self.has_user_reacted(broadcast_id, user_id)` call with an inline query inside `register_reaction`:
    ```python
    existing = self.db.query(BroadcastReaction).filter(
        BroadcastReaction.broadcast_id == broadcast_id,
        BroadcastReaction.user_id == user_id
    ).with_for_update().first()
    if existing:
        logger.info(f"Usuario {user_id} ya reaccionó al broadcast {broadcast_id}")
        return None
    ```
    Then proceed to create the reaction and credit besitos as before. Ensure `BroadcastReaction` is already imported (it is).
  </action>
  <verify>
    <automated>grep -n "with_for_update" services/broadcast_service.py</automated>
  </verify>
  <acceptance_criteria>
    - services/broadcast_service.py contains `with_for_update()` inside the `register_reaction` method
    - The guarded existing-reaction check uses the locked query instead of the unprotected `has_user_reacted` helper
  </acceptance_criteria>
  <done>Race condition in register_reaction protected by SELECT FOR UPDATE</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Write PromotionService unit tests</name>
  <files>tests/unit/test_promotion_service.py</files>
  <read_first>tests/unit/test_promotion_service.py, services/promotion_service.py, tests/unit/test_vip_service.py, tests/conftest.py</read_first>
  <behavior>
    - Test create_promotion returns Promotion with price_mxn stored as centavos
    - Test get_promotion returns correct promotion
    - Test get_available_promotions filters by active status and date range
    - Test update_product_allowed_fields (name, description, price_mxn, status, etc.)
    - Test pause_promotion and resume_promotion toggle status
    - Test delete_promotion sets is_active=False
    - Test express_interest success creates PromotionInterest with PENDING status
    - Test express_interest fails for blocked user
    - Test express_interest fails for unavailable promotion
    - Test express_interest fails for duplicate interest
    - Test block_user creates BlockedPromotionUser
    - Test unblock_user removes the block
    - Test get_promotion_stats returns correct counts
    - Test mark_interest_attended updates status to ATTENDED
  </behavior>
  <action>
    Overwrite `tests/unit/test_promotion_service.py` with complete tests following existing project patterns. Use db_session fixture and conftest fixtures (sample_user, sample_package, sample_promotion, sample_admin). Add a `TestPromotionServiceRaceCondition` class that uses mock chain verification to assert `with_for_update()` is called in `express_interest`, similar to test_vip_service.py pattern.

    Key test specifics:
    - `test_create_promotion`: assert `price_mxn == 99900` and `price_display == "$999.00 MXN"`
    - `test_express_interest_success`: returns `(True, _, interest)` and interest.status == InterestStatus.PENDING
    - `test_express_interest_blocked`: create BlockedPromotionUser first, then assert failure
    - `test_express_interest_duplicate`: call express_interest twice, second returns False
    - `test_mark_interest_attended`: attended_by == sample_admin.telegram_id, status == InterestStatus.ATTENDED
    - Race condition test: mock `db_session.query` to return a chain where `.with_for_update()` is verified.
  </action>
  <verify>
    <automated>python -m pytest tests/unit/test_promotion_service.py -v --no-cov</automated>
  </verify>
  <acceptance_criteria>
    - `python -m pytest tests/unit/test_promotion_service.py -v --no-cov` exits with code 0
    - tests/unit/test_promotion_service.py contains at least 12 test methods
    - contains TestPromotionServiceRaceCondition class verifying with_for_update in express_interest
  </acceptance_criteria>
  <done>PromotionService fully tested and race condition fix verified</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Write BroadcastService unit tests</name>
  <files>tests/unit/test_broadcast_service.py</files>
  <read_first>tests/unit/test_broadcast_service.py, services/broadcast_service.py, tests/unit/test_vip_service.py, tests/conftest.py</read_first>
  <behavior>
    - Test create_reaction_emoji creates emoji with besito_value
    - Test get_reaction_emoji_by_emoji finds active emoji
    - Test update_emoji_value changes besito_value
    - Test toggle_emoji flips is_active
    - Test delete_emoji removes the emoji
    - Test create_broadcast_message registers broadcast with correct fields
    - Test register_reaction success creates BroadcastReaction and credits besitos
    - Test register_reaction returns None for duplicate reaction
    - Test get_broadcast_stats returns total reactions, besitos, emoji breakdown
    - Test get_reactions_by_broadcast returns list of reactions
    - Test get_user_reactions filters by user_id
  </behavior>
  <action>
    Overwrite `tests/unit/test_broadcast_service.py` with complete tests. Use db_session and conftest fixtures (sample_user, sample_reaction_emoji, sample_broadcast_message, sample_balance). Add `TestBroadcastServiceRaceCondition` class that mocks the query chain to verify `with_for_update()` is called in `register_reaction`.

    Key specifics:
    - `test_register_reaction_success`: assert reaction.besitos_awarded == emoji.besito_value, and balance increased
    - `test_register_reaction_duplicate`: call twice, second returns None, balance should not increase twice
    - `test_get_broadcast_stats`: create 2 reactions with same emoji, assert total count and besitos sum
    - Race condition test: patch `db_session.query` (or `self.db.query` via the service instance) to verify `with_for_update` is called on the BroadcastReaction query inside `register_reaction`.
  </action>
  <verify>
    <automated>python -m pytest tests/unit/test_broadcast_service.py -v --no-cov</automated>
  </verify>
  <acceptance_criteria>
    - `python -m pytest tests/unit/test_broadcast_service.py -v --no-cov` exits with code 0
    - tests/unit/test_broadcast_service.py contains at least 10 test methods
    - contains TestBroadcastServiceRaceCondition class verifying with_for_update in register_reaction
  </acceptance_criteria>
  <done>BroadcastService fully tested and race condition fix verified</done>
</task>

</tasks>

<verification>
- `python -m pytest tests/unit/test_promotion_service.py tests/unit/test_broadcast_service.py -v` passes
- grep confirms `with_for_update` exists in both services/promotion_service.py and services/broadcast_service.py
</verification>

<success_criteria>
PromotionService and BroadcastService have complete unit test coverage, their race conditions are fixed with SELECT FOR UPDATE, and dedicated race-condition tests verify the locking patterns.
</success_criteria>

<output>
After completion, create `.planning/phases/11-cubrir-los-servicios-cr-ticos-en-los-tests-realizar-test-e2e/11-03-SUMMARY.md`
</output>
