# Plan 13-01: Foundation - Model, Migration y Service

## Goal
Agregar soporte para promociones VIP exclusivas en el modelo y servicio.

## Tasks

### Task 1: Agregar campo `is_vip_exclusive` al modelo Promotion
**File:** `models/models.py`

Agregar columna booleana a la clase `Promotion`:
```python
is_vip_exclusive = Column(Boolean, default=False)
```

Ubicación: Después del campo `is_active`, antes de `created_by`.

---

### Task 2: Crear migración Alembic
**Command:**
```bash
alembic revision --autogenerate -m "add is_vip_exclusive to promotions"
```

Verificar que la migración generada:
- Agrega columna `is_vip_exclusive` a tabla `promotions`
- Default value: `False`
- Nullable: `False` (con server_default)

---

### Task 3: Agregar método `get_vip_exclusive_promotions()` a PromotionService
**File:** `services/promotion_service.py`

Agregar método después de `get_available_promotions()`:

```python
def get_vip_exclusive_promotions(self) -> List[Promotion]:
    """Obtiene promociones exclusivas para VIPs"""
    db = self._get_db()
    now = datetime.now(timezone.utc)
    return db.query(Promotion).filter(
        Promotion.is_active == True,
        Promotion.status == PromotionStatus.ACTIVE,
        Promotion.is_vip_exclusive == True,
        and_(
            Promotion.start_date.is_(None) | (Promotion.start_date <= now)
        ),
        and_(
            Promotion.end_date.is_(None) | (Promotion.end_date >= now)
        )
    ).order_by(desc(Promotion.price_mxn)).all()
```

---

### Task 4: Actualizar `get_available_promotions()` para excluir VIP exclusivas
**File:** `services/promotion_service.py`

Modificar método para excluir promociones VIP del catálogo general:

```python
def get_available_promotions(self) -> List[Promotion]:
    """Obtiene promociones disponibles para los usuarios (no VIP exclusivas)"""
    db = self._get_db()
    now = datetime.now(timezone.utc)
    return db.query(Promotion).filter(
        Promotion.is_active == True,
        Promotion.status == PromotionStatus.ACTIVE,
        Promotion.is_vip_exclusive == False,  # <-- Agregar esta línea
        and_(
            Promotion.start_date.is_(None) | (Promotion.start_date <= now)
        ),
        and_(
            Promotion.end_date.is_(None) | (Promotion.end_date >= now)
        )
    ).order_by(desc(Promotion.created_at)).all()
```

---

## Verification

1. Migración ejecuta sin errores: `alembic upgrade head`
2. Modelo tiene el nuevo campo
3. Service tiene ambos métodos funcionando:
   - `get_available_promotions()` - retorna solo promociones NO VIP
   - `get_vip_exclusive_promotions()` - retorna solo promociones VIP
4. Tests pasan (si existen)

## Dependencies

None - foundation phase

## Estimated Effort

30-45 minutos
