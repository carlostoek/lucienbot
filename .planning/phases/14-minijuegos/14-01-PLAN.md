---
phase: 14-minijuegos
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - models/models.py
  - services/game_service.py
autonomous: true
requirements:
  - GAME-01
  - GAME-02
  - GAME-03

must_haves:
  truths:
    - "Usuario puede lanzar dados y ver resultado visual"
    - "Usuario puede jugar trivia con preguntas aleatorias"
    - "Usuario recibe +1 besito al ganar"
  artifacts:
    - path: "services/game_service.py"
      provides: "Lógica de minijuegos (dados, trivia)"
      exports: ["GameService.roll_dice", "GameService.check_winner", "GameService.get_random_question", "GameService.check_trivia_answer"]
    - path: "models/models.py"
      provides: "TransactionSource.GAME para registrar besitos ganados"
      contains: "GAME = \"game\""
  key_links:
    - from: "GameService.credit_besitos"
      to: "BesitoService.credit_besitos"
      via: "llamada directa"
      pattern: "besito_service\\.credit_besitos.*GAME"
---

<objective>
Implementar la lógica de negocio de los minijuegos (dados y trivia) y añadir GAME a TransactionSource.
</objective>

<context>
@models/models.py (líneas 167-175: TransactionSource enum)
@services/besito_service.py (métodos: credit_besitos, debit_besitos, has_sufficient_balance)
@docs/preguntas.json (formato: {"q": "...", "opts": [...], "answer": index})

# BesitoService existente:
```python
# services/besito_service.py
class BesitoService:
    def credit_besitos(self, user_id: int, amount: int, source: TransactionSource,
                       description: str = None, reference_id: int = None) -> bool
    def has_sufficient_balance(self, user_id: int, amount: int) -> bool
    def get_balance(self, user_id: int) -> int
```

# TransactionSource actual (models/models.py):
```python
class TransactionSource(str, enum.Enum):
    REACTION = "reaction"
    DAILY_GIFT = "daily_gift"
    MISSION = "mission"
    PURCHASE = "purchase"
    ADMIN = "admin"
    ANONYMOUS_MESSAGE = "anonymous_message"
```

# Formato de preguntas (docs/preguntas.json):
```json
[
  { "q": "¿Cuál es la capital de Francia?", "opts": ["Madrid", "París", "Roma"], "answer": 1 },
  ...
]
```
</context>

<tasks>

<task type="auto">
  <name>Task 1: Añadir GAME a TransactionSource</name>
  <files>models/models.py</files>
  <read_first>models/models.py - línea 167-175 (TransactionSource enum)</read_first>
  <action>
Agregar la constante GAME al enum TransactionSource después de ANONYMOUS_MESSAGE:

```python
class TransactionSource(str, enum.Enum):
    REACTION = "reaction"
    DAILY_GIFT = "daily_gift"
    MISSION = "mission"
    PURCHASE = "purchase"
    ADMIN = "admin"
    ANONYMOUS_MESSAGE = "anonymous_message"
    GAME = "game"  # Minijuegos (dados, trivia)
```
  </action>
  <accept_criteria>
- grep -n "GAME = \"game\"" models/models.py devuelve la línea añadida
- El enum TransactionSource contiene 7 elementos
  </accept_criteria>
  <verify>
grep -n "GAME = " models/models.py | grep -q "game"
  </verify>
  <done>TransactionSource.GAME disponible para acreditación de besitos</done>
</task>

<task type="auto">
  <name>Task 2: Crear GameService con lógica de dados</name>
  <files>services/game_service.py</files>
  <read_first>
services/besito_service.py (estructura de service, manejo de sesión)
services/CLAUDE.md (reglas de services: max 50 líneas, naming)
  </read_first>
  <action>
Crear services/game_service.py con:
1. Importaciones: random, json, logging, BesitoService, TransactionSource
2. Método roll_dice() -> tuple[int, int]: genera 2 números aleatorios 1-6
3. Método check_winner(d1: int, d2: int) -> bool: retorna True si son dobles (d1==d2) o pares (ambos pares diferentes: d1%2==0 and d2%2==0 and d1!=d2)
4. Método get_random_question() -> dict: carga preguntas de docs/preguntas.json y devuelve una aleatoria con estructura {"q": "...", "opts": [...], "answer": index}
5. Método check_trivia_answer(question: dict, answer_index: int) -> bool: compara answer_index con question["answer"]
6. Método award_besitos(user_id: int, amount: int = 1) -> bool: usa BesitoService.credit_besitos con TransactionSource.GAME

Seguir patrones de BesitoService: __init__, _get_db, close, logger.
  </action>
  <accept_criteria>
- services/game_service.py contiene class GameService
- Métodos: roll_dice, check_winner, get_random_question, check_trivia_answer, award_besitos
- check_winner(3, 3) retorna True (dobles)
- check_winner(2, 4) retorna True (pares)
- check_winner(2, 3) retorna False
- get_random_question() devuelve dict con keys "q", "opts", "answer"
  </accept_criteria>
  <verify>
grep -n "def roll_dice\|def check_winner\|def get_random_question\|def check_trivia_answer\|def award_besitos" services/game_service.py | wc -l
  </verify>
  <done>GameService implementa lógica de dados y trivia</done>
</task>

<task type="auto">
  <name>Task 3: Validar integración con BesitoService</name>
  <files>services/game_service.py</files>
  <read_first>services/game_service.py (el archivo creado en Task 2)</read_first>
  <action>
Verificar que award_besitos usa correctamente BesitoService:
- Importa BesitoService desde services.besito_service
- Usa TransactionSource.GAME como source
- La llamada es: besito_service.credit_besitos(user_id, amount, TransactionSource.GAME, description="Victoria en minijuego")
  </action>
  <accept_criteria>
- GameService importa BesitoService y TransactionSource
- award_besitos llama a besito_service.credit_besitos con source=TransactionSource.GAME
  </accept_criteria>
  <verify>
grep -n "TransactionSource.GAME" services/game_service.py
  </verify>
  <done>Award besitos usa TransactionSource.GAME</done>
</task>

</tasks>

<verification>
- [ ] TransactionSource contiene GAME = "game"
- [ ] GameService tiene todos los métodos requeridos
- [ ] check_winner() implementa lógica correcta (dobles y pares)
- [ ] get_random_question() carga y devuelve preguntas aleatorias
- [ ] award_besitos() usa BesitoService con GAME source
</verification>

<success_criteria>
1. models/models.py tiene TransactionSource.GAME disponible
2. services/game_service.py existe con lógica de minijuegos
3. Tests básicos de lógica pasan (dobles gana, pares gana, trivia)
</success_criteria>

<output>
After completion, create `.planning/phases/14-minijuegos/14-01-SUMMARY.md`
</output>