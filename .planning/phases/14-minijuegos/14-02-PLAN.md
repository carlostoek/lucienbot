---
phase: 14-minijuegos
plan: 02
type: execute
wave: 2
depends_on: [14-01]
files_modified:
  - handlers/game_user_handlers.py
  - keyboards/inline_keyboards.py
  - bot.py
autonomous: true
requirements:
  - GAME-01
  - GAME-02
  - GAME-03

must_haves:
  truths:
    - "Usuario puede acceder a minijuegos desde menú principal"
    - "Usuario puede lanzar dados con resultado visual"
    - "Usuario puede jugar trivia con botones inline"
    - "Usuario recibe mensaje de victoria al ganar"
  artifacts:
    - path: "handlers/game_user_handlers.py"
      provides: "Handlers de usuario para dados y trivia"
      exports: ["callback_query_handler para game_dice", "callback_query_handler para game_trivia", "callback_query_handler para game_trivia_answer"]
    - path: "keyboards/inline_keyboards.py"
      provides: "Botón de minijuegos en menú principal"
      contains: "🎲 Minijuegos"
    - path: "bot.py"
      provides: "Registro del router de minijuegos"
      contains: "game_user_router"
  key_links:
    - from: "game_dice callback"
      to: "GameService.roll_dice + check_winner"
      via: "handler"
      pattern: "game_service\\.roll_dice"
    - from: "game_trivia_answer callback"
      to: "GameService.check_trivia_answer + award_besitos"
      via: "handler"
      pattern: "check_trivia_answer"
---

<objective>
Implementar los handlers de usuario para minijuegos: menú, dados con animación, y trivia con botones inline.
</objective>

<context>
@services/game_service.py (métodos a usar: roll_dice, check_winner, get_random_question, check_trivia_answer, award_besitos)
@handlers/vip_user_handlers.py (estructura de handlers, FSM, callback patterns)
@keyboards/inline_keyboards.py (main_menu_keyboard - línea 11-46)

# Patrón de callback existente:
- "game_dice" - lanzar dados
- "game_trivia" - iniciar trivia
- "game_trivia_answer_X" - responder opción X

# Mensajes de victoria (per design):
- "¡Felicidades! Has ganado 1 besito 💋🎉"
- Dados: mostrar 🎲 con resultado (ej: "5 - 3")
</context>

<tasks>

<task type="auto">
  <name>Task 1: Crear handlers de minijuegos</name>
  <files>handlers/game_user_handlers.py</files>
  <read_first>
handlers/vip_user_handlers.py (estructura de callbacks, patrón de FSM)
services/game_service.py (métodos disponibles)
handlers/CLAUDE.md (reglas: lógica en services, handlers solo enrutan)
  </read_first>
  <action>
Crear handlers/game_user_handlers.py con:
1. Importaciones: Router, CallbackQuery, FSMContext, GameService, InlineKeyboardMarkup, InlineKeyboardButton
2. Crear router: game_user_router = Router()
3. Handler game_menu (callback "game_menu"): muestra menú de minijuegos con botones "🎲 Dados" y "🎯 Trivia"
4. Handler game_dice (callback "game_dice"):
   - Muestra mensaje "🎲 Tirando dados..."
   - Llama game_service.roll_dice()
   - Llama game_service.check_winner(d1, d2)
   - Si gana: llama game_service.award_besitos(user_id, 1), muestra mensaje de victoria con 💋🎉
   - Si pierde: muestra mensaje "Los dados muestran {d1} - {d2}. ¡Sigue intentando!"
   - Incluir botón "Otra ronda" -> game_dice
5. Handler game_trivia (callback "game_trivia"):
   - Llama game_service.get_random_question()
   - Muestra pregunta con botones inline para cada opción (opts[0], opts[1], etc.)
   - Los callbacks son "game_trivia_answer_X" donde X es el índice
6. Handler game_trivia_answer (callback "game_trivia_answer_{index}"):
   - Extrae índice de la query
   - Llama game_service.check_trivia_answer(question, index)
   - Si gana: llama game_service.award_besitos(user_id, 1), muestra victoria 💋🎉
   - Si pierde: muestra "Incorrecto. La respuesta era: {opts[answer]}"
   - Incluir botón "Otra pregunta" -> game_trivia

Usar FSM para mantener la pregunta actual durante la trivia (state="trivia_question").
  </action>
  <accept_criteria>
- handlers/game_user_handlers.py tiene game_menu, game_dice, game_trivia, game_trivia_answer
- game_dice muestra resultado de dados y mensaje de victoria/al perder
- game_trivia muestra pregunta con 3-4 botones inline
- game_trivia_answer verifica respuesta y otorga besitos si acierta
- Todos los handlers usan GameService (no lógica en handler)
  </accept_criteria>
  <verify>
grep -n "def game_menu\|def game_dice\|def game_trivia\|def game_trivia_answer" handlers/game_user_handlers.py | wc -l
  </verify>
  <done>Handlers de minijuegos implementados</done>
</task>

<task type="auto">
<name>Task 2: Añadir botón Minijuegos al menú principal</name>
  <files>keyboards/inline_keyboards.py</files>
  <read_first>keyboards/inline_keyboards.py - línea 11-46 (main_menu_keyboard)</read_first>
  <action>
Agregar botón de minijuegos al main_menu_keyboard, antes del botón de narrative:

```python
[InlineKeyboardButton(
    text="🎲 Minijuegos",
    callback_data="game_menu"
)],
```

Insertar después del botón "✨ Ofertas especiales" (línea 32-33).
  </action>
  <accept_criteria>
- main_menu_keyboard contiene botón con text="🎲 Minijuegos" y callback_data="game_menu"
- El botón aparece entre "Ofertas especiales" y "Fragmentos de la historia"
  </accept_criteria>
  <verify>
grep -n "Minijuegos\|game_menu" keyboards/inline_keyboards.py
  </verify>
  <done>Botón de minijuegos visible en menú principal</done>
</task>

<task type="auto">
<name>Task 3: Registrar router en bot.py</name>
  <files>bot.py</files>
  <read_first>bot.py - líneas 22-50 (imports de routers) y 226-257 (registro de routers)</read_first>
  <action>
1. Agregar importación en la sección de imports (línea ~47):
```python
from handlers.game_user_handlers import game_user_router
```

2. Agregar registro del router después de vip_user_router (línea ~254):
```python
dp.include_router(game_user_router)
```
  </action>
  <accept_criteria>
- bot.py importa game_user_router de handlers.game_user_handlers
- dp.include_router(game_user_router) está presente
- El import está en orden alfabético con los demás
  </accept_criteria>
  <verify>
grep -n "game_user_router" bot.py
  </verify>
  <done>Router de minijuegos registrado en bot</done>
</task>

</tasks>

<verification>
- [ ] game_user_handlers.py tiene 4 handlers (menu, dice, trivia, answer)
- [ ] main_menu_keyboard tiene botón "🎲 Minijuegos"
- [ ] bot.py importa y registra game_user_router
- [ ] Dados muestran resultado visual (d1 - d2)
- [ ] Victoria muestra mensaje con 💋🎉
- [ ] Trivia usa botones inline para opciones
- [ ] Trivia verifica respuesta y otorga besitos si acierta
</verification>

<success_criteria>
1. Usuario ve botón "🎲 Minijuegos" en menú principal
2. Al hacer click ve menú con opciones "Dados" y "Trivia"
3. Dados muestran resultado aleatorio con mensaje de victoria si gana
4. Trivia muestra pregunta aleatoria con botones inline
5. Victoria en trivia otorga +1 besito
</success_criteria>

<output>
After completion, create `.planning/phases/14-minijuegos/14-02-SUMMARY.md`
</output>