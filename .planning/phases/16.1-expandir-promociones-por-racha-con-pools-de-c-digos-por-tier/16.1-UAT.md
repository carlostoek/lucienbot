---
status: resolved
phase: 16.1-expandir-promociones-por-racha-con-pools-de-c-digos-por-tier
source:
  - 16.1-01-SUMMARY.md
started: 2026-05-08T00:00:00Z
updated: 2026-05-08T01:50:00Z
---

## Current Test

number: 5
name: Pool Exhaustion Does Not Block Other Tiers
expected: |
  When pool for tier 5 is exhausted, users who reach tier 10 (or higher) still receive
  codes for that higher tier if its pool is available. The promotion continues.
awaiting: user response

## Tests

### 1. Cold Start Smoke Test
expected: |
  Bot boots without errors. TriviaDiscountService loads, validate_discount_tiers and
  get_tier_pool_status methods are callable. Game service initializes with tier pool tracking.
result: pass

### 2. Admin Creates Multi-Tier Promo with max_codes
expected: |
  Admin uses /trivia_config or similar command. Creates promotion with JSON like:
  [{"streak": 5, "discount": 50, "max_codes": 3}, {"streak": 10, "discount": 75, "max_codes": 2}]
  System validates JSON and accepts max_codes per tier. Confirmation shows tier limits.
result: issue
reported: "No no veo ningún cambio en el sistema todo está igual como estaba"
severity: major

### 3. User Completes Streak and Receives Code (Pool Available)
expected: |
  User has an active trivia promotion config with multi-tier tiers including max_codes.
  User reaches streak 5 (or first tier). User receives a discount code for that tier.
  Response shows the code or confirms code was generated.
result: issue
reported: "Bot crashed when creating trivia discount - AttributeError: 'PromotionService' object has no attribute 'get_active_promotions'. Also error_handler_middleware crashes: AttributeError: 'HandlerObject' object has no attribute '__name__'"
severity: blocker

### 4. User Completes Streak When Pool Exhausted
expected: |
  When pool for user's tier is exhausted (codes_issued >= max_codes), user does NOT receive
  a code. Instead receives a notification like "Este nivel de descuento esta agotado, pero
  su racha continua." User keeps their streak progress.
result: blocked
blocked_by: server
reason: "Cannot test until trivia discount creation (test 3) works without crashing - crashes on 'get_active_promotions' and error_handler_middleware also crashes when trying to log the exception"

### 5. Pool Exhaustion Does Not Block Other Tiers
expected: |
  When pool for tier 5 is exhausted, users who reach tier 10 (or higher) still receive
  codes for that higher tier if its pool is available. The promotion continues.
result: blocked
blocked_by: prior-phase
reason: "Cannot test pool exhaustion without a promo with max_codes that has been exhausted. Need to admin-create a multi-tier promo first (blocked by test 2)"

## Summary

total: 5
passed: 1
issues: 2
pending: 0
skipped: 0
blocked: 2

## Gaps

- truth: "Admin wizard accepts max_codes per tier in multi-tier JSON and confirms to user"
  status: resolved
  reason: "User reported: No no veo ningún cambio en el sistema todo está igual como estaba"
  severity: major
  test: 2
  root_cause: "UI/UX issue - max_codes field exists in JSON validation but admin wizard UI doesn't clearly show max_codes limits to user during confirmation"
  artifacts:
    - path: "handlers/trivia_discount_admin_handlers.py"
      issue: "Admin wizard processes max_codes in JSON but doesn't display tier pool limits in the confirmation message"
  missing:
    - "Display max_codes per tier in the tier review/confirm step"
    - "Show pool status in admin menu for existing configs"
  debug_session: "pending-diagnosis"

- truth: "Trivia discount creation works without crashes"
  status: resolved
  reason: "Bot crashed when creating trivia discount - AttributeError: 'PromotionService' object has no attribute 'get_active_promotions'. Also error_handler_middleware crashes on AttributeError: 'HandlerObject' object has no attribute '__name__'"
  severity: blocker
  test: 3
  root_cause: "Method get_active_promotions() never existed on PromotionService - should be get_all_promotions(active_only=True)"
  artifacts:
    - path: "services/promotion_service.py:86"
      issue: "get_all_promotions(active_only=True) is the correct method"
    - path: "handlers/trivia_discount_admin_handlers.py:672"
      issue: "Calls non-existent get_active_promotions() instead of get_all_promotions(active_only=True)"
  missing:
    - "Change line 672 from promo_service.get_active_promotions() to promo_service.get_all_promotions(active_only=True)"
  debug_session: "diagnosed"

- truth: "Error handler middleware doesn't crash when logging exceptions"
  status: resolved
  reason: "error_handler_middleware.___extract_context crashes with AttributeError: 'HandlerObject' object has no attribute '__name__' - secondary crash during exception logging"
  severity: blocker
  test: 3
  root_cause: "HandlerObject is a dataclass wrapper - actual handler is in handler.callback attribute, not directly on HandlerObject"
  artifacts:
    - path: "handlers/error_handler_middleware.py:109"
      issue: "Accesses handler.__name__ directly but should use handler.callback.__name__"
  missing:
    - "Change line 109 from f\"{handler.__module__}.{handler.__name__}\" to f\"{handler.callback.__module__}.{handler.callback.__name__}\""
  debug_session: "diagnosed"
