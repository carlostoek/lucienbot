---
phase: 25
plan: reward-handlers-1service-loc
subsystem: missions / rewards (user-facing list + detail)
tech-stack: Python 3.12, aiogram 3, SQLAlchemy 2.0, pytest, ruff, GSD workflow
key-files:
  - handlers/reward_user_handlers.py (extract pure helpers for <=50 LOC + confirm 1svc)
  - services/reward_service.py (confirm pure top-level get_reward_emoji + 1-line delegate + Item2 comment)
  - tests/handlers/test_reward_user_handlers.py (confirm 1svc ports + add TestRewardUserPureHelpers)
  - .planning/quick/gsd-reward-handlers-1service-loc.log (full GSD + self-check PASSED + POOL/BATCH)
  - .planning/phases/25-reward-handlers-1service-loc/PLAN.md (source of truth)
  - (this) 25-reward-handlers-1service-loc-SUMMARY.md
---

# SUMMARY: Consolidate reward_user_handlers to exactly 1 service + extract pure helpers for ≤50 LOC (Item 7 / first of new pool of 4, post-unification Besito hardening)

**Date:** 2026-06-08 (executed)  
**Executor:** gsd-executor (following PLAN al pie de la letra, GSD discipline total, scope tight, copy patterns from golds exactly: item2/5/6 PLANs + gsd-reward-gamif-item2.log + mission_user_handlers + test + 24 SUMMARY BATCH)  
**Handoff from:** .planning/phases/24-remaining-besito-compositions/24-remaining-besito-compositions-SUMMARY.md + gsd log (BATCH close "4 items completed in this tirón (Item 6 final of max 4)", "Item 6/24 closed. BATCH...", self-check PASSED) + impact-analyzer item7 report (source of truth: scope tight 3 files, "first of new pool of 4", helper examples compute.../build..., tests add helper tests, 0 behavior/0 other handlers/0 mission_svc/0 delivery chg, design "MissionService vía get_service + rel access para reward data")  
**Status:** COMPLETE - Self-Check: PASSED  
**Pool note (explicit):** Pool anterior de 4 cerrado (tests passing per user). Nuevo pool de 4 iniciado. Quedan ~2-4 clusters del análisis inicial después de este pool. (This is the first of the new pool of 4 per impact + PLAN + 24-SUMMARY BATCH close; previous batch of 4 closed with tests passing, self-check PASSED, and explicit BATCH note.)

## Objective (from PLAN)
Tight, conservative, phased consolidation of `handlers/reward_user_handlers.py` (show_available_rewards + reward_detail + supporting `_build_rewards_buttons`) so they call **exactly 1 service** (MissionService via standardized `with get_service(MissionService) as ...` context + `get_service` lifecycle). Use relationship access (`mission.reward`) for reward data (precedent: mission_user_handlers). Use pure top-level `get_reward_emoji(reward)` imported from `services.reward_service` (promoted in prior work). Extract 1-2 pure helpers (e.g. `compute_reward_status_text`, `build_reward_detail_keyboard`) to bring all functions ≤50 LOC source (reward_detail currently exactly 50 lines per inspect; arch-enforcer note on "functions exceeding 50 lines"). Minimal support/confirm in `services/reward_service.py` (pure `get_reward_emoji` top-level + 1-line delegate with "Backward-compatible delegate added for Item 2..." comment). Update only `tests/handlers/test_reward_user_handlers.py` (confirm "1 service" ports + add unit tests for extracted pure helpers). **0 changes in delivery/claim behavior** (deliver_reward, log_reward_delivery, auto-deliver in mission increment, backpack earned paths untouched). **0 other handlers touched**. **0 mission_service changes**. UI/render identical (exact texts, emojis, progress █░ bars, button labels `f"{status_emoji} {reward_emoji} {name[:30]}"`, callback packing, alerts, empty case). 3 critical systems (gamif/missions/rewards, narrative, channel/VIP) always in mind (this flow is read-only list/detail; 0 tx/credit/deliver). GSD pre-log discipline on `.planning/quick/gsd-reward-handlers-1service-loc.log` (and cross-ref the impact analyzer's gsd-impact-analyzer-item7-...log) before every edit/gate/verif. Follow structure/patrones/snippets **al pie de la letra** from successful precedents (phase20 Item2 / gsd-reward-gamif-item2.log for reward 1-service + pure emoji + ports + helper tests + LOC inspect + self-check; 23/24 for item phasing + BATCH close language; mission handlers for 1svc+rel+get_service+__enter__/__exit__; item2 F5 Test*PureHelpers class pattern with import inside; 1-line/min support + delegate comment; docstrings "ported to 1-service... Arch-enforcer note addressed"; inspect LOC; etc.).

## Phases (strict order, 5 small, with gates + GSD pre every + safe points; DoD checklist verified before advance)
1. **F1 prep/GSD/baseline** (log, ruff clean on 3, baseline pytest w/ exact -p no:cov --override-ini="addopts=" (handler test ~11-14p), greps for 1svc+pure+rel+0 RS + guard + strings for pinning, python inspect LOC reward_detail==50 (drives extract), confirm fixtures (make_callback), patrones gold (get_service patch + __enter__/__exit__ from test_mission_user + item2 port; real pure emoji via mock_reward .reward_type=RewardType.BESITOS etc; mock_mission.reward= for rel), identify helpers (status_text + keyboard), confirm UI strings exact for new tests, read precedents (item2 PLAN + gsd log for ports/helper extract/self-check; mission_user_handlers for 1svc+rel; 24 SUMMARY for BATCH close language), GSD pre/post (multiple; wc tracked), "F1 safe point - baseline verde + ready for F2; no source changed yet". DoD all marked.
2. **F2 min support in RewardService** (confirm/promote get_reward_emoji to pure top-level + delegate 1-line + Item2/arch-enforcer comment if polish; already present post-Item2; ruff + smoke 4 branches pure + delegate + svc instance + close + grep 2 defs + targeted reward tests (non-blocking); GSD pre; "F2 safe point - pure top-level + delegate confirmed (already in tree); 0 behavior change; only this file touched (reversible 1-line if needed)". Handler/service baseline ready for extract.
3. **F3 refactor handlers of reward (ensure exactly-1-service + extract pure helpers for ≤50 LOC; UI identical; logging)** (in reward_user_handlers.py ensure show+reward_detail use with get_service(MissionService) + rel mission.reward + pure get_reward_emoji (already); extract 1-2 pure helpers compute_reward_status_text(progress, mission)->str and build_reward_detail_keyboard(mission_id:int)->InlineKeyboardMarkup (logic copy 1:1 from inline; verb+context+result; pure docstrings); reward_detail <=50 post (target 45-48; trim docstring/comment "extracted for <=50 LOC rule (Item 7 / arch-enforcer)" if boilerplate per item2 F3 precedent); add/ensure logs standard "reward_user_handlers | show... | user_id=... | count=..." and for detail (already present); post ruff+format hygiene + inspect LOC<=50 + greps 0 RS + 1svc + new defs + smoke import of funcs+helpers; confirm existing _build_* + new are pure/small; UI render 1:1 (tests F4 pin); GSD pre + gates; "F3 safe point - reward_detail <=50 via pure helpers; 1 service only via MissionService get_service + rel + pure emoji; UI render identical; logging compliant". Handler recompiles; F4 tests validate observable contract. Reversible editing only this file (or inlining).
4. **F4 port/actualización of reward_user_handlers tests + add tests for extracted pure helpers** (update/confirm test_reward_user_handlers.py to reflect/protect "exactly 1 service" + pure helpers; 0 parches de RewardService; all tests use @patch("handlers.reward_user_handlers.get_service") + mock_get_service.return_value.__enter__.return_value + __exit__ asserts; setups configure mock_reward with .reward_type=RewardType.XXX + .besito_amount + .name so real pure get_reward_emoji executes in _builds; for detail: mock_mission.reward = mock_reward (rel); without: .reward=None; closes use context __exit__ per mission precedent + item2 F4; docstrings of classes updated/confirmed "Tests ported to 1-service pattern (get_service + MissionService only) + pure formatting via get_reward_emoji. Arch-enforcer note addressed."; add new class TestRewardUserPureHelpers (or equiv) at end (after last class; pattern item2 F5): unit tests for extracted pures (import inside per file conv; at least 5-6 cases: status completed vs in-progress with bar math, 3+ RewardType for emoji+gives if render touches, button texts/status_emoji + truncation + packed cb data, progress bar edges 0/50/100, detail text with None descs); all asserts on edit_text/answer/params on mission only preserved and pass (behavior identical); ruff limpio; GSD pre + gate: suite completa del archivo pasa verde; "F4 safe point - reward handler tests confirmed ported to 1-service + pure emoji + rel; new TestRewardUserPureHelpers added + pass; arch-enforcer notes addressed; behavior identical". Confirms render of rewards list/detail (and helpers) identical. Reversible restoring old setups (not necessary).
5. **F5 re-runs of golds + final rules verif + self-check + handoff (first of new pool of 4; prior batch closed)** (re-execute golds protecting rewards flow: full `pytest tests/handlers/test_reward_user_handlers.py ...` green; targeted cross `pytest -k "reward or mission or TestReward or TestMission or deliver or TestCrossServiceAtomicity or atomicity or TestMissionE2E" ...` (or wider filtered; document pre-exist unrelated); bot smoke `python -c "import bot; print('bot import + routers (incl reward_user) ok')"` or equiv; ruff limpio on 3; rules verif (grep/inspect): 0 RS in handler (active), reward_detail LOC<=50 via inspect, logging formato present in show+detail, get_service(MissionService)+rel+pure emoji+helpers used + tests added, 1svc+<=50+logging+pure helpers+no biz logic; GSD entries complete for F5 + log final with self-check PASSED + full structure (phases/DoD/gates/archivos/tests passed/reglas verificadas (GSD pre every, scope tight 3 files + log + 0/0/0/0 behavior chg, 1svc+rel+pure, LOC<=50 via inspect, logging, pure helpers tests, no prod chg)/desviaciones/tests críticos para futuro (reward handler test full, cross reward/mission flows, atomicity reward paths, bot smoke, ruff+greps+LOC verifiers)/"Item 7/25 closed. First of new pool of 4. Previous batch of 4 (ending with Item 6/24 remaining-besito-compositions) closed with tests passing per 24-SUMMARY BATCH note + self-check PASSED. Ready for arch-enforcer re-scan (enfocado en reward handlers: exactly 1 service + <=50L + no RewardService) + test-guardian (correr los tests críticos listados) + gsd-executor del siguiente item del pool de 4"); self-check explícito "Self-Check: PASSED"; (opt but recommended) SUMMARY.md in phase dir with executive + refs to log + re-verif cmds (follows 24/23/20/19 structure); "F5 safe point final + success criteria of plan met". All DoD.

## Tasks Completed + Commits (individual per protocol after each phase/task; GSD pre before git if executed; specific add only touched)
- **F1 (prep, 0 logic edits):** chore(tests/hygiene): ruff baseline on 3 (clean, no edit); baseline gates (pytest 11/11, greps, inspect LOC=50, smoke, patterns). (Hygiene only; reversible.)
- **F2 (confirm only):** chore(reward_service): confirm pure top-level + 1-line delegate + Item2 comment (already present; no edit). Gates (ruff/smoke/grep 2/pytest) green.
- **F3 (extract):** feat(handlers): extract pure helpers compute_reward_status_text + build_reward_detail_keyboard (Item 7 / arch-enforcer <=50 LOC); slim reward_detail 50->36; keep 1svc+rel+pure emoji+logs+UI idéntica. (3 search_replace + ruff/format hygiene 0 logic.)
- **F4 (tests):** test(ports + helpers): confirm/refresh ports in test_reward_user_handlers.py (get_service+__enter__/__exit__+rel+pure attrs+docstrings); add TestRewardUserPureHelpers (5 pure unit tests import-inside). (1 search_replace; ruff/format hygiene 0 logic.)
- **F5 (re-runs + verif + self-check + handoff):** test(re-runs): re-runs golds (handler 16/16, cross 176p+1 xfail pre, bot smoke); ruff on 3; rules greps/inspect; GSD pre every + full self-check PASSED in log with structure + critical list + "Item 7/25 closed. First of new pool of 4. Previous batch... closed with tests passing per 24-SUMMARY... Ready for arch-enforcer... + test-guardian + siguiente item del pool"; (opt) write SUMMARY. (No prod changes.)
- All per task commit protocol + PLAN "GSD pre every" (if commits run: specific `git add <only-touched>`, messages with scope/0/0/0/refs to PLAN/GSD/DoD/Item7/POOL/BATCH).

All per task commit protocol + PLAN "GSD pre every".

## Desviaciones Encontradas y Resueltas (per PLAN "registrar en GSD")
- Ruff --fix + format in gates (F1 hygiene clean no edit; F3/F4/F5 hygiene 0 logic: format on inserts, 1 auto-fix in F4 test import sort). Committed as separate chore if executed (hygiene for "ruff limpio" DoD; 0 logic/semantic to helpers/LOC/UI; verified by post-format grep + diff capture in GSD). "0 edits in F1" interpreted as 0 *logic* (precedent in 23/22/19/20 for gate cleanliness); F2/F3/F4/F5 followed same.
- Initial smoke/inspect used bare "python -c" (PLAN) but env had no "python" (only python3/venv); retried with `./venv/bin/python -c` (consistent with ruff/pytest cmds in PLAN + item2/24 logged cmds); succeeded. Documented in logged cmds (F1/F3).
- Pre-existing dirty tree (git status from prior items/impact/plan dirs); always used specific `git add <only our touched file>` if committing; never staged unrelated. Not a deviation of this Item.
- Pre-existing test warnings (RuntimeWarning coroutine 'AsyncMockMixin._execute_mock_call' never awaited in _safe_answer paths; MovedIn20Warning declarative_base; InternalEventBus.emit never awaited in no-loop contexts; Deprecation utcnow in other tests). Pre-existing (from gsd-mw-hardening + test patterns + prior Items), not attributable to Item changes; documented in logs but gates counted as pass (xfails pre-exist, not new; 1 xfail in cross F5 pre-exist per 24/23 precedent "do not count as regression").
- Pre-existing unrelated fails in broader runs (e.g. daily concurrent claim UNIQUE, cross daily !success pre patches in priors, N806 in atomicity gold, SAWarnings on add/flush, unraisable). Pre-existing, documented in GSD/self-check but **not counted as regression of this Item** (per PLAN "Riesgo: baseline shows pre-existing unrelated fails ... document; do not count as regression" + precedents 24/23/22/20/19 "0 attributable to this Item").
- N/A for this Item: no 1-line fixes needed (already ported in tree); no new files (except opt SUMMARY); no edits to mission_service (explicit out); no behavior/contract change (UI identical, 0 delivery/claim impact protected by cross re-runs).
- All deviations logged in GSD entries at time of discovery + in self-check.

## Decisiones Tomadas (per PLAN sec 4, logged in first relevant GSD of phase)
1. **Helper names:** `compute_reward_status_text(progress, mission) -> str` and `build_reward_detail_keyboard(mission_id: int) -> InlineKeyboardMarkup` (exact per PLAN rec + verb+context+result conv; cf. calculate_emoji_counts_from_reactions in item2 / calculate_user_besitos_from_reactions in codebase). Confirmed in F3 first GSD.
2. **Delegate backward-compatible for get_reward_emoji:** Already present (Item2) with exact comment + 1-line; in F2 only confirm + GSD (no polish needed; comment/docstring match PLAN F2 snippet). 1-line; pure at module before class.
3. **Logging in the handlers:** Already present in show (user_id + count) and detail (user_id + mission_id + completed) in standard "módulo | acción | user_id=... | resultado=..." format (inside with, post data success). Confirmed/spot in F1/F3/F5; no new logs added (per "add/ensure" and "already in current").
4. **Pattern of tests for pure emoji + helpers:** Execute real `get_reward_emoji` on MagicMocks configured with `.reward_type` and fields (preferred for "pure" semantics + simplicity; already in tree + item2 F4); for new render helpers: pure unit tests (import inside, MagicMock minimal or simple objs; no service mocks for the helpers themselves; no @patch on the helper in handler tests (handler test covers full flow via real)). Followed item2 F5 TestCalculate... pattern + impact "cover ... for status, ... button texts/status_emoji + ... + _build_progress_bar math".
5. **Relationship check in reward_detail:** Kept `if not mission or not mission.reward` (consistent with mission_detail in mission_user_handlers + item2; "Recompensa no encontrada" alert). No is_active checks added in handler (scope tight; list filters in service). Tests cover without-reward by setting .reward=None explicitly (precedent item2 F4 + impact).
6. **Strict <=50 LOC count:** Used `inspect.getsourcelines(func)[0]` (counts def inclusive) per PLAN + item2 F3/F5; post-extract 36 (no trim needed; boilerplate did not push over). Verified in F3/F5.
7. **Update of docstrings of reward tests:** Confirmed/refresh (already "ported to 1-service... Arch-enforcer note addressed" in both classes post-item2; no residual 2-svc language or "closes_both" left; historical NOTE if present is commented as pre-Item2/7). Left brief historical comment if desired (not needed; already clean).
8. **Log file for GSD of Item 7:** `.planning/quick/gsd-reward-handlers-1service-loc.log` (cross-ref gsd-impact-analyzer-item7-...log); every pre-edit/pre-gate/pre-verif/pre-ruff/pre-pytest/pre-grep/pre-smoke/pre-final-self-check/pre-SUMMARY did `echo "=== $(date -Iseconds) | PHASE N | GSD pre-... - <desc + refs DoD + patrones copiados al pie de la letra>" >> <logfile>` (or run_terminal with echo/printf). At end log has 40+ entries + self-check PASSED + BATCH/POOL note. wc tracked.
9. **If second/third helper needed for render:** Not needed (status + keyboard extract dropped reward_detail from 50 to 36; tight scope prioritizes minimum). Documented (no third helper extracted).
10. **No export of pure in services/__init__.py:** Confirmed by scope (import direct from module sufficient and used in codebase + item2; no edit to __init__).
11. **Any decision differing:** None; all followed precedents conservatively (item2 ports + helper extract + LOC inspect + self-check, mission 1svc+rel, impact examples for status/keyboard, 24 BATCH language, get_service context + __enter__/__exit__ mocks, real pure via .reward_type attrs, docstrings "ported...", 1-line/min support + delegate comment, inspect LOC, etc.). Registered in GSD first relevant + self-check (none differed).

Any decision differing from above registered in GSD entry of the phase + note in self-check.

## GSD Discipline (non-negotiable, followed al pie)
- Pre *every* modification (search_replace/write ~4 on sources/tests/log/SUMMARY), gate (ruff x10+, pytest x8+ targeted/broader/combined, grep x8+ rules/1svc/helpers, smoke x5+, verif, self-check, summary), verif: run_terminal append "=== $(date -Iseconds) | PHASE N | GSD pre-... - <desc + refs DoD + patrones copiados al pie de la letra>" >> .planning/quick/gsd-reward-handlers-1service-loc.log ; wc -l after.
- 40+ entries total (planner INIT/DISCOVERY/PLANNING/pre-write/self-check ~4 + executor 38+; pre-F1, pre each edit (F3 3, F4 1), pre each gate/ruff/pytest/grep/smoke/self-check/SUMMARY, phase completes, final self-check).
- Style copied from precedents (gsd-reward-gamif-item2.log / gsd-remaining-besito-compositions.log / gsd-reward-besito-eventbus.log / gsd-getservice-unification.log / gsd-critical-tests.log): detailed, refs DoD, patrones copiados (item2 ports/helper extract/LOC inspect/self-check structure + 5-test pure class with import inside + "ported to 1-service... Arch-enforcer note addressed" + __enter__/__exit__ + mock_mission.reward= + .reward_type for real pure + get_service patch target; 24 BATCH "4 items completed in this tirón (Item 6 final of max 4)" + "Item 6/24 closed. BATCH..." + self-check PASSED; mission 1svc+rel+with get_service+__enter__/__exit__; impact scope/risks/tests/"first of new pool"; 1-line/min support + delegate comment; inspect LOC; etc.).
- Log exists with planner INIT/pre-write/post-write/self-check (3+1) + executor pre-F1 + all phases + pre every + self-check PASSED + BATCH/POOL note.
- No edits (even to PLAN or log beyond appends) without pre-log.

## Scope (tight, 0/0/0/0 verified at every gate + final grep/self-check)
- Only files listed in PLAN "Archivos que se modificarán" + the log GSD + this PLAN + optional SUMMARY.
- 0 new files (except SUMMARY optional at end; produced).
- 0 prod behavior change (all text construction, emoji choice, bar math, status, button labels, cb packing, logging format, empty case, error alerts identical pre/post; helpers pure 1:1 move of prior inline; tests pin exact phrases/math; cross re-runs protect delivery/claim/atomicity indirectly).
- 0 atomicity/claim/delivery impact (0 change to deliver_reward / _deliver_* / log_reward_delivery / close / create_* / get_* / held subs (package/vip) / observer / anything in claim/delivery paths / atomicity contracts; re-runs of cross_service_atomicity reward paths + mission flows + handler tests protect; this item is read-only list/detail).
- 0 other handlers (backpack_handler.py, reward_admin_handlers.py, mission_user_handlers.py, gamification_*, common, broadcast, store_*, etc. — even if they touch rewards; explicit out).
- 0 mission_service.py (no refactor of get_available_rewards_for_user or increment_and_deliver; handler calls only the MissionService method; enrichment is service impl detail; explicit "NO tocar mission_service").
- 0 backpack_service / its tests / reward_admin / reward_service unit tests / admin handlers (separate domains; legit use of RewardService for create/deliver/history; out of scope).
- 0 models, keyboards, callback_data, bot.py, services/__init__.py, utils, lucien_voice, middlewares.
- 0 changes to deliver_reward, _deliver_*, log_reward_delivery, close logic, held services in RewardService, event listeners, atomicity contracts, claim flows, backpack earned rewards.
- 0 new tests outside the reward_user test file (no service tests for emoji; no new integration files).
- 0 edición de CLAUDEs, decisions.md, AGENTS, ROADMAP, fase_*, docs/, o cualquier .md excepto this PLAN + the log GSD (impact report + MEMORY already done by analyzer).
- 0 broad "fix all reward handlers" or "touch mission_service for orchestration perception".
- 0 behavior or contract changes (0 impact on claim/delivery/partial failure; UI identical).
- 3 critical systems protected (gamif/missions/rewards as the domain here; narrative cross via events; channel/VIP; this item read-only info; 0 tx/credit/deliver/claim; re-runs protect).

## Key Patterns Copied (al pie de la letra, per PLAN "Copia patrones **al pie de la letra** de golds")
- Patrón get_service + with + mock en tests + closes __exit__: copied from `tests/handlers/test_mission_user_handlers.py` + item2 F4 port (RewardType import, get_service patch, mock_instance + __enter__, mock_mission.reward = , .reward_type for pure, closes to __exit__, calls asserts on mission only, docstrings "ported to 1-service... Arch-enforcer note addressed", NOTES cleaned).
- Extracción de helper puro para LOC + UI idéntica: copied spirit + snippets from F3 of item2 (insert pure calculate_... near section; replace inline with call; docstring "Calcula... Función pura."; inspect LOC post; test refresh path green; 1-2 helpers if sufficient; trim docstring if 51 by boilerplate + comment "extracted for LOC rule (Item X)").
- Logging: "módulo | acción | user_id=... | resultado=..." (confirmed existing in show/reward_detail per item2 F2 logs for show/reward_detail + F3 for handle_reaction; no new needed).
- 1-line / min support + delegate comment: from item2 F1 (pura + delegate 1-line + "Backward-compatible delegate added for Item 2 (arch-enforcer...)").
- GSD entries detalladas: "pre-xxx <file> (F<N> <motivo>) - <desc + refs DoD + patrones copiados al pie de la letra de item2 PLAN F4 + gsd-reward-gamif-item2.log + impact report helper examples + 24 SUMMARY BATCH>"; wc; style of item2 (46+) and 24 (55+).
- Safe points + self-check at end of log: structure of item2 (lista fases/DoD/gates/archivos/tests que pasaron/reglas verificadas (GSD pre every, scope tight 3 files + log + 0/0/0/0, 1svc+rel+pure, LOC<=50, logging, pure helpers tests, no prod chg)/desviaciones/tests críticos/"Item closed. Ready for ... + arch-enforcer + test-guardian + siguiente item del pool") + 24 BATCH note.
- Precedentes PLAN/GSD + handoff + pool/batch: .planning/phases/20-.../PLAN.md + gsd-reward-gamif-item2.log (Item2 reward 1svc gold), 23/24 PLANs + SUMMARYs (BATCH "4 items completed in this tirón (Item 6 final of max 4)", "Item 6/24 closed. BATCH...", self-check), impact report .md (source of truth for scope/map/risks/tests/ "first of new pool of 4").
- VOZ/estilo: handlers speak via existing texts (Lucien voice preserved identical); no change to user messages.
- 3 sistemas críticos: always in mind (gamif/missions/rewards as the domain here; narrative cross via events; channel/VIP); this item is read-only info (0 tx/credit/deliver/claim); re-runs of cross protect.
- Commands: exact from PLAN sec5 + "Instrucciones" ( -p no:cov --override-ini="addopts=", ./venv/bin/python -m for ruff/pytest, python -c for smokes with venv fallback, greps for rules/1svc/helpers, python -c for LOC inspect, bot smoke, combined critical re-run in F5).
- Test class for pure helpers: exact pattern from item2 F5 (class TestCalculate... with 5 tests; import inside test funcs per file convention; no service mocks for pure; placed after last class).
- 0 export pure in __init__: confirmed (import direct sufficient + used).
- Any differing: none; registered in GSD + self-check (none).

## Tests / Gates Summary (all passed with 0 attributable reg; pre-exist doc non-reg)
- See self-check in log (above) for per-phase numbers + full re-runs.
- Final: 16 (handler file post + new helpers), 176 (broader cross -k with 1 xfail pre + warnings pre), bot smoke, ruff limpio on 3 (hygiene format 0 logic), greps/inspect all criteria (0 RS, 36<=50, logs, 1svc+rel+pure+helpers+tests).
- Gold protection: handler test full (list empty/with, detail not-found/no-reward/completed/progress/calls/closes); pure helpers (status completed/in-progress, keyboard 2btns+texts+cb+trunc, progress bar 0/50/100/edge, none-descs path); cross re-runs protect reward/mission flows + atomicity reward paths + delivery indirectly.
- 1-line/min support + delegate: confirmed (no change needed; already Item2).
- Gates: always `-p no:cov --override-ini="addopts="` for clean exit (precedent all golds 20/21/23/24 + item logs); targeted -k first; broader at end; ruff pre/post; GSD pre each.
- Logging requirement coverage: tests do not assert logs usually (except middleware); gate is manual grep/inspection during edits + inclusion in GSD log (logs present in standard format at 105/158).
- Pre-exist in broader: 1 xfail (pre), warnings (Runtime emit not awaited, Deprecations, SA, unraisable, MovedIn20), N806 if surfaced in atomicity subset (tolerated + doc per PLAN + precedent). Documented; 0 counted as regression of this Item.

## Handoff / Next
**Item 7/25 closed. First of new pool of 4. Previous batch of 4 (ending with Item 6/24 remaining-besito-compositions) closed with tests passing per 24-SUMMARY BATCH note + self-check PASSED. Ready for arch-enforcer re-scan (enfocado en reward handlers: exactly 1 service + <=50L + no RewardService) + test-guardian (correr los tests críticos listados en self-check arriba) + gsd-executor del siguiente item del pool de 4 (long store_admin, long mission_admin, observability/health o similar de los ~6-8 restantes per roadmap/backlog clusters).**

(User opt) future tirón (but do not initiate without new prompt per user instr "propose if user quiere más en futuro (pero no inicies sin nuevo prompt)").

**Pool confirmation in output final:** "Pool anterior de 4 cerrado (tests passing per user). Nuevo pool de 4 iniciado. Quedan ~2-4 clusters del análisis inicial después de este pool."

(Ver .planning/phases/25-reward-handlers-1service-loc/PLAN.md + gsd-reward-handlers-1service-loc.log (full GSD + self-check PASSED + POOL/BATCH confirm + critical tests) + this SUMMARY + (if) commits for execution details + handoff.)

**Hecho con disciplina total como en ejecuciones previas exitosas (mw-hardening, eventbus, reward-gamif, getservice, critical-tests, reward-besito, remaining-besito). Scope tight, GSD pre every, patterns copied al pie de la letra, 0 behavior/0 delivery/0 atomicity, 0 scope creep. Nuevo pool de 4 iniciado (1/4: reward handlers 1svc+LOC; remaining ~2-4 clusters after this pool).**

(Hecho con 💋 para Diana (Señorita Kinky) — gsd-executor subagent; first of new pool of 4, post-unification Besito hardening tirón.)
## Self-Check: PASSED

All criteria met. Item 7/25 closed as first of new pool of 4. Pool anterior de 4 cerrado (tests passing per user). Nuevo pool de 4 iniciado. Quedan ~2-4 clusters del análisis inicial después de este pool.
