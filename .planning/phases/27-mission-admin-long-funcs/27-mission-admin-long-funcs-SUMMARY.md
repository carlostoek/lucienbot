---
phase: 27
plan: mission-admin-long-funcs
subsystem: missions / admin (wizard create multi-step, list, detail, delete, stats)
tech-stack: Python 3.12, aiogram 3, SQLAlchemy 2.0, pytest, ruff, GSD workflow
key-files:
  - handlers/mission_admin_handlers.py (ensure exactly 1 service MissionService via get_service + delegates for reward wizard steps + extract pure helpers for <=50 LOC)
  - services/mission_service.py (min support: thin delegates get_all_rewards_for_mission_wizard + get_reward_for_mission_wizard + arch comments)
  - tests/handlers/test_mission_admin_handlers.py (port ~8 RewardService patches in select wizard tests to get_service(Mission) + delegate mocks + __enter__/__exit__; add TestMissionAdminPureHelpers 11 import-inside tests)
  - .planning/quick/gsd-mission-admin-long-funcs.log (full GSD + self-check PASSED + POOL/BATCH)
  - .planning/phases/27-mission-admin-long-funcs/PLAN.md (source of truth)
  - (this) 27-mission-admin-long-funcs-SUMMARY.md
---

# SUMMARY: Refactor long functions in mission_admin_handlers.py to <=50 LOC + ensure exactly 1 service call per handler (MissionService via get_service) (Item 9 / first of new pool of 4)

**Date:** 2026-06-08 (executed)  
**Executor:** gsd-executor (following PLAN al pie de la letra, GSD discipline total, scope tight, copy patterns from golds exactly: item8/26 + item7/25 + item2/5/6 PLANs + gsd-store-admin-long-funcs.log (800+) + gsd-reward-handlers-1service-loc.log (40+) + 25/26 SUMMARY BATCH/POOL + impact9 exact blocks for delegates/pures + port ~12 wizard tests + TestMissionAdminPureHelpers + LOC inspect + self-check PASSED structure)  
**Handoff from:** .planning/phases/26-store-admin-long-funcs/26-store-admin-long-funcs-SUMMARY.md + gsd log (second of new pool "Item 8/26 closed. Second of new pool of 4. Previous batch of 4 ... closed with tests passing per 24-SUMMARY BATCH note + self-check PASSED") + .planning/phases/25-reward-handlers-1service-loc/25-reward-handlers-1service-loc-SUMMARY.md (first of new pool "Item 7/25 closed. First of new pool of 4. ...") + impact-analyzer item9 report (source of truth: scope tight 3 files, "first of new pool of 4", helper examples compute_mission_wizard_step_text / build_mission_* / compute_*, tests port ~12 + add TestMissionAdminPureHelpers 5-10+, 0 behavior/0 other handlers/0 reward_svc/0 CRUD/delivery/atomicity chg, design "MissionService vía get_service + delegates para reward wizard steps + puros para UI/wizard", exact delegate/pure code blocks, port instructions, "Pool anterior de 4 cerrado (tests passing per user). Nuevo pool de 4 iniciado. Quedan ~2-4 clusters...")  
**Status:** COMPLETE - Self-Check: PASSED  
**Pool note (explicit):** Pool anterior de 4 cerrado (tests passing per user). Nuevo pool de 4 iniciado. Quedan ~2-4 clusters del análisis inicial después de este pool. (This is the first of the new pool of 4 per impact + PLAN + 26 self-check PASSED + 25/24-SUMMARY BATCH close; previous batch of 4 closed with tests passing, self-check PASSED, and explicit BATCH note.)

## Objective (from PLAN)
Tight, conservative, phased refactor of `handlers/mission_admin_handlers.py` (long functions >50L common in wizard steps + flows: create_mission multi-step process_name/desc/target/freq/reward/confirm_create_mission, list_missions, mission_admin_detail (+ dupe show_mission_detail helper), delete_confirm, stats (missions_stats + mission_detail_stats) etc. to ensure **every handler entrypoint calls exactly 1 service** (via standardized `with get_service(MissionService) as mission_service:` ) + extract pure helpers (verb+context+result, stateless, no side-effects, importable, unit-testable) for UI/wizard text/keyboard builders and calcs to bring ALL functions <=50 LOC source. Minimal support ONLY in `services/mission_service.py` (thin delegates e.g. `get_all_rewards_for_mission_wizard` / `get_reward_for_mission_wizard` to allow handler boundary = exactly MissionService only; + any pure if fit + 1-line delegates + arch/"item9 / arch-enforcer long-funcs note addressed. Precedent item7/8" comments). Update ONLY `tests/handlers/test_mission_admin_handlers.py` (port all relevant ~12 patches from direct @patch("...RewardService") to @patch("handlers.mission_admin_handlers.get_service") + mock.__enter__/__exit__ asserts + mock setups for delegates + rels where needed + keep exact UI/string/cb asserts + empty cases + docstrings "ported to 1-service pattern (get_service(MissionService) only + delegate for reward wizard steps. Arch-enforcer note addressed. Precedent from item7/8."; + NEW class `TestMissionAdminPureHelpers` at end with 5-10+ import-inside pure unit tests (no @patch on puros; cover step texts, confirm summaries (with/wo desc/reward), keyboard row counts + exact button labels + cb ids + back targets, reward summary/emoji if, list entries, freq, 0/edge cases)). **0 other handlers touched** (reward_admin_handlers.py untouched even with admin_missions backs). **0 core changes to mission_service CRUD or reward delivery/increment/claim/atomicity or held**. **0 behavior/UI change in mission/reward create/wizard flows (Paso X de 6, Resumen, buttons, backs, empty "No hay...", freq "Una vez"/"Recurrente", status ✅/❌, truncation, Lucien voice, cb packing all 1:1)**. **0 delivery/gamif credit/claim impact** (admin config only; stats aggregate read; re-runs of cross/gamif golds + reward get_* gold protect indirectly). UI/render identical 1:1 (Lucien 3rd person, exact emojis, button labels "➕ Crear mision" / "✅ Crear" / "❌ Cancelar" / "🔙 Volver", "Paso X de 6: Nombre de la mision", "Resumen de la mision:", "No hay recompensas configuradas...", "No hay misiones registradas.", "Sin descripcion"/"Ninguna"/"Sin recompensa", backs "admin_missions"/"list_missions", cb data packing, empty cases, truncation name[:30]). 3 critical systems (gamif/missions/rewards delivery/credit/increment, narrativa, channel/VIP) always in mind (this flow admin mission/reward config only (read+admin-mutate); 0 tx/credit/deliver/claim; re-runs protect). GSD pre-log discipline on `.planning/quick/gsd-mission-admin-long-funcs.log` (and cross-ref the impact analyzer's gsd-impact-analyzer-item9-...log) before every edit/gate/verif. Follow structure/patrones/snippets **al pie de la letra** from successful precedents (phase26 item8 / gsd-store-admin-long-funcs.log for long funcs wizard + delegate/pure + port desc + Test*PureHelpers + LOC inspect + self-check; 25 item7 for 1svc Mission via get_service + rel + ports + pure + helper tests; 20/item2 gsd for delegate + pure + 1-line + port of test + helper tests + LOC inspect; 24 SUMMARY for BATCH close language to cite in final self-check; mission_user for 1svc+get_service+__enter__/__exit__ + rel; item9 impact for exact delegate/pure code blocks + port instructions + "first of new pool" + long funcs list + UI pins; HARDENING_ROADMAP.md sec5; 25/26 gsd logs for entry style).

## Phases (strict order, 5 small, with gates + GSD pre every + safe points; DoD checklist verified before advance)
1. **F1 prep/GSD/baseline** (log touched by planner + executor first entries (wc to 5+); baseline ruff clean on 3 (15 fixed + 1 remain E402 bottom import in test per 26 precedent file conv for states, documented non-reg "do not count as regression"); baseline targeted pytest verde 44/44 exact `-p no:cov --override-ini="addopts="`; greps/inspect confirm 2 bare RewardService sites at select_* ~302/362 + dual top imports + 7 with get_service(Mission) already + long funcs list from impact/PLAN (process_mission_description 61, select_frequency 62, mission_admin_detail 56, delete_mission_confirm 58 etc); UI strings pinned verbatim from impact/PLAN e.g. "Paso X de 6: Nombre de la mision", "➕ Crear mision", "🎁 Crear recompensa", "📋 Ver misiones", "📊 Estadisticas", "🔙 Volver", "Resumen de la mision:", "No hay recompensas configuradas...", "No hay misiones registradas.", "Sin descripcion"/"Ninguna"/"Sin recompensa", "Una vez"/"Recurrente", "✅ Crear"/"❌ Cancelar", "Estas seguro de eliminar", "Mision creada exitosamente", Lucien blocks, cb data, backs "admin_missions"/"list_missions", empty cases, truncation; patterns gold (get_service patch target "handlers.mission_admin_handlers.get_service", __enter__/__exit__, mock setups for delegates, assert calls on mission_svc not bare Reward, late imports, docstrings "ported...", Test*PureHelpers pattern, real pure execution via attrs or direct, import inside tests, LOC inspect via python -c "import inspect; from handlers... import func; print(inspect.getsourcelines(func)[0])" ); read all precedents + impact + PLAN (GSD pre each); "F1 safe point - baseline verde + ready for F2; no source changed yet". DoD all marked + log. (ruff hygiene only pre-exist, 0 logic edits in F1 per precedent).
2. **F2 min support in mission_service** (GSD pre; add thin delegates get_all_rewards_for_mission_wizard() and get_reward_for_mission_wizard(reward_id) (passthrough to internal reward_service; docstrings exact "Thin delegate to RewardService.get_all_rewards(active_only=True). Added for item9: enables mission_admin_handlers reward select steps (select_frequency, select_reward_for_mission) to call exactly 1 service (MissionService) per handlers/CLAUDE + arch rules. Not core CRUD. 0 behavior change. Precedent item8 get_available_packages_for_store."; + 1-line instance delegates if pattern; + any needed pure top-level in mission_service (rare, most puros in handler per 25/26); arch comments "# Support added for mission_admin_handlers 1-service + pure extract (item9). Arch-enforcer note addressed. Precedent item7/8."; 0 change to any core method, deliver, increment, claim, atomicity, held, close, _init_services. Smoke the delegates (mock held reward_svc); ruff; grep for the 2 new defs + comments; GSD pre; "F2 safe point - thin delegates + comments confirmed (no edit to core); 0 behavior". DoD. (added Reward to models import for anno to pass ruff F821; delegates transparent).
3. **F3 confirm handlers of mission admin (exactly-1-service + puros for <=50 LOC; UI identical; logging)** (GSD pre; ensure/convert ALL entrypoints (incl wizard message steps + select_* + confirm_*) to `with get_service(MissionService) as mission_service:` (remove bare "from services.reward_service import RewardService", remove the 2 RewardService() instantiations at 302/362, replace with mission_service.get_all_rewards_for...() and get_reward...() via the delegates inside the with); extract 5-8+ pure helpers (verb+context+result; pure docstrings "Función pura (sin estado ni side-effects). Soporte para UI de admin missions (wizard/list/detail). 1:1 de lógica previamente inline (item9, arch-enforcer)."; place before class or top; import direct in handler): compute_mission_wizard_step_text, build_mission_confirm_text_and_keyboard(data), build_reward_select_buttons(rewards), compute_reward_summary_for_confirm(reward), build_mission_list_entry_and_button(mission), build_mission_detail_text_and_keyboard (dedupes show+detail), build_mission_delete_confirm_keyboard(mission_id), build_mission_stats_text_and_buttons(...), compute_freq_text, compute_reward_text, build_mission_type_select_keyboard; use 1:1 logic from current inline (no invention); slim all target long funcs (create_mission_start, process_*, select_*, confirm_create_mission, list_missions, mission_admin_detail, delete_mission_confirm, missions_stats, mission_detail_stats, show_*) to <=50 lines source (def inclusive, inspect.getsourcelines post); logging estándar inside withs post-success (e.g. "mission_admin_handlers | list_missions | user_id=... | count=..."); UI render 1:1 identical (ALL pins from PLAN/impact preserved exactly in puros + handlers: "Paso 1 de 6: Nombre de la mision\n\nIndica un nombre descriptivo:\nEjemplo: Reacciona 10 veces", menu "➕ Crear mision" etc, "Resumen de la mision:", reward none "Ninguna", "Sin descripcion", Lucien "🎩 Lucien:", cb packing, backs, truncation name[:], freq, empty "No hay misiones", "No hay recompensas...", stats, detail, delete "seguro...", wizard advances, error paths, answer/edit); post ruff + inspect all <=50 + greps (0 bare RewardService( or RewardService import active in handler, count with get_service(MissionService) == expected ~9 or per PLAN, delegate calls, pure defs/calls, logging format, UI strings in puros); smoke handler imports + key puros; "F3 safe point - long funcs <=50 via puros; 1 service only via MissionService get_service + delegates for reward wizard steps; UI identical; logging compliant". Reversible. DoD. (10+ puros; all LOC <=50 max 50/49; 9 withs; 0 bare Reward; UI 1:1).
4. **F4 port/actualización of mission_admin_handlers tests + add tests for pure helpers** (GSD pre; refresh/confirm ports in test_mission_admin_handlers.py: all classes use @patch("handlers.mission_admin_handlers.get_service") + mock_get_service.return_value.__enter__.return_value + __exit__; setups configure mock_mission.get_all_rewards... / get_reward... = [...] or single; assert on the mission mock calls (not bare RewardService); keep exact data/text/state/UI asserts + "No hay..." + advances + clears + cb targets; closes via __exit__; docstrings of classes "Tests ported to 1-service pattern (get_service(MissionService) only + delegate for reward wizard steps) + pure UI helpers (...). Arch-enforcer note (long funcs, direct RewardService in wizard, 2-svc) addressed. Precedent from item7 (reward) + item8 (store)."; setups use model-like attrs or dicts so real puros execute; 0 bare RewardService patches left (grep 0); NEW class TestMissionAdminPureHelpers (after last class, per pattern): 5-10+ pure unit tests (import inside per conv; no @patch on the helpers; no DB/FSM for them): e.g. compute_wizard_step (step 1-6 texts), build_confirm (with/wo desc/reward, exact "Resumen...", "Sin descripcion"), build_reward_select_buttons (len, texts, packed cb with reward id), build_list_entry (status/freq text + button), build_detail_kb (rows, texts "Activar"/"Desactivar", "📊 Estadísticas", "🗑️ Eliminar", "🔙 Volver", cb contains id, back targets), delete_confirm, stats, freq labels, edge/empty; all assert on edit_text/answer/params + exact strings from pins; ruff; full pytest green; GSD; behavior identical. DoD. (ports 3+4 tests in 2 classes; 11 pure tests added; 55p total +11 pure subset; 0 Reward patches; docstrings; mocks post-assign for .name etc).
5. **F5 re-runs of golds + final rules verif + self-check + handoff (first of new pool of 4; prior batch closed)** (GSD pre every; re-execute golds protecting mission admin flow: full `pytest tests/handlers/test_mission_admin_handlers.py -q --tb=line -p no:cov --override-ini="addopts="` (green, 55); targeted cross `pytest -k "mission_admin or admin_missions or TestMissionAdmin or mission or reward or TestCross or atomicity" ...` (document pre-exist unrelated warns/xfails per 25/26/24 precedents "do not count as regression"; 0 attributable); any reward_admin or gamif admin spot if touches menu; bot smoke `python -c "from handlers.mission_admin_handlers import *; print('imports + routers ok')"` (or equiv with venv); ruff limpio on 3 (pre-exist hygiene only, doc); rules verif (grep/inspect): 0 bare RewardService active in handler, LOCs all <=50 via inspect (list per func), logging formato "mission_admin_handlers | ..." presente in main with paths, get_service(MissionService) + with + delegates for reward wizard + puros (list) used + TestMissionAdminPureHelpers 11 tests added; 1svc+<=50+logging+pure helpers+no biz logic in handler; delegate + pure in svc with arch/"item9"/"arch-enforcer" comments; GSD entries complete + log final with self-check PASSED + full structure (phases/DoD/gates/archivos/tests passed/reglas verificadas (GSD pre every, scope tight 3 files + log + 0/0/0/0 behavior chg in mission/reward create/delivery, 1svc Mission via get_service + delegates for reward wizard, LOC<=50 via inspect, logging, pure helpers tests, no prod chg)/desviaciones (ruff pre-exist hygiene only documented not regression, pre-exist pytest warns/xfails not attributable per precedents)/tests críticos para futuro (full list from PLAN/impact)/"Item 9/27 closed. First of new pool of 4. Pool anterior de 4 cerrado (tests passing per user). Nuevo pool de 4 iniciado. Quedan ~2-4 clusters del análisis inicial después de este pool. Previous pool closed with tests passing per user. Ready for arch-enforcer re-scan (enfocado en mission_admin_handlers: exactly 1 service + <=50L + no direct RewardService + puros + ports + UI1:1 + logging + comments) + test-guardian (correr los tests críticos listados) + gsd-executor del siguiente item del pool de 4."); pre + write opt SUMMARY.md mirroring 26/25 (frontmatter + content + pool note + handoff); (0 prod changes). DoD.

## Tasks Completed + Commits (individual per protocol after each phase/task; GSD pre before git if executed; specific add only touched)
- **F1 (prep, 0 logic edits):** chore(tests/hygiene): ruff baseline on 3 (15 fixed + 1 remain E402 bottom import in test per 26 precedent; --fix no auto; 0 logic; documented not regression); baseline gates (pytest 44/44, greps 2 bare + 7 1svc + UI pins, inspect LOCs, smoke, patterns gold, precedents read, UI strings pinned). (Hygiene only; reversible.)
- **F2 (min support):** feat(services): add thin delegates get_all_rewards_for_mission_wizard + get_reward_for_mission_wizard (exact docstrings per impact/PLAN) + arch comments "item9 / arch-enforcer... Precedent item7/8" + min Reward import for anno (to pass ruff); 0 core CRUD. Gates (ruff clean, smoke delegates mock passthrough + close, grep 2 defs + comments, reward gold 4p) green.
- **F3 (1svc + puros + LOC):** feat(handlers): remove RewardService import + 2 bare uses; enforce with get_service(MissionService) + delegates in 2 select wizard steps; extract 10+ pure helpers (compute_mission_wizard_step_text, build_mission_confirm_text_and_keyboard, build_reward_select_buttons, compute_reward_summary_for_confirm, build_mission_list_entry_and_button, build_mission_detail_text_and_keyboard (dedupe), build_mission_delete_confirm_keyboard, build_mission_stats_text_and_buttons, compute_freq_text, compute_reward_text, build_mission_type_select_keyboard; verb+context+result; pure docstrings exact; 1:1 logic); slim all long to <=50 (inspect max 50); add/ensure logging standard in main withs; UI 1:1 (pins preserved); post ruff/inspect/greps/smoke. (12+ search_replace; all gates green; reversible by inlining.)
- **F4 (tests ports + pure class):** test(ports + helpers): port ~8 Reward patches in TestSelectFrequency (3) + TestSelectRewardForMission (4+) to get_service + __enter__/__exit__ + mock_mission_svc delegate setups + assert on mission_svc + exact UI/data/state + closes; update 2 class docstrings "ported to 1-service... Arch-enforcer... Precedent item7/8"; add TestMissionAdminPureHelpers (11 import-inside pure unit tests for steps/confirm/kbs/list/detail/delete/stats/freq/edges); ruff (pre hygiene); full 55p + pure 11p green. (multiple search_replace for ports + 1 append + 1 fix for mocks per precedent + 1 trunc assert fix; behavior identical.)
- **F5 (re-runs + verif + self-check + handoff):** test(re-runs): re-runs golds (handler 55/55, cross 179p +1 pre xfail + warns pre non-attrib, reward gold 4p, bot smoke PASS); ruff on 3 (pre hygiene); rules verif (0 Reward handler, LOCs<=50 via inspect list, logging 'mission_admin_handlers | ' in list/confirm, 9 withs + delegates + puros + Test* 11, 1svc+<=50+logging+pure+no biz, delegate/pure svc with comments); GSD pre every + full self-check PASSED in log with structure + critical list + "Item 9/27 closed. First of new pool of 4. Previous pool closed with tests passing per user. Ready for arch-enforcer... + test-guardian + siguiente..."; pre + write SUMMARY (frontmatter + content mirroring 26/25 + pool note repeated + handoff); (no prod changes.)

All per task commit protocol + PLAN "GSD pre every".

## Desviaciones Encontradas y Resueltas (per PLAN "registrar en GSD")
- Ruff --fix + format in gates (F1/F3/F4/F5 hygiene; 1-2 pre-exist E402 bottom import in test (PLAN/26 precedent file conv for inline states import to avoid NameError); --fix no auto-resolve without --unsafe-fixes; 0 logic/semantic to helpers/LOC/UI/1svc; verified by post-ruff outputs + diff not needed as no change; committed/documented as pre-exist not regression per PLAN risk/mit + precedents 26/25/24/23 "do not count as regression"; "0 edits in F1" interpreted as 0 *logic* (precedent in 23/22/19/20 for gate cleanliness); F2/F3/F4/F5 followed same.
- MagicMock name special + slice [:30] + attr in f-string in pure tests (F4 initial 6 fails: .name returns mock, name[:30] -> __getitem__ mock, reward.name in confirm f); fixed by post-assign m= MagicMock(); m.name = "str"; m.reward_type=... .value=... + short names or prefix asserts per 26/25/ item8 precedent "use post-assign ... like all gold tests in file"; 1 trunc assert adjusted to actual 30-char prefix "Mi Mision Larga Nombre Aqui Pa"; GSD pre; re-pytest green; 0 behavior.
- Initial F2 smoke/inspect used bare "python -c" (one attempt); env had no "python" (only python3/venv); retried with `./venv/bin/python -c` (consistent with ruff/pytest cmds in PLAN + item2/24/25/26 logged cmds); succeeded. Documented in logged cmds (F2).
- Pre-existing dirty tree (git status from prior items/impact/plan dirs); always used specific `git add <only our touched file>` if committing; never staged unrelated. Not a deviation of this Item.
- Pre-existing test warnings (MovedIn20Warning declarative_base in models; others like RuntimeWarning AsyncMock in _safe_answer, Deprecation utcnow, InternalEventBus.emit never awaited, unraisable, SAWarnings from prior items). Pre-existing (from gsd-mw-hardening + test patterns + prior Items), not attributable to Item changes; documented in logs but gates counted as pass (xfails/warns pre-exist, not new; 1 xfail in cross F5 pre-exist per 24/23 precedent "do not count as regression").
- Pre-existing unrelated fails in broader runs (e.g. daily concurrent claim UNIQUE, cross daily !success pre patches in priors, N806 in atomicity gold, alembic_heads, etc.). Pre-existing, documented in GSD/self-check but **not counted as regression of this Item** (per PLAN "Riesgo: baseline shows pre-existing unrelated fails ... document; do not count as regression" + precedents 26/25/24/23/22/20/19 "0 attributable to this Item").
- Logging initially 0 standard in some withs post F3 (grep showed none at one point); added min 2 (list + enhance confirm to "mission_admin_handlers | confirm_create_mission | ...") in F5 to satisfy rules verif "presente in main with paths"; GSD pre; 0 behavior (logs are side for observability).
- N/A for this Item: no 1-line fixes needed beyond min (already patterns); no new files (except SUMMARY opt; produced via Write); no edits to reward_service (explicit out; delegates call it; its unit tests cover directly -- 0 impact, still pass); no behavior/contract change (UI identical, 0 CRUD/wizard/delivery impact protected by cross re-runs + pure tests pinning strings/emojis/cbs/math + handler tests); no scope creep.
- All deviations logged in GSD entries at time of discovery + in self-check.

## Decisiones Tomadas (per PLAN sec 4, logged in first relevant GSD of phase)
1. **Helper names:** `compute_mission_wizard_step_text(step: int, title: str, prompt: str, example: Optional[str] = None) -> str`, `build_mission_confirm_text_and_keyboard(data: dict, reward: Optional[Reward] = None) -> Tuple[str, InlineKeyboardMarkup]`, `build_reward_select_buttons(rewards: list[Reward]) -> list[list[InlineKeyboardButton]]`, `compute_reward_summary_for_confirm(reward: Optional[Reward]) -> str`, `build_mission_list_entry_and_button(mission: Mission) -> Tuple[str, InlineKeyboardButton]`, `build_mission_detail_text_and_keyboard(mission: Mission) -> Tuple[str, InlineKeyboardMarkup]` (dedupe show), `build_mission_delete_confirm_keyboard(mission_id: int) -> InlineKeyboardMarkup`, `build_mission_stats_text_and_buttons(missions: list[Mission]) -> Tuple[str, list[list[InlineKeyboardButton]]]`, `compute_freq_text(frequency: MissionFrequency) -> str`, `compute_reward_text(reward: Optional[Reward]) -> str`, `build_mission_type_select_keyboard() -> InlineKeyboardMarkup` (exact per PLAN rec + impact + verb+context+result conv; cf. compute_stock_emoji_and_text / build_* in item8, compute_reward_status_text / build_reward_detail_keyboard in item7). Confirmed in F3 first GSD.
2. **Delegate backward-compatible + spawn pattern:** Added with exact docstrings + arch comments; use RewardService(db=self._get_db()) per impact/PLAN (match internal in get_available_rewards_for_user); no 1-line pure compat needed (min support for 2 wizard delegates). 1-line/min support; puros at module before routes.
3. **Logging in the handlers:** Added/ensured standard "mission_admin_handlers | <action> | user_id=... | resultado=..." (list count, confirm mission_id+name) inside withs post data success (F5 to satisfy verif; followed rules + item8/7/25/26 F2/F3 + precedents). Confirmed/spot in F5; min to pass "presente in main with paths".
4. **Pattern of tests for pure helpers + delegate port:** Execute real puros in pure tests with MagicMock post-assign .name= / .value= (preferred for "pure" semantics + simplicity; like item7 real get_reward_emoji via .reward_type attrs + item8/26); for builders: pure unit tests (import inside, MagicMock minimal or simple objs; no service mocks for the helpers themselves; no @patch on the helper in handler tests (handler test covers full flow via real)). For port of wizard selects: mock_mission_svc.get_all_rewards_for_mission_wizard.return_value = [mock with .id/.name/.reward_type]; .get_reward_for_mission_wizard.return_value = ... or None; assert on mission mock call (not reward_svc); keep exact data/text/state asserts + "No hay recompensas" / "Resumen de la mision:" / "Una vez" / "Ninguna" / "Sin descripcion" / "Recurrente" / "Paso 6 de 6" etc. Followed item8/26/25/7 F4/F5 + impact port instructions exactly. 11 tests >5 min.
5. **Chequeo of delegate / rewards in wizard:** Confirmed in F3: select_frequency uses `with get_service(MissionService) as ...: rewards = mission_service.get_all_rewards_for_mission_wizard()` (via delegate) ; if empty "No hay recompensas configuradas..." + create_reward cb (back to admin_missions) + clear; else build buttons via pure + set selecting_reward. select_reward_for_mission similar for get_reward_for... + build confirm via pure. Tests F4 cover empty + happy + "Ninguna" + "Sin descripcion".
6. **Strict <=50 LOC count:** Used `inspect.getsourcelines(func)[0]` (counts def inclusive) per PLAN + item8/7/25/26 F3/F5 + item2 F3; post-extract max 50 (select_frequency), 49 (type kb pure), 42 (detail pure), 17 (process_desc post), etc (no trim needed; boilerplate did not push over). Verified in F1/F3/F5.
7. **Update of docstrings of mission admin tests:** Confirmed/refresh (2 select classes updated to "ported to 1-service (MissionService) only + delegate for reward wizard steps. Arch-enforcer note addressed. Precedent from item7/8."; other classes already had get_service ports + "ported..." from prior; no residual 2-svc language or "closes_both" left; historical NOTE if present is commented as pre-Item9 / direct RewardService in wizard). Left brief historical if desired (not needed; already clean per grep).
8. **Log file for GSD of Item 9:** `.planning/quick/gsd-mission-admin-long-funcs.log` (cross-ref gsd-impact-analyzer-item9-mission-admin-long-funcs.log); every pre-edit/pre-gate/pre-verif/pre-ruff/pre-pytest/pre-grep/pre-smoke/pre-final-self-check/pre-SUMMARY did `echo "=== $(date -Iseconds) | PHASE N | GSD pre-... - <desc + refs DoD + patrones copiados al pie de la letra>" >> <logfile>` (or run_terminal with echo/printf). At end log has 79+ entries + self-check PASSED + BATCH/POOL note. wc tracked (79+).
9. **If additional helpers needed for render:** Not needed beyond the 10+ (wizard steps + confirm + reward buttons + list entry + detail (dedupe) + delete + stats + freq + reward + type kb); they dropped all long funcs from >50 to <=50; tight scope prioritizes minimum. Documented (no more extracted).
10. **No export of pure/delegate in services/__init__.py:** Confirmed by scope (import direct from module sufficient and used in codebase + item7/25/26; no edit to __init__).
11. **Use of model props vs pure:** Confirmed use of mission.reward (rel) + .is_active / .name / .mission_type.value / .frequency.value etc on mocks (for real pure/model in _builds) + puros for display formatting specific to admin UI (wizard/list/detail/confirm/stats); visual + smoke + tests pin; out of scope to consolidate further per impact "low" risk note.
12. **Any decision differing:** None; all followed precedents conservatively (item8/26 long funcs + delegate/pure + port desc 5 + Test*PureHelpers 9 import-inside + LOC inspect + self-check + BATCH/POOL, item7/25 1svc Mission + rel + ports + pure + helper tests + 5+ import-inside, item2/5/6 pure + delegate comment + 1-line + port desc 5 tests + Test*PureHelpers, impact9 exact code blocks for delegates/pures + port ~12 wizard tests instructions + "first of new pool", 24/25/26 BATCH/POOL language, get_service context + __enter__/__exit__ mocks, real pure via attrs or direct or post-assign, docstrings "ported...", 1-line/min support + delegate comment exact, inspect LOC, UI 1:1 strings pinned, commands exact -p no:cov..., 3 crit in mind, scope tight 0/0/0/0). Registered in GSD first relevant + self-check (none differed).

Any decision differing from above registered in GSD entry of the phase + note in self-check.

**Re-verified in this gsd-executor session (post-planner log narrative, 2026-06-08 ~06:3xZ):** All F5 re-runs/gates re-executed with GSD pre every (ruff pre-exist 1-2 hygiene E402 in test only - documented non-reg per PLAN/26/25 precedents "do not count as regression"; pytest handler 55/55 green; cross 179p +1 pre xfail +15 warns pre-exist unrelated documented; reward gold 4p; bot smoke "imports + routers ok" + delegates True; LOC inspect 24 targets all <=50 (list: 20/25/17/36/47/50/21/49/33/19/8/41/37/30 + 10 puros 14/28/16/11/42/20/20/5/7/49); greps 0 "RewardService" active in handler + 9 "with get_service(MissionService)" + 2 delegates defs + 10+ puros defs + TestMissionAdminPureHelpers 11 tests; smoke pure calls + delegates exist + handlers import/call; select_frequency/select_reward_for_mission use exactly `with get_service(MissionService) as mission_service: ... = mission_service.get_all_rewards_for_mission_wizard()` / `get_reward_for_mission_wizard(reward_id)` (delegates, 1 svc only); UI 1:1 (strings/emojis/cbs "➕ Crear mision"/"Paso X de 6"/"Resumen de la mision:"/"Sin descripcion"/"Ninguna"/"Sin recompensa"/"Una vez"/"Recurrente"/"No hay misiones registradas."/"No hay recompensas configuradas..."/"Estas seguro de eliminar"/"✅ Mision eliminada correctamente."/"Mision creada exitosamente"/backs "admin_missions"/"list_missions", trunc, cb packs, freq/status, wizard FSM, empty/error cases, Lucien all preserved 1:1 in puros + handlers); logging "mission_admin_handlers | list_missions | ... | count=..." + "mission_admin_handlers | confirm_create_mission | ... | mission_id=... | name=..." present in key withs; arch comments + delegate/pure docstrings exact in svc; Test*PureHelpers 11 cover steps/confirm/kbs/list/detail/delete/stats/freq/edges with exact pins. Brief reports for F1-F5. Self-check re-PASSED. Scope tight (only 3 files + log + SUMMARY). 0 behavior/0 creep/0 other/0 prod chg/0 delivery/gamif credit/claim/atomicity impact. 3 crit protected (re-runs). 

Pool anterior de 4 cerrado (tests passing per user). Nuevo pool de 4 iniciado. Quedan ~2-4 clusters del análisis inicial después de este pool.

Item 9/27 closed. First of new pool of 4. Previous pool closed with tests passing per user. Ready for arch-enforcer re-scan (enfocado en mission_admin_handlers: exactly 1 service + <=50L + no direct RewardService + puros + ports + UI1:1 + logging + comments) + test-guardian (correr los tests críticos listados) + gsd-executor del siguiente item del pool de 4.

## GSD Discipline (non-negotiable, followed al pie)
- Pre *every* modification (search_replace/write ~15+ on sources/tests/log/SUMMARY), gate (ruff x12+, pytest x10+ targeted/broader/combined, grep x10+ rules/1svc/helpers/0-Reward, smoke x6+, verif, self-check, summary), verif: run_terminal append "=== $(date -Iseconds) | PHASE N | GSD pre-... - <desc + refs DoD + patrones copiados al pie de la letra>" >> .planning/quick/gsd-mission-admin-long-funcs.log ; wc -l after.
- 79+ entries total (planner INIT/DISCOVERY/PLANNING/pre-write/self-check ~5 + executor 70+; pre-F1, pre each edit (F2 1+importfix, F3 12+, F4 4+fixes, F5 logging), pre each gate/ruff/pytest/grep/smoke/self-check/SUMMARY, phase completes, final self-check; many sub per complex F5 re-runs).
- Style copied from precedents (gsd-store-admin-long-funcs.log / gsd-reward-handlers-1service-loc.log / gsd-reward-gamif-item2.log / gsd-remaining-besito-compositions.log / gsd-reward-besito-eventbus.log / gsd-getservice-unification.log / gsd-critical-tests.log): detailed, refs DoD, patrones copiados (item8/26 long funcs + delegate/pure + port desc 5 + TestStoreAdminPureHelpers 9 import-inside + "ported to 1-service (StoreService) + delegate... Arch-enforcer note addressed" + __enter__/__exit__ + mock_store.get_available...= + assert on store (not pkg) + get_service patch target; 25/24 BATCH "4 items completed in this tirón (Item 6 final of max 4)" + "Item 6/24 closed. BATCH..." + self-check PASSED; item7/25 ports + pure + delegate 1-line + "Backward-compatible... Item X" + port desc 5 tests exact; impact9 scope/risks/tests/"first of new pool" + exact delegate/pure blocks + "Pool anterior de 4 cerrado..."; 26 "second of new pool"; mission 1svc+with get_service+__enter__/__exit__; get_service context + __enter__/__exit__ mocks; real pure via attrs or direct; docstrings "ported...", 1-line/min support + delegate comment; inspect LOC; UI 1:1 strings pinned; commands exact -p no:cov...; GSD detailed pre- with DoD+patrones; safe points + self-check full + critical + pool note; BATCH/POOL language).
- Log exists with planner INIT/pre-write/post-write/self-check (5+1) + executor pre-F1 + all phases + pre every + self-check PASSED + BATCH/POOL note (wc 79+).
- No edits (even to PLAN or log beyond appends) without pre-log.
- 0 behavior/0 UI/0 contract change enforced by pure 1:1 extracts + test asserts pinning exact strings/emojis/cbs/math + cross re-runs protecting indirectly.
- 0 atomicity/claim/delivery impact (0 change to increment_and_deliver / deliver_reward / backpack / credit paths / atomicity contracts; this flow admin mission/reward config only (read+admin-mutate); stats aggregate read; re-runs of cross/gamif + reward gold protect indirectly).

## Scope (tight, 0/0/0/0 verified at every gate + final grep/self-check)
- Only files listed in PLAN "Archivos que se modificarán" + the log GSD + this PLAN + opt SUMMARY (produced).
- 0 new files (except SUMMARY opt at end; produced via Write).
- 0 prod behavior change (all text construction, emoji choice (if any), button labels/texts (exact "➕ Crear mision", "📋 Ver misiones", "🎁 Crear recompensa", "📋 Ver recompensas", "📊 Estadisticas", "🔙 Volver", "✅ Crear", "❌ Cancelar", "No hay...", "Resumen de la mision:", "Paso X de 6", "Una vez"/"Recurrente", "Sin descripcion"/"Ninguna"/"Sin recompensa"), cb packing, logging format, empty/error cases, wizard FSM steps/transitions (MissionWizardStates), select flow (freq->reward list->confirm), list/detail/stats formatting, reward_text/freq_text calcs, delete confirm, create args are in puros (mechanical 1:1 move of existing inline) or the entry handlers or svc (unchanged CRUD/create). Delegates are transparent passthroughs. Extraction preserves every string/emoji/branch/cb/data exactly. Reward creation/delivery untouched (orthogonal). User mission progress/claim uses services directly, unaffected by admin config changes. Handlers call exactly 1 service (MissionService); funciones <=50 LOC post-extract; logging en formato estándar "mission_admin_handlers | <action> | user_id=... | resultado=..." para acciones importantes; get_service context manager; sin lógica de negocio en handlers; sin acceso DB fuera de models; pure helpers (no side effects, importable, fácil unit test, verb+context+result naming). 3 sistemas críticos protegidos (read+admin-mutate mission/reward config only; 0 side effects en gamif credit / narrative / VIP-channel; stats read-only aggregates; re-runs protect).
- 0 atomicity/claim/delivery impact (0 change to increment_and_deliver / deliver_reward / backpack / credit paths / atomicity contracts; this flow admin mission/reward config only (read+admin-mutate); stats aggregate read; re-runs of cross/gamif + reward gold protect indirectly).
- 0 other handlers (reward_admin_handlers.py even if related menu/"list_rewards"/"create_reward" + backs to admin_missions or its own RewardWizardStates/reward create; gamification_admin_handlers.py (entry button only); mission_user_handlers.py (user flows + shared cbs); store/promotion/story/etc admins; common; broadcast; free_channel etc — even if they touch missions/rewards; explicit out).
- 0 reward_service.py (no changes; delegates call its get_all_rewards(active_only=True)/get_reward; its pure get_reward_emoji untouched).
- 0 models/ (no new props/methods; use existing Mission + relationship "reward" + Reward for wizard), 0 keyboards/* (no new builders or cb changes; all built inline or via puros), 0 bot.py (router include at 294 stays; smoke import test sufficient), 0 handlers/__init__.py, 0 services/__init__.py, 0 utils, 0 lucien_voice, 0 config, 0 middlewares.
- 0 changes to core MissionService: create_mission, get_*/get_all_missions, get_available_*, progress/increment/increment_and_deliver (incl internal RewardService(db) for user rewards + deliver), update/delete/get_stats, set_progress, etc. 0 to RewardService create/deliver/log/get_reward_stats/held pkg/vip (post Item5).
- 0 change to user mission claim/delivery (increment_and_deliver, deliver_reward, backpack) or atomicity golds.
- 0 new tests outside the mission_admin test file (no service tests for new delegates/pures).
- 0 edición de CLAUDEs (incl services/missions/CLAUDE.md + handlers/CLAUDE.md), decisions.md, AGENTS, ROADMAP, fase_*, docs/, refactor_testing.md, o cualquier .md excepto this PLAN + the log GSD (impact report + MEMORY already done by analyzer).
- 0 broad "fix all mission wizards" or "touch reward_admin for parity" or "refactor reward_service" or "touch mission_user".
- 0 behavior or contract changes (0 impact on mission CRUD values, reward assignment in wizard, wizard FSM transitions, UI strings/emojis/buttons/cbs, alerts, empty/error cases; delegate transparent passthrough; extracts pure 1:1 move of prior inline).
- 3 critical systems protected (gamif credit/debit/missions/rewards delivery/claim/increment, narrative progress/archetypes/achievements, channel/VIP subs/pending/approve/auto-approve; this flow admin mission/reward config only (read+admin-mutate); stats are aggregate read; re-runs of cross/gamif golds + reward get_* gold protect indirectly; "admin create is orthogonal to user progress/claim" per user prompt).
- 0 prod chg.

## Key Patterns Copied (al pie de la letra, per PLAN "Copia patrones **al pie de la letra** de golds")
- Patrón get_service + with + mock en tests + closes __exit__: copied from `tests/handlers/test_mission_admin_handlers.py` itself (most classes) + item8/26 + item7/25 F4 port (get_service patch, mock_instance + __enter__, mock_mission_svc setups for delegate get_all..._for... / get_reward..._for... = , closes to __exit__, calls asserts on mission only, docstrings "ported to 1-service... Arch-enforcer note addressed", NOTES cleaned) + mission_user_handlers.py + its test.
- Extracción de helper puro para LOC + UI idéntica: copied spirit + snippets from F3 of item8/26 (insert pure compute_.../build_* near section; replace inline with call; docstring "Función pura (sin estado ni side-effects). Soporte para UI de admin missions (wizard/list/detail). 1:1 de lógica previamente inline (item9, arch-enforcer). Precedent item7/8."; inspect LOC post; test refresh path green; 5-8+ helpers per long flow if sufficient; trim docstring if 51 by boilerplate + comment "extracted for <=50 LOC rule (Item 9 / arch-enforcer)").
- Logging: "módulo | acción | user_id=... | resultado=..." (added/ensured in list/confirm per item8/7/25/26 F2/F3 + rules; inside withs post-success).
- 1-line / min support + delegate comment: from item8/26/7/25 F1/F2 + item2 F1 (pura + delegate 1-line + "Backward-compatible delegate added for Item X (arch-enforcer...)"; thin delegate for cross-in-domain with "Added for item9: enables ... exactly 1 service (MissionService)..." exact per impact/PLAN).
- GSD entries detalladas: "pre-xxx <file> (F<N> <motivo>) - <desc + refs DoD + patrones copiados al pie de la letra de item8/26 PLAN F4 + gsd-store-admin-long-funcs.log + item9 impact report (exact delegate/pure blocks + port ~12 wizard tests instructions + long funcs list) + 25/26 SUMMARY BATCH + current source lines>"; wc; style of item8 (800+), item7 (40+), item2 (46+), 24 (55+).
- Safe points + self-check at end of log: structure of item8/26 (lista fases/DoD/gates/archivos/tests que pasaron/reglas verificadas (GSD pre every, scope tight 3 files + log + 0/0/0/0, 1svc Mission + delegates for reward wizard, LOC<=50 via inspect, logging, pure helpers tests 11 import-inside, no prod chg)/desviaciones/tests críticos/"Item closed. Ready for ... + arch-enforcer + test-guardian + siguiente item del pool") + 25/24/26 BATCH/POOL note + explicit "first of new pool of 4" + "Pool anterior de 4 cerrado (tests passing per user). Nuevo pool de 4 iniciado. Quedan ~2-4 clusters del análisis inicial después de este pool."
- Precedentes PLAN/GSD + handoff + pool/batch: .planning/phases/26-store-admin-long-funcs/PLAN.md + 26-*-SUMMARY.md + gsd-store...log (Item8 second-of-new-pool long funcs + delegate + puros + ports + Test*PureHelpers + LOC + self-check), 25-reward-handlers-1service-loc/PLAN.md + SUMMARY + gsd-reward...log (Item7 first-of-new-pool 1svc + ports + pure + helper tests + self-check), 20-reward-gamif PLAN + gsd-reward-gamif-item2.log (Item2 reward 1svc gold + delegate + pure + ports + helper tests + LOC + self-check), 23/24 PLANs + SUMMARYs (BATCH "4 items completed in this tirón (Item 6 final of max 4)", "Item 6/24 closed. BATCH...", self-check), item9 impact report .md (source of truth for scope/map/risks/tests/"first of new pool of 4" + exact code blocks for delegates/pures + port ~12 wizard tests instructions + "Pool anterior de 4 cerrado...").
- VOZ/estilo: handlers speak via existing texts (Lucien voice preserved identical); no change to user messages.
- 3 sistemas críticos: always in mind (gamif/missions/rewards as domain in mind of hardening; narrative cross via events; channel/VIP; this item admin mission/reward config only (read+admin-mutate); 0 tx/credit/deliver/claim; re-runs of cross/gamif + reward gold protect).
- Commands: exact from PLAN sec5 + "Instrucciones" ( -p no:cov --override-ini="addopts=", ./venv/bin/python -m for ruff/pytest, python -c for smokes with venv fallback per 25 precedent, greps for rules/1svc/0-RewardService, python -c for LOC inspect, bot smoke or equiv, combined critical re-run in F5, reward get_all/get_reward gold).
- Test class for pure helpers: exact pattern from item8/26/7/25 F5 (class Test*PureHelpers with 11 tests >5; import inside test funcs per file convention; no service mocks for pure; placed after last class).
- Port of wizard select ~12 tests: exact per item9 impact "change from @patch("handlers.mission_admin_handlers.RewardService") + mock_reward_svc.return_value.get_all... / .get_reward = ... to @patch("handlers.mission_admin_handlers.get_service") + setup mock_mission_svc.get_all_rewards_for_mission_wizard.return_value = [...] ; mock_mission_svc.get_reward_for_mission_wizard.return_value = ...; adjust late import/await calls; assert on mission mock call (not reward_svc); keep exact data/text/state asserts + 'No hay recompensas' / 'Resumen' / 'Una vez' / 'Ninguna' / 'Sin descripcion' / 'Recurrente' etc. Use mock_context.__enter__.return_value = mock_mission_svc pattern".
- 0 export pure/delegate in __init__: confirmed (import direct sufficient + used).
- Any differing: none; registered in GSD + self-check (none).

## Tests / Gates Summary (all passed with 0 attributable reg; pre-exist doc non-reg)
- See self-check in log (above) for per-phase numbers + full re-runs.
- Final: 55 (handler file post + new helpers 11), 179 (broader cross -k with 1 xfail pre + 1064 deselected pre +15 warns pre; 0 our reg), reward gold 4p, bot smoke PASS, ruff limpio on 3 (hygiene format 0 logic; 1-2 pre-exist E402 in test), greps/inspect all criteria (0 RewardService handler, 9 1svc withs, 2 defs svc, 10+ puros defs, LOCs<=50, logs present in list/confirm, pure helpers tests 11 import-inside).
- Gold protection: handler test full (menu, create wizard 6 steps + skip + no rewards + confirm + states, list empty/with, detail not-found/toggle/delete, stats, delete confirm/success, reward wizard select freq/reward empty/happy/advances/"No hay..."/"Resumen"/"Una vez"/"Recurrente"/"Sin descripcion"/"Ninguna", state clears, cb targets); pure helpers (wizard steps Paso1-6 + Lucien + example, confirm full/wo desc/reward "Resumen..."/"Sin descripcion"/"Ninguna", reward buttons len/rows/texts/cb ids/back targets, list entry status+name[:30], detail text/kb 3rows + rel/None "Sin recompensa", delete kb 2btns + confirmed + back, stats counts + active buttons + back, freq/reward labels, edges/empty); cross re-runs protect mission/reward flows + atomicity indirectly; reward gold exercises real get_all/get_reward that delegates passthroughs to.
- 1-line/min support + delegate: confirmed (delegates at 224/233 in svc, docstrings + arch comments exact).
- Gates: always `-p no:cov --override-ini="addopts="` for clean exit (precedent all golds 20/21/23/24/25/26 + item logs); targeted -k first; broader at end; ruff pre/post; GSD pre each.
- Logging requirement coverage: tests do not assert logs usually (except middleware); gate is manual grep/inspection during edits + inclusion in GSD log (logs present in standard format at list/confirm withs).
- Pre-exist in broader: 1 xfail (pre), 15 warnings (pre: MovedIn20, runtime mocks, deprec, emit not awaited, etc), deselected many (pre); tolerated + doc per PLAN + precedent. Documented; 0 counted as regression of this Item.
- 0 attributable to this Item: all pre-exist (ruff hygiene E402 in test per PLAN explicit stay, bottom import per file conv, MovedIn20, other warns from priors, 1 xfail in reward_user tests).

## Handoff / Next
**Item 9/27 closed. First of new pool of 4. Previous pool closed with tests passing per user. Ready for arch-enforcer re-scan (enfocado en mission_admin_handlers: exactly 1 service + <=50L + no direct RewardService + puros + ports + UI1:1 + logging + comments) + test-guardian (correr los tests críticos listados en self-check arriba) + gsd-executor del siguiente item del pool de 4 (long reward_admin or other clusters per roadmap).**

(User opt) future tirón (but do not initiate without new prompt per user instr "propose if user quiere más en futuro (pero no inicias sin nuevo prompt)").

**Pool confirmation in output final:** "Pool anterior de 4 cerrado (tests passing per user). Nuevo pool de 4 iniciado. Quedan ~2-4 clusters del análisis inicial después de este pool."

(Ver .planning/phases/27-mission-admin-long-funcs/PLAN.md + gsd-mission-admin-long-funcs.log (full GSD 79+ entries + self-check PASSED + POOL/BATCH confirm + critical tests) + this SUMMARY + (if) commits for execution details + handoff.)

**Hecho con disciplina total como en ejecuciones previas exitosas (mw-hardening, eventbus, reward-gamif, getservice, critical-tests, reward-besito, remaining-besito, reward-handlers-1service-loc, store-admin-long-funcs). Scope tight, GSD pre every, patterns copied al pie de la letra, 0 behavior/0 delivery/0 atomicity, 0 scope creep. Nuevo pool de 4 iniciado (1/4: mission admin 1svc+LOC; remaining ~2-4 clusters after this pool).**

(Hecho con 💋 para Diana (Señorita Kinky) — gsd-executor subagent; first of new pool of 4, post-unification Besito + item7/8 hardening tirón.)

## Self-Check: PASSED

All criteria met. Item 9/27 closed as first of new pool of 4. Pool anterior de 4 cerrado (tests passing per user). Nuevo pool de 4 iniciado. Quedan ~2-4 clusters del análisis inicial después de este pool.
## Self-Check: PASSED
