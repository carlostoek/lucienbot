# PLAN: Implement lightweight observability/health system (core checks for DB/bot/channels/bus/scheduler/critical services + overall; simple /health JSON endpoint for platform/monitoring; admin-friendly bot command/menu "🛡️ Pulso del reino" with Lucien voice + terminal script `python -m scripts.health_check` [--json][--verbose]; reuse AnalyticsService read-only + get_service + is_admin + Channel/VIP/Scheduler/EventBus public APIs + structured logging + Lucien voice; 0 impact on 3 critical systems or atomicity/contracts) (Item 11 / third of new pool of 4, automatic continuation after Item 10/28 closed full + "suite protege adecuadamente" + tests green + documentador updated HARDENING_ROADMAP for current new pool state)

**Type:** gsd-planner output (for gsd-executor)  
**Date:** 2026-06-09  
**Focus:** Tight, conservative, phased implementation of lightweight observability/health per impact-analyzer recs + precedents (AnalyticsService read-only pattern for HealthService: __init__(db=None), _owns_session, _get_db, close, direct model counts for speed, no mutation; analytics_handlers.py for Custodios-only Command + is_admin + with get_service(XXX) as svc: data=svc.xxx() + LucienVoice render; 1-svc/handler strict; bot.py on_startup central wiring precedent for listeners/scheduler + optional non-blocking task; ChannelService/VIPService public getters + counts for read-only status; SchedulerService + get_scheduler() + internal jobs for inspection (thin public or system-domain access with comment); EventBus get_event_bus() for listener counts (best-effort, "MUST NOT mutate"); structured logging "módulo | acción | user_id | resultado" for every important health action (user_id=0 for terminal/startup, admin_id for bot cmds); LucienVoice 3rd-person elegant with "Diana"/"custodios"/"visitantes" + emojis ✅⚠️❌ + sections mirroring analytics_dashboard; GSD pre every + wc; 5-6 small phases strict order like 28/27/26/25/24/23; self-check PASSED full structure + verbatim pool phrase + handoff to arch-enforcer (enfocado en health checks + endpoint + admin views + no impact on 3 crit) + test-guardian (correr tests críticos) + documentador (for final ROADMAP update) + gsd-executor siguiente del pool; 0 behavior/0 atomicity/0 prod change; 0 risk to gamif credits/reactions/daily/missions, narrative progress/archetypes/FSM/quiz, channel pending/approve/expire/bans/subs/VIP grant/revoke; all checks read-only/best-effort/timeout-protected/non-blocking. "Item 11/29 third of new pool of 4; previous Item 10/28 second closed with tests passing per user + self-check PASSED + BATCH/POOL note; Pool anterior de 4 cerrado (tests passing per user). Nuevo pool de 4 iniciado. Quedan ~2-4 clusters del análisis inicial después de este pool."

**Input principal (source of truth):** 
- The just-completed impact-analyzer report: `.claude/agent-memory/impact-analyzer/item11-observability-health.md` (full read first via tools + GSD pre-log + wc before any other action; exec summary + risks LOW + complete map + 3 systems explicit 0-risk checks + tight scope recs + exact design notes for HealthService checks (check_db_connectivity, check_bot_runtime, check_channels, check_event_bus, check_scheduler_jobs, check_critical_services_sanity, overall_status), friendly admin views (bot command/menu with Lucien voice + terminal script), simple /health endpoint (aiohttp or minimal), wiring in bot.py, tests, logging, GSD, documentador at end for ROADMAP update; pool context verbatim "Pool anterior de 4 cerrado (tests passing per user). Nuevo pool de 4 iniciado. Quedan ~2-4 clusters del análisis inicial después de este pool."; "Ready for gsd-planner (observability/health item, next after Item 10 in current flow)"; "GSD pre: Initiated via run_terminal_command creating/appending to .planning/quick/gsd-observability-health.log (cross-ref the impact gsd log)"; "Analysis only (no impl; ready for gsd-planner with tight 5-6 phase PLAN copy 28/27/26/25/24/23 precedents + atomicity golds + get_service + is_admin + Analytics read-only + scheduler/channel precedents + Lucien voice + 1-svc rule + self-check structure + pool phrase + handoff + documentador for docs"; DoD (for downstream): HealthService implements listed checks (read-only/best-effort/timeouts), logs in format, overall + render data; bot admin view (cmd or menu cb) + terminal script functional, use get_service + is_admin + LucienVoice, exactly 1 svc; endpoint (if in scope) serves JSON on /health, non-blocking, no breakage to polling; 0 changes to critical flows/services (grep confirm 0 new writes/mutations in gamif/narrative/channel paths); tests: new unit green; targeted -k re-runs of golds + scheduler + free_entry + event_bus + vip/story all green with 0 regressions; ruff clean; smokes pass; docs: decisions entry, services/CLAUDE table+notes, (documentador handles ROADMAP); Grep/LOC/verif in final GSD; handoff "Item 11/29 closed. Third of new pool of 4. ... Ready for arch-enforcer re-scan (enfocado en health_service + get_service usage + admin views + 3 critical systems read-only) + test-guardian (correr los tests críticos listados) + documentador (for final ROADMAP update) + gsd-executor del siguiente item del pool de 4".
- Precedents al pie de la letra: `.planning/phases/28-remaining-besito-store/PLAN.md` + `28-remaining-besito-store-SUMMARY.md` + `.planning/quick/gsd-remaining-besito-store.log` (Item 10/28 second of new pool of 4; 5 phases F1-F5 strict; GSD pre every with detailed style + wc -l tracking; "Copia patrones **al pie de la letra**" + "Instrucciones para gsd-executor" sections; self-check PASSED full structure with phases/DoD/gates/archivos/tests passed/reglas verificadas (GSD pre every, scope tight X files + log + 0/0/0/0 behavior chg in critical systems)/desviaciones (pre only, doc non-reg)/tests críticos para futuro (exact list)/"Item 10/28 closed. Second of new pool of 4. Pool anterior de 4 cerrado (tests passing per user). Nuevo pool de 4 iniciado. Quedan ~2-4 clusters... Previous batch of 4 (Item 7/25 + Item 6/24 + Item9/27 as first of this pool) closed with tests passing per user. Ready for arch-enforcer re-scan (...) + test-guardian (...) + gsd-executor del siguiente item del pool de 4 + documentador if pool close"; copy locals/observer/1-line/guard + daily precedent + atomicity gold with patch schedule_emit + DESIRED + TestSession + strict + "credit survives deliver False" + "post-credit best effort (misiones + listeners)" + N806 tol w/doc + 777 + gather + try/finally; bot reg + comment; targeted docs appends; BATCH/POOL language verbatim; handoff exact); `.planning/phases/27-mission-admin-long-funcs/PLAN.md` + `27-mission-admin-long-funcs-SUMMARY.md` + `.planning/quick/gsd-mission-admin-long-funcs.log` (Item 9/27 first of new pool; 6-step full pipeline; 1-service via get_service(MissionService) + thin delegates in svc for cross; pure helpers extract verb+context+result + "Función pura (sin estado ni side-effects)"; ports of tests with get_service mocks + __enter__/__exit__ + docstrings "ported to 1-service... Precedent item7/8"; add Test*PureHelpers with 5-10+ import-inside pure unit tests (no @patch on puros); UI 1:1 exact strings/emojis/cbs/texts; LOC <=50 via getsourcelines/inspect; logging standard; ruff (pre E402 etc doc non-reg); self-check PASSED + "Item 9/27 closed. First of new pool of 4. ... Ready for arch-enforcer re-scan (enfocado en mission_admin_handlers: exactly 1 service + <=50L + no direct RewardService + puros + ports + UI1:1 + logging) + test-guardian (...) + gsd-executor del siguiente..."; GSD 79+ executor + pool/BATCH); `.planning/phases/26-store-admin-long-funcs/PLAN.md` + SUMMARY + gsd (Item 8/26 second of new pool; same 1-svc + puros + ports + UI1:1 + LOC + logging + self-check + "second of new pool of 4" language); `.planning/phases/25-reward-handlers-1service-loc/PLAN.md` + SUMMARY + gsd (Item 7/25 first of new pool; "Item 7/25 closed. First of new pool of 4. Previous batch of 4 (ending with Item 6/24 ...) closed with tests passing per 24-SUMMARY BATCH note + self-check PASSED. Ready for arch-enforcer... + test-guardian... + gsd-executor del siguiente item del pool de 4."; pool phrase); `.planning/phases/24-remaining-besito-compositions/PLAN.md` + 24-*-SUMMARY.md + gsd (Item 6; BATCH "4 items completed in this tirón (Item 6 final of max 4)"; pool phrase; 5 phases; locals in credit sites + 1-2 obs + "MUST NOT" + DESIRED + central reg 4 total + 1-line guards in cross/daily + property kept for daily; golds re-runs); `.planning/phases/23-reward-besito-eventbus-decoupling/PLAN.md` + SUMMARY + gsd (Item 5; held→local inside _deliver + obs listener + central reg + 1-line test + DESIRED; 5 phases; copy story listener block al pie; atomicity gold re-runs with patch+DESIRED+TestSession+strict+"credit survives"; self-check + handoff); `.planning/HARDENING_ROADMAP.md` (recent sections on observability spike, structured logging, /health (DB/bot/channels/bus/scheduler), pool context post Item 10/28 + documentador update; "Pool anterior de 4 cerrado (tests passing per user). Nuevo pool de 4 iniciado. Quedan ~2-4 clusters del análisis inicial después de este pool."; "Item 11/29 ... next after Item 10"); gold tests `tests/integration/test_cross_service_atomicity.py` (full happy/partials with patch schedule_emit + DESIRED CONTRACT + TestSession/file + 777 TG + gather + N806 tol w/doc + "credit survives deliver False" + "post-credit best effort (misiones + listeners)" + strict tx/deltas + daily guards "if hasattr... else BesitoService(db=...)" at 726/762 + class patch precedent); `tests/integration/test_free_entry_flow.py` (scheduler/pending/channel jobs); `tests/unit/test_scheduler.py`, `test_event_bus.py`, `test_analytics_service.py` (adapt pattern for health), `test_channel_service.py`, `test_vip_service.py`; handlers tests admin/analytics; current sources (bot.py on_startup 187+ + listeners reg block 203-212 + "Cross-domain listeners (explicit, central...)" comment + Item 10 store + imports 69-77; services/analytics_service.py full read-only _get_db/owns/close/direct counts precedent + get_dashboard_stats; services/scheduler_service.py jobs (approve_join 30s, expiry_reminders cron, expire_subscriptions cron 0/6/12/18, daily_backup 3:00, cleanup_streak 60m + ad-hoc free_welcome/streak) + get_scheduler singleton + _scheduler; services/event_bus.py register/emit gather return_exceptions + logs "event_bus | ..." + get_event_bus singleton + _listeners; services/channel_service.py count_pending_requests, get_ready_to_approve, get_free_channels, get_vip_channels, get_all_pending; services/vip_service.py get_active_subscriptions, get_expiring_subscriptions, get_expired_subscriptions, is_user_vip; services/backup_service.py; models/database.py (SessionLocal, engine pool_pre_ping, no get_db_session exported but usable); handlers/analytics_handlers.py (Command /stats /export + is_admin + with get_service(AnalyticsService) as svc: + LucienVoice.analytics_dashboard(stats) + analytics_access_denied + error_message + exactly 1 svc per entrypoint + logger); handlers/admin_handlers.py (is_admin filters on cbs + admin menu routing); keyboards/inline_keyboards.py admin_menu_keyboard() (list of buttons incl admin_analytics at 126; add 1 for health); utils/admin.py is_admin (user_id in bot_config.ADMIN_IDS); utils/lucien_voice.py (analytics_dashboard 451+ style: 🎩 <b>..</b> <i>..</i> emojis sections "Diana" "custodios" "visitantes"; analytics_access_denied 486+; error_message; admin_greeting; add system_health(health:dict), health_access_denied, health_check_section); config/settings.py (ADMIN_IDS from env CSV, DATABASE_URL, TIMEZONE); railway.toml (healthcheck commented; startCommand alembic+bot); Procfile (worker only); services/CLAUDE.md (services table + get_service + EventBus + cross notes; add HealthService row under System/Observability); handlers/CLAUDE.md ("UN service por handler" strict 1 svc call); decisions.md (recent Item 10/28 entry style: Motivo/Riesgos/Decisión/Resultado + refs + pool "third of new pool of 4" + handoff; append after Item 10); models/CLAUDE.md (Channel id/PK duality for channel health checks (use get_by_id vs by_db_id); no raw SQL; alembic rules (0 impact)); root CLAUDE.md + AGENTS.md + architecture.md + rules.md (GSD pre every modify, handlers exactly 1 service no logic/DB, services own logic, <50 LOC funcs, verb+context+result naming, mandatory "módulo | acción | user_id | resultado" logging for important actions, is_admin for custodios, read-only/best-effort for health MUST NOT affect 3 crit, get_service context, atomicity/EventBus contracts (health observational only)); tests (golds + scheduler + free_entry + event_bus + admin/analytics handlers + analytics_service unit); scripts/ (verify_env.py + others for terminal script pattern `python -m scripts.xxx`); requirements.txt (0 web deps currently; aiohttp for endpoint optional/risky — scope can defer or flag); no current /health impl (grep 0 active; only commented railway + old TG cmd example in DEPLOY.md + ref in decisions).
- Project rules (non-negotiable): CLAUDE.md (3 critical systems always protected: Gamification (besitos/missions/daily/reactions/minigames/streaks/trivia — health pure reads of balances/tx vol/sanity, 0 risk to credit_besitos/debit paths, REACTION tx + mission best-effort + event emit, daily claim atomic, game payouts, streaks; "0 risk to gamif credits, reaction_mission_flow, daily atomic, partial failure contracts"); Narrative (story nodes/archetypes/achievements/FSM/quiz/progress/inverse besitos on logros — health counts UserStoryProgress etc read-only, 0 risk to advance_to_node/calculate_archetype/_grant_achievement (besitos credit path protected by story listener "MUST NOT credit" + atomicity); Channel/VIP (subs/pending/auto-approve/expire/ban propagation/free entry ritual/VIP tokens/tariffs — health uses ChannelService/VIPService publics + reads on Subscription/Pending/Channel, 0 risk to create_pending/approve/_process_pending_requests/expire_subscriptions cron (ban/unban + clear vip_entry)/startup expired/free welcome jobs; "0 risk to channel approvals/pending/expirations, VIP grant/revoke, ban prop, atomic subs"); handlers exactly 1 service call (no logic/DB); services own logic; logging mandatory for important actions; <50 LOC; verb+context+result; GSD pre-log before every edit/gate/verif/ruff/pytest/grep/smoke/self-check/summary; is_admin() for Custodios; get_service context manager; atomicity/EventBus/get_service contracts (health is observational only, best-effort, timeouts, never blocks main loop or tx, MUST NOT mutate); read-only/best-effort for health (MUST NOT affect gamif credits/reactions/daily/missions, narrative progress/archetypes/FSM, channel pending/approve/expire/bans/subs); 0 new models (0 alembic); EventBus for notifications (obs only, "MUST NOT credit/debit/mutate"); local db= inside methods for atomic if needed (but health uses owns or get_service); 3 systems + rules in every section; documentador agent (not manual) for docs/ROADMAP at end of tirón/pool.
- Current code (read first via tools as above + greps): 0 health mentions active (only comments + old TG example); on_startup wires init_db (noop), check_expired (VIP), scheduler.start(), 5 besitos_awarded listeners (narrative/rewards/broadcast/game/store from Item5/6/10) + log "Event listeners registrados (besitos_awarded -> narrative, rewards, broadcast, game, store)" + admin notify; scheduler singleton + jobs persist via SQLAlchemyJobStore; analytics_handlers exact 1svc+is_admin+get_service+Command+ Lucien render precedent (perfect for /health cmd or reuse); admin_menu has admin_analytics btn (add health after); Channel/VIP publics as listed (for counts/pending/active/expiring without touching approve/expire logic); EventBus _listeners for counts (best effort); database SessionLocal + pool_pre_ping (use for db check ping SELECT 1 + latency); get_service in services/__init__ (context __enter__ creates svc(db), __exit__ close if has; add HealthService to __all__); is_admin central; lucien_voice analytics style for system_health (elegant 3rd person, emojis, sections, "El pulso del reino...", "Diana", "custodios", "visitantes"; + health_access_denied + error); logging convention used in event_bus/analytics (extend to "health_service | check_* | user_id=0 | status=ok latency=.."); no web deps (aiohttp for endpoint optional/flag/ separate port to not conflict aiogram polling; or scope bot+terminal first if risk high); scripts/ patterns for `python -m scripts.health_check`; tests critical list from impact (golds cross/reaction/free_entry/vip/story/event_bus/scheduler + new health unit + admin handler updates + broader -k "health or TestHealthService or scheduler or free_entry or atomicity or event_bus or vip or story").

**GSD enforcement:** Executor MUST prefix **every** modification, gate, verification, ruff, pytest, grep, smoke, self-check, or summary step with a GSD log append (timestamp | PHASE N | GSD pre-... - <desc + refs DoD + patrones copiados al pie de la letra>) to `.planning/quick/gsd-observability-health.log` (cross-ref the impact gsd log `.planning/quick/gsd-impact-analyzer-item11-observability-health.log`). Use identical detailed style + wc -l tracking from 28/27/26/25/24/23 gsd logs (pre + post + counts, "GSD pre-edit <file> (F<N> <short motive>) - <desc + refs DoD + patrones copiados al pie de la letra>", "GSD pre-ruff F<N>: ...", "GSD pre-pytest ...", "F<N> safe point - ... DoD all marked + log.", "GSD pre self-check", full self-check structure at F6). No edits (even to PLAN or log beyond appends) without pre-log. Planner did INIT + pre-mkdir + pre-write entries (see log, 10+ lines at PLAN time; cross-ref impact analyzer's 16+ entries). "Planner did INIT + pre-write."

---

## 1. Alcance preciso (In / Out explícito)

### En esta entrega (scope "tight" per impact report + "smallest change" + precedents 28/27/26/25/24/23 + 0 behavior/0 atomicity/0 prod/0 other files beyond listed + "Item 11/29 third of new pool of 4"):

- **New service (core, read-only/best-effort like AnalyticsService):** `services/health_service.py`
  - `class HealthService:` (follow AnalyticsService al pie de la letra: `def __init__(self, db: Session = None): self.db = db or SessionLocal(); self._owns_session = db is None`; `def _get_db(self) -> Session: ...`; `def close(self): ...`; import from models.database import SessionLocal; from sqlalchemy import text; import time, logging; logger = logging.getLogger(__name__)).
  - Checks (all <=50 LOC; verb+context+result names; read-only/best-effort with short timeouts where possible (e.g. try/except around queries); no writes, no mutation, no side effects on 3 crit; log every: "health_service | <action> | user_id=0 | status=... latency=..."):
    - `def check_db_connectivity(self) -> dict:` (try: start=time.time(); with self._get_db() as s: s.execute(text("SELECT 1")).scalar(); latency=int((time.time()-start)*1000); logger.info(f"health_service | check_db_connectivity | user_id=0 | status=ok latency_ms={latency}"); return {"status": "ok", "latency_ms": latency, "pool": "pre_ping" if pg else "sqlite"} except Exception as e: logger.warning(...); return {"status": "fail", "error": str(e)[:120]})
    - `def check_bot_runtime(self) -> dict:` (use module-level _BOT_START_TIME = datetime.now(UTC) set in bot.py on_startup or import; uptime = (now - start).total_seconds(); return {"status":"ok", "uptime_seconds": int(uptime), "start_time": start.isoformat()})
    - `def check_channels_status(self) -> dict:` (with get_service(ChannelService) as ch or direct ChannelService(): free=len(ch.get_free_channels()), vip=len(ch.get_vip_channels()), pending=ch.count_pending_requests(), ready=len(ch.get_ready_to_approve()); status = "ok" if pending < 50 else "degraded" if pending < 100 else "unhealthy"; ch.close() if owns; return {"status": status, "free_channels": free, "vip_channels": vip, "pending_requests": pending, "ready_to_approve": ready}; log "health_service | check_channels_status | user_id=0 | pending=N ready=M")
    - `def check_scheduler_jobs(self) -> dict:` (scheduler = get_scheduler(); if not scheduler or not hasattr(scheduler, '_scheduler'): return {"status":"fail", "error":"no scheduler"}; jobs = scheduler._scheduler.get_jobs() if hasattr(scheduler._scheduler, 'get_jobs') else []; job_list = [{"id":j.id, "name":getattr(j,'name',j.id), "next_run_time": str(j.next_run_time) if j.next_run_time else None, "trigger": str(j.trigger)} for j in jobs]; status="ok" if jobs else "degraded"; log "health_service | check_scheduler_jobs | user_id=0 | jobs=N next_approve=..."; return {"status":status, "jobs_count":len(jobs), "jobs": job_list})
    - `def check_event_bus_listeners(self) -> dict:` (bus = get_event_bus(); counts = {e: len(ls) for e,ls in getattr(bus, '_listeners', {}).items()}; total = sum(counts.values()); besitos_listeners = counts.get(EVENT_BESITOS_AWARDED, 0); log "health_service | check_event_bus_listeners | user_id=0 | total=N besitos_listeners=M"; return {"status":"ok", "total_listeners": total, "by_event": counts, "besitos_awarded_listeners": besitos_listeners})
    - `def check_critical_services_sanity(self) -> dict:` (besito: from models.models import BesitoBalance, BesitoTransaction; neg = self._get_db().query(BesitoBalance).filter(BesitoBalance.balance < 0).count(); recent_tx = self._get_db().query(BesitoTransaction).filter(BesitoTransaction.created_at >= now - timedelta(hours=1)).count(); vip: with get_service(VIPService) as v: active = len(v.get_active_subscriptions()); expiring = len(v.get_expiring_subscriptions(24)); narrative: from models.models import UserStoryProgress, UserStoryAchievement; progress = self._get_db().query(UserStoryProgress).count(); achievements = self._get_db().query(UserStoryAchievement).count(); return {"besitos": {"neg_balances": neg, "recent_tx_vol": recent_tx, "status": "ok" if neg==0 else "degraded"}, "vip": {"active_subscriptions": active, "expiring_24h": expiring, "status":"ok"}, "narrative": {"progress_count": progress, "achievements_count": achievements, "status":"ok"}, "overall_sanity": "ok" if neg==0 else "degraded"}; log "health_service | check_critical_services_sanity | user_id=0 | neg_besito=N active_vip=M progress=P")
    - `def check_backup_status(self) -> dict:` (try: from pathlib import Path; backups = sorted(Path("backups").glob("*"), key=lambda p: p.stat().st_mtime, reverse=True); latest = backups[0] if backups else None; age_h = (time.time() - latest.stat().st_mtime)/3600 if latest else 999; status = "ok" if age_h < 25 else "degraded" if age_h < 48 else "fail"; return {"status":status, "last_backup": latest.name if latest else None, "age_hours": round(age_h,1)} except: return {"status":"unknown", "error":"no backups dir or access"}; log ...)
    - `def get_overall_status(self) -> dict:` (run all checks (best effort, wrap in try for any that block); aggregate: unhealthy if any "fail" or critical neg>0 etc, degraded if any "degraded", else "healthy"; add "timestamp": datetime.now(UTC).isoformat(), "version": "1.0-observability-spike", "uptime_s": from bot_runtime if avail; logger.info(f"health_service | get_overall_status | user_id=0 | overall={overall} checks=7"); return {"status": overall, "checks": { "db": db_res, "bot": bot_res, ... }, "timestamp": ..., "version": ...})
  - Optional: `def render_for_admin(self) -> str:` (or return data only; precedent is data to LucienVoice; keep data in svc, render in voice for separation).
  - Best-effort + timeouts: for checks that may vary, use try/except + short budget; no blocking main loop (health called on-demand from admin/terminal/endpoint).
  - Add to services/__init__.py: `from .health_service import HealthService`; `"HealthService"` in __all__ (enables `with get_service(HealthService) as h:` zero other changes).

- **Bot admin view (Custodios-only, exactly 1 service call, reuse analytics_handlers precedent or dedicated per impact "new or extend"):** Extend `handlers/analytics_handlers.py` (minimal, fits Custodios stats pattern) or add `handlers/admin_health_handlers.py` + router include in bot.py (to keep analytics pure, prefer small dedicated per impact rec; but tight scope lists 1 handler file: use analytics_handlers.py for /health Command to avoid new router registration if possible, or new + 1-line include in bot.py like analytics_router). Handler pattern al pie de la letra:
  ```python
  @router.message(Command("health"))
  async def health_cmd(message: Message):
      if not is_admin(message.from_user.id):
          await message.answer(LucienVoice.health_access_denied(), parse_mode=ParseMode.HTML)
          return
      try:
          with get_service(HealthService) as svc:  # exactly 1 service
              health = svc.get_overall_status()
          await message.answer(LucienVoice.system_health(health), parse_mode=ParseMode.HTML)
      except Exception as e:
          logger.error(f"health | cmd | user_id={message.from_user.id} | error={e}")
          await message.answer(LucienVoice.error_message())
  ```
  For menu cb: similar (if dedicated router or extend admin cb routing) `if data == "admin_health": ... same 1 svc + is_admin guard`. Add import + router include only if new file.
  - Logging: "health | cmd | user_id=XXX | overall=healthy" or error.

- **Menu button:** `keyboards/inline_keyboards.py` admin_menu_keyboard(): add 1 button e.g. after admin_analytics:
  `[InlineKeyboardButton(text="🛡️ Pulso del reino / Salud", callback_data="admin_health")]`
  (UI addition only; routing in handler as above; 1:1 with existing style).

- **Lucien voice renders (elegant 3rd person, copy analytics_dashboard + admin_greeting style al pie):** `utils/lucien_voice.py`
  - `def system_health(health: dict) -> str:` (🎩 <b>Pulso del Reino</b> <i>El guardián observa el latido...</i> then per check: ✅ DB: latencia 8ms or ⚠️ Scheduler: 5/5 jobs (next expire_sub 2h) or ❌ ... ; overall status emoji + "Diana recomienda..." if degraded; timestamp; "Los custodios velan por el reino de Diana."; <50 LOC)
  - `def health_access_denied() -> str:` (copy analytics_access_denied style: "Estos secretos del pulso son solo para los custodios... ⚠️ Acceso denegado")
  - Optional helpers: health_check_section(name, status, details) if keeps <50.
  - Preserve all existing; add at end of ANALYTICS or new OBSERVABILITY section.

- **Terminal script (standalone, reuses HealthService or direct with db= for ops; no install; supports --json/--verbose):** `scripts/health_check.py` (chmod +x; `#!/usr/bin/env python`; argparse; `from services import get_service, HealthService`; or direct HealthService() for no context if ops; `with get_service(HealthService) as svc: h=svc.get_overall_status()`; if --json: print(json.dumps(h)); sys.exit(0 if healthy else 1); else: print(LucienVoice.system_health(h) or human sections); logs inside use user_id=0; example: `python -m scripts.health_check --json`; `python -m scripts.health_check --verbose`.

- **Endpoint (lightweight /health JSON for Railway/curl/monitoring; optional/non-blocking):** Minimal (if aiohttp available or add as optional dep behind flag; or pure stdlib http.server but async preferred for bot compat). Per impact design:
  - New `health_server.py` (or inline in bot.py careful): `from aiohttp import web`; async def health_handler(request): with get_service(HealthService) as svc: data=svc.get_overall_status(); return web.json_response(data); app=web.Application(); app.router.add_get('/health', health_handler); runner etc.
  - Start non-blocking in bot.py on_startup (after scheduler/listeners): `if os.getenv("HEALTH_ENABLED") == "1": try: from health_server import start_health_http_server; asyncio.create_task(start_health_http_server(port=int(os.getenv("HEALTH_PORT", "8080")))); logger.info("health_service | startup_endpoint | user_id=0 | result=started") except Exception as e: logger.warning(...)` (fire-and-forget; separate port 0 conflict with aiogram polling).
  - JSON shape: {"status": "healthy|degraded|unhealthy", "checks": {... per check dicts ...}, "timestamp": "...", "version": "1.0-observability-spike", "uptime_s": 12345}
  - If no aiohttp or flag off: skip (log "endpoint disabled"); 0 breakage.
  - Railway/Procfile: no change this item (future healthcheckPath config after validation; endpoint on custom port if used).
  - 0 heavy queries (reuse light checks).

- **Wiring in bot.py (minimal, no logic):** Import `from services import get_service, HealthService` (or direct); optional `from health_server import ...` or inline; in on_startup after scheduler + listeners block: add health startup log or task start (comment "Health/observability (Item 11 spike)"); on_shutdown optional stop if server; extend listeners comment if needed (health not EventBus listener; observational service only). `logger.info("health_service | startup_checks_available | user_id=0 | result=ready")`.

- **Logging:** Every important health action (checks, overall, cmd, terminal, startup, endpoint) in "módulo | acción | user_id | resultado" (e.g. "health_service | check_db_connectivity | user_id=0 | status=ok latency_ms=12"; "health | cmd | user_id=123 | overall=healthy"; "health_check | cli | user_id=0 | status=degraded"). Follow event_bus precedent + CLAUDE mandate.

- **Tests (update/add; reuse analytics_service unit + scheduler unit + admin handler test patterns):** 
  - New `tests/unit/test_health_service.py` (unit mirroring analytics + scheduler: keys, mocked/db_session for checks (use TestSession or tmp for db checks), error paths (fail statuses), overall_status aggregation, logging format, best-effort (no exception to caller), <50 LOC funcs; 10-15+ tests; import-inside or patch for get_service/scheduler/bus if needed; no real side effects).
  - Update `tests/handlers/test_analytics_handlers.py` (or new test_admin_health_handlers.py if dedicated router): port/add tests for /health cmd (is_admin deny → health_access_denied; success with get_service mock + system_health render exact; error path; 1 svc call asserted via mock_context.__enter__; keep existing /stats /export 100%).
  - Integration/smoke: in broader or new, but tight: exercise real but safe checks (db ping, scheduler jobs list, bus counts, channel counts, vip counts, sanity aggregates); terminal smoke in gates (not unit test file).
  - 0 changes to golds/atomicity (re-runs protect); coverage for health via new unit + re-runs of admin/scheduler/channel/vip.
  - Exact pytest: `pytest -q --tb=line -p no:cov --override-ini="addopts=" -k "health or TestHealthService or TestAnalyticsService or scheduler or free_entry or TestScheduler or TestFreeEntry or TestEventBus or atomicity or vip or story or TestCrossServiceAtomicity" tests/`

- **Docs (minimal targeted; at end use documentador agent not manual):** 
  - `decisions.md`: Append full decision entry after last (Item 10/28) following exact style of Item 10/6/5 (## Title (Item 11 / third of new pool of 4); Motivo (observability spike per roadmap + friendly admin views + platform /health); Riesgos (LOW if tight read-only + timeouts; 3 crit explicit 0-risk per impact; pre-exist scheduler serialization/test flakes mit by safe points/re-runs); Decisión (HealthService checks as listed + overall + render data; /health JSON optional aiohttp/flag/separate port; bot /health cmd or menu "🛡️ Pulso del reino" via 1 svc get_service(HealthService)+is_admin in analytics_handlers or dedicated + 1 btn in admin_menu; terminal scripts/health_check.py --json/--verbose; wiring on_startup; logging mandatory; tests unit+admin+smokes; documentador for ROADMAP/services/CLAUDE at end; 0 impact on 3 crit/atomicity/get_service/EventBus contracts; copy Analytics read-only + get_service + is_admin + Lucien + precedents al pie); Resultado (0 behavior/0 atomicity/0 prod; HealthService + endpoint + bot view + terminal + logging + tests green; gates; handoff "Item 11/29 closed. Third... Ready for arch-enforcer re-scan (enfocado en health checks + endpoint + admin views + no impact on 3 crit) + test-guardian (correr los tests críticos) + documentador (for final ROADMAP update) + gsd-executor del siguiente item del pool de 4."); pool phrase verbatim).
  - `services/CLAUDE.md`: Update services table (add `| HealthService | System/Observability | health_service.py | check_db_connectivity, check_bot_runtime, check_channels_status, check_scheduler_jobs, check_event_bus_listeners, check_critical_services_sanity, check_backup_status, get_overall_status, close, _get_db |`); add note under cross-cutting or new "Observability" section: "HealthService (Item 11): read-only/best-effort system status for Custodios/ops/platform; follows AnalyticsService pattern (_get_db/owns/close/direct counts); admin-only via is_admin + get_service(HealthService) exactly 1 call in handlers; logging 'health_service | ... | user_id=0 | ...'; best-effort timeouts; 0 mutation/0 impact on 3 crit (gamif/narr/channel) or atomicity/EventBus/get_service contracts; see decisions Item 11 + impact report.").
  - (documentador handles HARDENING_ROADMAP appends + any root notes at end of tirón/pool per user mandate "use documentador agent (not manual) for the docs update at the end"; prompt lists tirón artifacts: this PLAN + opt SUMMARY + gsd-observability-health.log + impact item11 + decisions append + services/CLAUDE update + tests + bot/handlers changes).

- **Gates + re-runs (protect 0 regression + 3 crit + contracts; exact from precedents + impact critical list):** 
  - Targeted re-runs: full `tests/integration/test_cross_service_atomicity.py` (gold happy + sad/partials with patch schedule_emit + strict + DESIRED + "credit survives deliver False" + "post-credit best effort (misiones + listeners)" + TestSession/file + 777 TG + gather + N806 tol w/doc; 0 attributable change); `tests/integration/test_free_entry_flow.py` (scheduler/pending/channel); `tests/unit/test_scheduler.py`, `test_event_bus.py`, `test_channel_service.py`, `test_vip_service.py`, `test_analytics_service.py`; broader `pytest -q --tb=line -p no:cov --override-ini="addopts=" -k "cross_service_atomicity or reaction_mission_flow or reaction_full_chain or free_entry_flow or TestScheduler or scheduler or vip or story or event_bus or atomicity or daily_claim or besitos or TestCrossServiceAtomicity or TestFreeEntry or TestAnalytics or health or TestHealthService" tests/`; handler admin/analytics spot if /health added.
  - Non-pytest: ruff + format --check on new/touched (services/health_service.py services/__init__.py handlers/*health*.py or analytics_handlers.py scripts/health_check.py utils/lucien_voice.py keyboards/inline_keyboards.py bot.py decisions.md); bot smoke `python -c "import bot; print('bot import ok'); from services import get_service, HealthService; print('get_service Health ok'); h=HealthService(); print(h.check_db_connectivity())"` + manual on_startup import/register sim (no crash); manual endpoint `curl -s http://localhost:8080/health | python -m json.tool` (if enabled; status, checks keys, no secrets); terminal `python -m scripts.health_check --json` (valid json, exit 0 on healthy); `python -m scripts.health_check --verbose` (Lucien sections); full bot startup smoke (no crash, scheduler jobs, health available); greps (post edits, in GSD): `grep -n "health_service |" services/health_service.py handlers/... scripts/...`; `grep -c "with get_service(HealthService)" handlers/...` (expect >=1); `grep -n "def check_" services/health_service.py`; `grep -n "🛡️ Pulso" keyboards/...`; `grep -n "Item 11" decisions.md services/CLAUDE.md .planning/.../gsd-*.log`; LOC inspect `python -c "import inspect; from services.health_service import HealthService; print(inspect.getsourcelines(HealthService.check_db_connectivity)[0])"` etc <=50; `python -c "from services import get_service, HealthService; print('get_service ok')"`; 0 "self\.(besito|other)_service" new patterns; 3 crit + contracts protected via golds re-runs + greps (0 new writes in gamif/narr/channel paths).
  - Patch schedule_emit + DESIRED not needed (health no emit), but re-runs of golds protect indirectly.
  - 0 new test files beyond the listed unit + handler update (tight).

- **Behavior/contracts:** All existing flows identical (0 change to credits, reactions, daily claims, mission deliver, story advance/archetype/achievements, channel pending/approve/expire, VIP grant/revoke, store purchases, broadcast, game, etc.). Health is purely observational (reads + aggregates + status); best-effort (timeouts + try/except); non-blocking (on-demand or fire-and-forget task); admin/ops/monitoring only. No user-visible change. Partial failure/atomicity contracts untouched (golds re-runs confirm). get_service/EventBus contracts preserved (health uses get_service for its own; observes bus counts only, no register/emit/mutate).

- **Artefacts:** This PLAN.md + GSD entries (pre every) in the dedicated log + (optional post-exec) SUMMARY.md mirroring 28/27 style. Pool note "third of new pool of 4" at F6 self-check + log. Handoff explicit to gsd-executor of this PLAN (then arch-enforcer focused on health_service + get_service usage + admin views + 3 crit read-only + no impact; test-guardian; documentador for ROADMAP; next pool item).

**Archivos que se modificarán (exactos, por orden de fases; prefer extend, minimal; 0 other):**
1. `.planning/quick/gsd-observability-health.log` (all phases, pre only via echo/python -c safe; no "edit" of source).
2. `services/health_service.py` (new; F2: full class + all check_* + get_overall + close/_get_db per design + logging format + <50 LOC + arch comments "Item 11 / observability health / arch-enforcer").
3. `services/__init__.py` (F2: add import HealthService + "HealthService" to __all__; 0 other changes).
4. `bot.py` (F3: import get_service/HealthService + optional health_server; in on_startup after scheduler/listeners: optional asyncio.create_task for endpoint if HEALTH_ENABLED + log "health_service | startup..."; extend on_shutdown if; comment "Health/observability (Item 11 spike)"; 0 logic).
5. `handlers/analytics_handlers.py` (F4: add Command("/health") + cb support if menu (or minimal); is_admin guard + with get_service(HealthService) as svc: health=svc.get_overall_status(); answer(LucienVoice.system_health(health)); exactly 1 svc; logger; access denied path; update docstring "extended for /health (Item 11) reusing Analytics pattern + 1 svc + is_admin. Precedent analytics + admin handlers.").
6. `keyboards/inline_keyboards.py` (F4: add 1 button in admin_menu_keyboard() for "🛡️ Pulso del reino" callback_data="admin_health"; after analytics btn; 0 other).
7. `utils/lucien_voice.py` (F4: add system_health(health:dict) -> str (copy analytics_dashboard 451+ style al pie: 3rd person, emojis per status, sections for each check, overall, "Diana", "custodios", "El pulso del reino...", timestamp; <50 LOC); health_access_denied() -> str (copy analytics_access_denied 486+); optional error/health sections if fit).
8. `scripts/health_check.py` (new; F5: CLI script re-using get_service(HealthService) or direct; --json/--verbose; human/Lucien output or json; exit codes; user_id=0 in internal logs; chmod +x; docstring "Standalone health check for ops/monitoring. Reuses HealthService (Item 11). python -m scripts.health_check [--json] [--verbose]"; no new deps).
9. `tests/unit/test_health_service.py` (new; F6: unit tests for all checks (mocks for db/scheduler/bus/channel/vip), error paths, overall aggregation, logging, best-effort, lifecycle (owns/close), <50; 10-15+ tests; mirror test_analytics_service + test_scheduler).
10. `tests/handlers/test_analytics_handlers.py` (F6: update/add tests for new /health cmd (deny, success with mock get_service + render assert exact Lucien text, error path, 1 svc via __enter__ mock); keep 100% prior /stats/export coverage; docstrings "extended for health cmd (Item 11) + 1 svc HealthService + is_admin + Lucien. Precedent from analytics itself.").
11. `decisions.md` (F6: append full Item 11 decision entry after Item 10, exact style Motivo/Riesgos/Decisión/Resultado + refs to impact/PLAN/gsd + pool "third of new pool of 4" + handoff).
12. `services/CLAUDE.md` (F6: update services table + add Observability note under cross or new section; refs this PLAN + impact + gsd + decisions).
13. Re-runs/gates/verifs/smokes do not modify (except log appends + ruff auto-fixes if any on touched + documentador at end for ROADMAP which is agent-driven, no manual edit by executor).

**Fuera explícitamente (nada de scope creep, per "tight" + impact "0 other files" + "0 behavior change" + "0 prod change" + "0 impact on 3 crit or atomicity/contracts" + precedents):**
- **NO** other files in services/ (no changes to besito/credit paths, story/advance, channel/approve/pending/expire logic, VIP grant/revoke, store purchase, missions deliver, broadcast, game, daily, reward, backup impl, analytics_service (reuse only), scheduler job handlers (only observe status), event_bus (observe counts only), package, user, promotion, etc.; 0 new public methods unless thin optional for scheduler/bus per "safe extension" and only if private access ugly — prefer internal access in Health with comment for system domain).
- **NO** other handlers (no change to admin_handlers.py beyond if cb routing requires 1-line; no mission/store/gamif/story/vip/promotion/ etc handlers; 0 change to common/free_channel etc; if dedicated health router, only the new small file + 1 include in bot.py).
- **NO** models/ (0 new tables/columns/enums; 0 alembic; use existing Subscription/Pending/Channel/BesitoBalance/BesitoTransaction/UserStoryProgress/etc for counts only).
- **NO** changes to critical flows (grep confirm 0 new writes/mutations/side-effects in gamif credit/debit/reaction/daily/mission/narrative advance/archetype/achievement/channel approve/expire/pending/VIP grant/revoke/store purchase atomic paths; health never participates in tx).
- **NO** new events or changes to besitos_awarded payload or EventBus contracts (health observes only).
- **NO** broad "add metrics/alerts" (only core status checks + overall + friendly views + /health JSON per impact "Start with core...").
- **NO** heavy endpoint framework or port conflict (aiohttp optional/flag/separate port; or defer HTTP if risk; bot cmd + terminal have 0 dep).
- **NO** changes to close() patterns beyond HealthService's own (follows Analytics); 0 to get_service impl.
- **NO** editing CLAUDEs/decisions/ROADMAP except the specified targeted (documentador for ROADMAP at end); no AGENTS/root beyond cross if precedent.
- **NO** touching middlewares, config (beyond read ADMIN/DB), utils/helpers, keyboards beyond 1 btn, Procfile/railway beyond future comment, DEPLOY beyond opt, tests beyond listed (no new gold cases, no extension of atomicity/free_entry for health).
- **NO** behavior or contract changes (0 impact on partial failure, 0 on "credit survives...", 0 on any user/admin flows, 0 on UI/Lucien outside new health renders which are additive, 0 on FSM/narrative progress).
- **NO** mutation in any health path (all reads + aggregates + status; best-effort).

**Comportamiento observable:** Identical for all user/admin/gamif/narrative/channel/VIP/store/mission flows (no change to credits, approvals, progress, deliveries, etc.). New /health Command (admin-only) or menu "🛡️ Pulso del reino" shows Lucien-voiced summary (✅/⚠️/❌ per check + overall + "El pulso del reino..." + Diana refs); terminal script prints same or JSON with exit code; /health (if enabled) returns JSON {"status": "...", "checks": {...}, ...} for curl/Railway/monitoring. Health checks run on-demand (best-effort, non-blocking, timeouts); startup only logs availability or starts optional endpoint task. No user-visible or prod behavior change. The 3 critical systems (gamif/narr/channel) remain fully protected (observational reads only; golds + re-runs + greps confirm 0 impact). "0 behavior/0 atomicity/0 prod change".

---

## 2. Fases (strict order, 5-6 small gated, with DoD checklist + GSD pre every + safe points; copy 28/27/26/25/24/23 structure al pie de la letra)

**F1 prep/GSD/baseline** (log, ruff clean on health_service (new) + __init__ + bot + analytics_handlers + inline_keyboards + lucien_voice + scripts (new) + decisions + services/CLAUDE + tests, baseline targeted pytest w/ exact -q --tb=line -p no:cov --override-ini="addopts=" (unit analytics  (adapt), scheduler full, event_bus, channel, vip, free_entry spot, cross atomicity gold spot, admin/analytics handlers spot), greps current jobs/logging/on_startup/health mentions (expect 0 active) + gold patterns al pie (AnalyticsService _get_db/owns/close/direct counts, analytics_handlers 1svc+is_admin+get_service+Command+ Lucien render, bot on_startup 187+ + listeners 203-212 + "Cross-domain..." comment + Item 10 store, admin_menu btns, Channel count_pending/get_ready + get_free/vip, VIP get_active/expiring/expired/is_user_vip, scheduler get_scheduler + _scheduler.get_jobs, event_bus _listeners + register/emit logs, is_admin, get_service context, lucien analytics_dashboard 451+ style + access_denied, logging "módulo | acción | user_id | ...", TestSession/N806/777/gather/try/finally in golds, "credit survives deliver False" + "post-credit best effort (misiones + listeners)", "if hasattr.*besito_service" daily guards, "DESIRED CONTRACT", "MUST NOT credit", "Item 10 store", pool phrase from 25/26/27/28), confirm fixtures (db_session, TestSession/tmp_path for cross, mock_bot, sample_*, patch schedule_emit if needed for re-runs, get_event_bus, fresh TG 777x), read precedents (28/27/26/25/24/23 PLAN + gsd + SUMMARIES for GSD style + self-check + pool phrase + "Nth of new pool of 4" + handoff language + "Copia patrones al pie de la letra" + "Instrucciones para gsd-executor"; 22 critical tests; impact11 for exact scope/files/checks/endpoint design/bot view/terminal + "third of new pool of 4" + "Pool anterior..."; HARDENING_ROADMAP for observability spike + /health (DB/bot/channels/bus/scheduler) + pool post Item 10 + documentador; gold atomicity + free_entry + scheduler + event_bus + analytics for patterns + re-runs; current code bot/scheduler/analytics/event_bus/channel/vip/database/handlers/analytics/admin/keyboards/lucien/services init/settings/railway/Procfile/CLAUDEs/decisions as listed), UI strings pinned for bot view (admin menu "🛡️ Pulso del reino", /health cmd, Lucien sections from analytics style), "F1 safe point". DoD marked. (0 logic source changes; hygiene only if surfaces from ruff on new files.)

**F2 HealthService + core checks** (GSD pre each; implement services/health_service.py full (class + all check_* as listed + get_overall_status + lifecycle _get_db/owns/close following Analytics al pie + logging format every check + <50 LOC per func + verb+context+result + best-effort/try/except + timeout wrappers where sensible (e.g. for scheduler/bus access); add to services/__init__.py export + __all__; 0 other services touched; ruff limpio + format hygiene 0 logic; grep for "health_service |" + check_ defs + get_overall + logging format + no mutation; targeted unit smoke (import + basic checks with mocks); "F2 safe point". DoD marked. Copy Analytics read-only pattern al pie de la letra.)

**F3 endpoint + bot wiring (lightweight)** (GSD pre; implement minimal endpoint (health_server.py or inline careful; aiohttp if avail or flag/optional; /health handler using get_service(HealthService) + json_response; start non-blocking via asyncio.create_task in bot.py on_startup after scheduler/listeners + log "health_service | startup_endpoint | ..."; on_shutdown stop if; 0 breakage to polling; if no dep or HEALTH_ENABLED!=1 skip gracefully; ruff; bot smoke (import + on_startup sim no crash); greps for wiring + log; "F3 safe point". DoD marked. 0 changes to listeners reg or scheduler.)

**F4 bot admin view + menu + voice** (GSD pre; extend handlers/analytics_handlers.py (or new admin_health_handlers.py + include) with /health Command + is_admin + with get_service(HealthService) as svc: exactly 1 svc + answer(LucienVoice.system_health(health)); add health_access_denied path; logging "health | cmd | user_id=... | ..."; if menu cb support (data=="admin_health") same pattern; keyboards/inline_keyboards.py add 1 btn "🛡️ Pulso del reino" callback_data="admin_health" in admin_menu_keyboard() after analytics; utils/lucien_voice.py add system_health(health) copying analytics_dashboard 451+ al pie (3rd person elegant, emojis per status ✅⚠️❌, sections for db/bot/channels/scheduler/bus/sanity/backup/overall, "🎩 <b>Pulso del Reino</b>", <i>El guardián observa el latido del reino de Diana...</i>, "Los custodios velan...", timestamp, "Diana recomienda..." if degraded; <50 LOC) + health_access_denied copying analytics_access_denied; ruff; bot smoke (import + cmd sim); greps "with get_service(HealthService)" + "🛡️ Pulso" + system_health + "health | cmd"; "F4 safe point". DoD marked. UI 1:1 with existing admin menu/Lucien style. Exactly 1 svc/handler.)

**F5 terminal script** (GSD pre; create scripts/health_check.py (standalone CLI; reuses from services import get_service, HealthService; argparse for --json --verbose; with get_service or direct HealthService(db=...) for ops; h=svc.get_overall_status(); if json print + exit code based on status; else print(LucienVoice.system_health(h) or human formatted sections); logs use user_id=0; docstring + usage; chmod +x; no new deps; ruff; manual run --json + --verbose; grep for script + get_service inside; "F5 safe point". DoD marked. Copy scripts/verify_env.py pattern al pie for structure.)

**F6 tests + gates + self-check + handoff (documentador for docs)** (GSD pre every; implement tests/unit/test_health_service.py (full units for checks/overall/lifecycle/logging/best-effort/mocks; 10-15+; ruff); update tests/handlers/test_analytics_handlers.py (or equiv) for /health cmd coverage (deny/success/error/1svc mocks + render asserts + docstring "ported/extended for health (Item 11) + 1 svc + is_admin + Lucien. Precedent analytics"); re-execute: full cross atomicity gold + free_entry + scheduler + event_bus + vip/story units + broader -k with health filters (exact flags from impact/PLAN; all green 0 attributable); ruff on all touched + format --check; bot smoke (import + get_service Health + manual on_startup + cmd sim); terminal run + curl if endpoint; greps (0 writes in crit paths; "health_service |" logs; get_service(HealthService) count; 1 btn; system_health + denied in voice; "Item 11" in decisions/CLAUDE/gsd; LOC <=50 via inspect; no privates without comment); rules verif (GSD pre every, scope tight per PLAN  ~12 files + log + PLAN + opt SUMMARY + 0 other, 3 crit + contracts protected via re-runs + greps, HealthService checks as designed, endpoint non-blocking, bot view 1svc+is_admin+Lucien, terminal, logging format, <50, verb+ctx+res, get_service, is_admin, read-only/best-effort, documentador used for ROADMAP/docs); launch documentador agent (spawn_subagent subagent_type="documentador" with rich prompt: "Update HARDENING_ROADMAP.md for Item 11/29 observability-health completion (append to sec 4/5 + metrics + pool/BATCH notes); persist tirón learnings; use only the following artifacts as source of truth: .planning/phases/29-observability-health/PLAN.md + (opt) SUMMARY.md + .planning/quick/gsd-observability-health.log + .claude/agent-memory/impact-analyzer/item11-observability-health.md + decisions.md Item 11 entry + services/CLAUDE.md update + test files + bot/handlers changes; no manual edits; follow GSD pre-log discipline for your work in .planning/quick/gsd-documentador-*.log; output report in .claude/agent-memory/documentador/ + MEMORY.md pointer; include verbatim pool phrase and 'Item 11/29 closed...' handoff."); full self-check PASSED in log with structure mirroring 28/27/26/25/24/23 (phases/DoD/gates/archivos/tests passed/reglas verificadas (GSD pre every, scope tight per PLAN, HealthService checks read-only/best-effort, endpoint, bot view 1svc+is_admin+Lucien, terminal, logging, 3 crit + contracts protected, documentador used for docs)/desviaciones (pre only, doc non-reg)/tests críticos para futuro (exact list from PLAN/impact: cross atomicity, free_entry, scheduler, event_bus, channel/vip units, analytics service, new health unit, admin/analytics handlers with health, bot smoke, terminal/curl, ruff/greps/LOC)/"Item 11/29 closed. Third of new pool of 4. Pool anterior de 4 cerrado (tests passing per user). Nuevo pool de 4 iniciado. Quedan ~2-4 clusters del análisis inicial después de este pool. Previous batch of 4 (Item 7/25 + Item 6/24 + Item9/27 + Item 10/28) closed with tests passing per user. Ready for arch-enforcer re-scan (enfocado en health checks + endpoint + admin views + no impact on 3 crit) + test-guardian (correr los tests críticos listados) + documentador (for final ROADMAP update) + gsd-executor del siguiente item del pool de 4."); (opt) write SUMMARY.md mirroring 28/27; (0 prod changes). DoD all marked. "F6 safe point final + success criteria of plan met".

---

## 3. Copia patrones **al pie de la letra**

- **AnalyticsService read-only patterns (for HealthService):** __init__(db=None), self.db / self._owns_session = db is None, _get_db (create SessionLocal if needed), close (if owns and self.db: close; self.db=None), direct model queries .count() / .filter().count() / .all() + sum for aggregates (no full scans, light for bot); no mutation, no writes; used for Custodios/ops; see analytics_service.py 20-76 for get_dashboard_stats exact.
- **get_service + is_admin in admin handlers (Custodios views):** analytics_handlers.py exact: from services import get_service; from services.analytics_service import AnalyticsService; from utils.admin import is_admin; from utils.lucien_voice import LucienVoice; @router.message(Command("stats")): if not is_admin(...): await ... (LucienVoice.analytics_access_denied()...); return; try: with get_service(AnalyticsService) as svc: stats=svc.get_dashboard_stats(); await message.answer(LucienVoice.analytics_dashboard(stats), parse_mode=ParseMode.HTML); except: ... logger + error_message. Same for /export. Apply 1:1 for /health + HealthService + system_health + health_access_denied. "exactly 1 service" per handler/CLAUDE.
- **EventBus best-effort "MUST NOT mutate":** Copy from story_service 670-694 / reward/broadcast post-Item5/6: "Cross-domain event listeners (registered explicitly from bot.py...); ... MUST NOT call back into credit/debit besitos (to avoid any re-entrancy...); This is observational only (best effort; errors swallowed by bus)."; health applies same spirit (observes counts only via get_event_bus()._listeners; no register/emit/mutate; log "health_service | check_event_bus..."; best-effort).
- **Scheduler job inspection:** get_scheduler() singleton (from services.scheduler_service import get_scheduler); jobs via scheduler._scheduler.get_jobs() (or thin get_jobs_status if added per safe extension); list id/name/next/trigger; used in health check + startup; precedent in bot on_startup + scheduler_service itself + free_entry tests.
- **ChannelService / VIPService public APIs for counts/pending/active (read-only):** Channel: get_free_channels, get_vip_channels, count_pending_requests, get_ready_to_approve, get_all_pending_requests; VIP: get_active_subscriptions, get_expiring_subscriptions(hours), get_expired_subscriptions, is_user_vip; used for health "channels" / "vip" checks; 0 touch on approve/expire/create_pending logic (in _process_pending + handlers + scheduler cron).
- **Structured logging:** "módulo | acción | user_id | resultado" (or more fields); e.g. event_bus | emit | ... | listeners=N | errors=..; health_service | check_db_connectivity | user_id=0 | status=ok latency_ms=12; health | cmd | user_id=123 | overall=healthy. Every important action (checks, overall, bot/terminal/endpoint/startup). Follow CLAUDE + event_bus precedent.
- **Lucien voice (3rd person elegant, "Diana", "custodios", "visitantes", never vulgar):** Copy analytics_dashboard 451+ + analytics_access_denied 486+ + admin_greeting + error_message al pie: 🎩 <b>..</b> <i>..</i> emojis sections; for health: "🎩 <b>Pulso del Reino</b>" / "El guardián observa el latido del reino de Diana..." / "Los custodios velan por el reino..." / "Diana recomienda..." if degraded; health_access_denied "Estos secretos del pulso son solo para los custodios..."; system_health takes dict (like analytics_dashboard(stats)).
- **1-svc/handler rule + no logic in handlers:** Strict per CLAUDE/handlers/CLAUDE/architecture: handler = guard (is_admin) + exactly 1 with get_service(HealthService) as svc: health=svc.get_overall_status() + answer(voice) + error path + log; all aggregation/render prep in HealthService + LucienVoice. 0 biz logic/DB.
- **GSD pre every + wc tracking:** run_terminal echo "=== $(date -Iseconds) | PHASE N | GSD pre-... - <desc + refs DoD + patrones copiados al pie de la letra>" >> .planning/quick/gsd-observability-health.log (or python -c safe for long); wc -l after; identical to 28/27/26/25/24/23 + impact gsd (detailed, pre-edit/pre-gate/pre-ruff/pre-pytest/pre-grep/pre-smoke/pre-self-check, "F<N> safe point", full self-check at end).
- **Self-check structure + pool phrase + handoff:** Mirror 28-SUMMARY/27/26/25/24/23 verbatim at F6: phases/DoD/gates/archivos/tests passed/reglas verificadas (GSD pre every, scope tight per PLAN, ... 3 crit + contracts protected, documentador used...)/desviaciones (pre only, doc non-reg)/tests críticos para futuro (exact list)/"Item 11/29 closed. Third of new pool of 4. Pool anterior de 4 cerrado (tests passing per user). Nuevo pool de 4 iniciado. Quedan ~2-4 clusters del análisis inicial después de este pool. Previous batch of 4 (Item 7/25 + Item 6/24 + Item9/27 + Item 10/28) closed with tests passing per user. Ready for arch-enforcer re-scan (enfocado en health checks + endpoint + admin views + no impact on 3 crit) + test-guardian (correr los tests críticos listados) + documentador (for final ROADMAP update) + gsd-executor del siguiente item del pool de 4."
- **Handoff to arch-enforcer + test-guardian + documentador:** Explicit in self-check + log + (opt) SUMMARY; "Ready for arch-enforcer re-scan (enfocado en ...) + test-guardian (correr los tests críticos) + documentador (for final ROADMAP update) + gsd-executor del siguiente..."
- **Documentador at end:** Per user "use documentador agent (not manual) for the docs update at the end of the tirón"; in F6: spawn_subagent(subagent_type="documentador", prompt= rich with list of tirón artifacts (PLAN/SUMMARY/gsd log/impact/decisions/CLAUDE/tests/changes) + "update HARDENING_ROADMAP + persist learnings + GSD pre for your log + no manual code edits").
- **Commands exact:** pytest -q --tb=line -p no:cov --override-ini="addopts=" -k "..." ; ruff via ./venv/bin/python -m ruff check ... --fix && ./venv/bin/python -m ruff format --check ... ; bot smoke python -c "..." (or ./venv/bin/python -c if needed); curl for /health; python -m scripts.health_check --json; greps with -n -c -A; LOC via python -c "import inspect; ... getsourcelines"; GSD pre before each.
- **UI 1:1 where relevant:** Admin menu button + callback + handler flow + Lucien text 1:1 with analytics precedent (strings/emojis/cbs/backs preserved; new health additive).
- **3 crit + contracts + logging + GSD in every section:** Explicit in Alcance, Fases, Copia, Instrucciones, Risks, Self-check.

---

## 4. Instrucciones para gsd-executor

1. **Read first (mandatory, before any edit/gate):** This PLAN.md (full) + `.claude/agent-memory/impact-analyzer/item11-observability-health.md` (full) + `.planning/quick/gsd-impact-analyzer-item11-observability-health.log` + precedents `.planning/phases/28-remaining-besito-store/PLAN.md` + `28-remaining-besito-store-SUMMARY.md` + `.planning/quick/gsd-remaining-besito-store.log` + 27-mission-admin-long-funcs/PLAN + SUMMARY + gsd + 26/25/24/23 PLANs/SUMMARIES/gsds (for GSD style + self-check + pool phrase + handoff + "Copia... al pie" + "Instrucciones..." + 5-6 phase structure + "Nth of new pool of 4" language) + HARDENING_ROADMAP.md (recent + pool) + current code (bot.py on_startup/listeners, services/analytics_service.py full, scheduler_service.py, event_bus.py, channel_service.py, vip_service.py, models/database.py, handlers/analytics_handlers.py full, admin_handlers.py, keyboards/inline_keyboards.py admin_menu, utils/admin.py, utils/lucien_voice.py (analytics_dashboard + access_denied + error + structure), services/__init__.py (get_service), services/CLAUDE.md, handlers/CLAUDE.md, decisions.md (tail + recent Item 10), config/settings.py, railway.toml, Procfile, models/CLAUDE.md, architecture.md, rules.md, CLAUDE.md, tests/integration/test_cross_service_atomicity.py (guards + DESIRED + TestSession + 777 + gather + patch + "credit survives" + "post-credit...", daily guards), test_free_entry_flow.py, unit/test_scheduler.py + test_event_bus.py + test_analytics_service.py + test_channel_service.py + test_vip_service.py, handlers tests for admin/analytics, scripts/verify_env.py or similar for terminal pattern). Confirm 0 current health active (grep), 3 crit protected in mind.

2. **GSD pre-log discipline (non-negotiable, total):** BEFORE every modification (search_replace/write/edit on any source/test/doc/log/PLAN), gate (ruff/pytest/grep/smoke/LOC), verif, self-check, SUMMARY, or documentador launch: run_terminal append "=== $(date -Iseconds) | PHASE N | GSD pre-... - <desc + refs DoD + patrones copiados al pie de la letra from 28/27/26/25/24/23 + impact11 + Analytics read-only + get_service 1svc + is_admin + bot on_startup + Channel/VIP publics + scheduler jobs + event_bus counts + lucien analytics style + logging format + self-check struct + pool phrase + documentador at end>" >> .planning/quick/gsd-observability-health.log (use python -c for long/quoted safety); then wc -l .planning/quick/gsd-observability-health.log . Track 5-10+ entries per phase + pre/post gates; cross-ref impact gsd. No exceptions. "No edits without pre-log."

3. **Tight scope (0 creep, 0 other files):** Only the listed files in "Archivos que se modificarán" (services/health_service.py + __init__.py + bot.py + handlers/analytics_handlers.py (extend for /health) + keyboards/inline_keyboards.py (1 btn) + utils/lucien_voice.py + scripts/health_check.py (new) + tests/unit/test_health_service.py (new) + tests/handlers/test_analytics_handlers.py (update) + decisions.md + services/CLAUDE.md + log + this PLAN + opt SUMMARY). 0 models/0 alembic/0 other services (besito/story/channel/vip/store/mission/reward/etc untouched; health calls their publics read-only only)/0 other handlers (0 change to admin_handlers beyond if cb routing 1-line, 0 to user handlers, 0 to mission/store/gamif etc)/0 critical flows (no writes/mutation/side-effects in gamif/narr/channel paths; grep in F6). If endpoint requires health_server.py, count it in tight list (minimal). Use get_service(HealthService) for bot/terminal/endpoint; direct for health internal checks if needed (follows Analytics). 0 behavior/0 atomicity/0 prod change. Re-verify 3 crit protection via golds re-runs + greps (0 new writes).

4. **Copy al pie de la letra (every phase):** AnalyticsService lifecycle + read-only direct counts for HealthService; analytics_handlers 1svc+is_admin+get_service+Command+Lucien render for health cmd; bot on_startup wiring style (after scheduler/listeners + log); admin_menu 1 btn additive; Channel/VIP/Scheduler/EventBus publics for read-only status; structured logging format; Lucien 3rd-person analytics_dashboard style for system_health; 1-svc/handler strict; GSD pre + wc + detailed entry style from 28/27 gsd logs; self-check structure + pool phrase + "Nth of new pool of 4" + handoff language verbatim from 28/27/26/25/24/23 SUMMARIES/self-checks; atomicity gold patterns for re-runs (even if indirect); "Copia patrones **al pie de la letra**" + "Instrucciones para gsd-executor" sections in mind; documentador spawn at F6 end for ROADMAP/docs (not manual appends).

5. **Phases strict (5-6 small, gated, safe points, DoD before advance):** Follow F1-F6 order above; mark DoD checklist in GSD at end of each (e.g. "F2 gates complete + safe point: ruff limpio; grep 0 mutation + 'health_service |' + checks; pytest spot; 'F2 safe point - HealthService implemented + lifecycle + logging + best-effort; 0 other services; 0 crit impact; reversible. Ready for F3. DoD all marked.'"); "F<N> safe point" log append before next. Revertable (delete health_service + 1-2 lines in __init__/bot/handlers/keyboards/voice/decisions/CLAUDEs = clean; remove btn + cmd = no menu impact; skip endpoint = unaffected).

6. **Re-verify 3 crit + contracts + rules:** In F1 baseline + F6 final: re-run golds (cross atomicity full w/ patch+DESIRED+TestSession+strict+"credit survives deliver False"+"post-credit best effort (misiones + listeners)"+N806+777+gather+try/finally; free_entry full for scheduler/channel; scheduler/event_bus/channel/vip units); broader -k with health; greps "0 self\.(besito|...)" new in crit paths, 0 writes in gamif/narr/channel, get_service(HealthService), logging format, 1svc, LOC<=50, "Item 11" refs; bot/terminal/endpoint smokes; ruff. 3 crit explicit 0-risk in self-check.

7. **Gates/commands (exact from PLAN + precedents):** Use ./venv/bin/python -m where needed for ruff/pytest; pytest ... -q --tb=line -p no:cov --override-ini="addopts=" -k "...health or TestHealthService or ..."; ruff check --fix + format --check on touched; bot smoke python -c (venv fallback); terminal python -m scripts.health_check --json/--verbose; curl if endpoint; greps -n -c -A; LOC python -c inspect.getsourcelines; GSD pre before each cmd.

8. **Documentador at end (F6):** After tests green + rules verif + self-check PASSED: spawn_subagent(subagent_type="documentador", prompt= detailed: "For Item 11/29 observability-health tirón close (third of new pool of 4): update HARDENING_ROADMAP.md (append to completed work sec 4, pool/BATCH notes in sec 5, metrics); persist tirón report in .claude/agent-memory/documentador/ + MEMORY.md pointer; source of truth only: 29-observability-health/PLAN.md + (opt) SUMMARY + gsd-observability-health.log + impact item11 + decisions Item 11 entry + services/CLAUDE update + bot/handlers/tests changes; follow GSD pre-log in your dedicated quick/gsd-documentador-*.log mirroring impact/executor; include verbatim pool phrase + 'Item 11/29 closed...' handoff; no manual code edits outside your report. Output confirmation + report location.") . Cross-ref PLAN F6 + precedents (documentador used for Item 10/28 close + prior).

9. **Self-check at F6 end (full structure in log + opt SUMMARY):** phases/DoD/gates/archivos/tests passed/reglas verificadas (GSD pre every [wc count], scope tight [list X files + log + PLAN + opt SUMMARY; 0 other], HealthService checks as designed read-only/best-effort, endpoint (if), bot view (1svc+is_admin+Lucien), terminal, logging format, 3 crit + contracts protected via re-runs/greps, documentador used for docs)/desviaciones (pre only, doc non-reg per 28/27/26 precedents)/tests críticos para futuro (exact list: cross atomicity, free_entry, scheduler, event_bus, channel/vip/analytics units, new health unit, analytics_handlers update for /health, bot/terminal/curl smokes, ruff/greps/LOC verifs)/"Item 11/29 closed. Third of new pool of 4. Pool anterior de 4 cerrado (tests passing per user). Nuevo pool de 4 iniciado. Quedan ~2-4 clusters del análisis inicial después de este pool. Previous batch of 4 (Item 7/25 + Item 6/24 + Item9/27 + Item 10/28) closed with tests passing per user. Ready for arch-enforcer re-scan (enfocado en health checks + endpoint + admin views + no impact on 3 crit) + test-guardian (correr los tests críticos listados) + documentador (for final ROADMAP update) + gsd-executor del siguiente item del pool de 4."; + "Pool anterior de 4 cerrado (tests passing per user). Nuevo pool de 4 iniciado. Quedan ~2-4 clusters del análisis inicial después de este pool."

10. **Risks/mit (conservative, pre-exist):** Blocking checks (mit: best-effort + short timeouts + light queries + separate from tx; non-blocking task for endpoint); false positives (soft ok/degraded/unhealthy + details + "Diana recomienda..."; no auto-alerts); sensitive (admin-only bot view; /health high-level aggregates no PII/secrets; terminal ops-only); new dep aiohttp (mit: optional/flag/HEALTH_ENABLED + separate port + graceful skip if import fail; or scope bot cmd + terminal first 0 dep; Railway healthcheck remains commented); private access _scheduler/_listeners (mit: thin public get_*_status if ugly or accept in Health system-domain with comment per impact "safe extension"); test flakes/N806 (precedent tol + doc; re-runs focus attributable); handler 1svc (enforce via get_service context + tests); GSD/LOC/logging (pre every + ruff + inspect in phases); stale (jobs dynamic but reflects current APScheduler SQLAlchemyJobStore state). Safe points: delete health_service + exports/wiring/btn/cmd/voice = clean; skip endpoint = unaffected; revert on_startup addition. Pre-exist (scheduler serialization, daily concurrent, N806 in golds, etc) doc non-reg.

11. **Output per phase:** Brief report in GSD (what done + gates + safe point + DoD); full self-check + pool phrase + handoff at F6. (opt) SUMMARY.md post self-check mirroring 28/27 style (phase summary, desviaciones logged, decisions, GSD discipline, pool/BATCH verbatim, handoff).

12. **0 prod intent / verification:** This is hardening/observability spike (Item 11); 0 change to user flows or critical contracts. All verif via re-runs/greps/smokes/self-check. Handoff after documentador.

---

## 5. Riesgos / Mitigaciones + Safe Points + DoD

**Riesgos + Mitigaciones (conservative, per impact):**
- Blocking checks under load affecting polling/gamif/narrative/channel jobs: Mitigation — all checks best-effort + short timeouts (try/except + time budget); light queries only (counts, not full scans); separate from main tx; non-blocking server task for endpoint. 0 side effects.
- False positives / noisy admins: Soft statuses (ok/degraded on thresholds e.g. pending>50 or no recent backup); include details + "Diana recomienda..."; no auto-alerts yet.
- Exposing sensitive (user counts, exact balances? but aggregate only): Admin-only for bot (is_admin); for /health keep high-level aggregates (no per-user PII, no secrets like tokens/urls); terminal ops-only.
- New dep (aiohttp) for endpoint: Mitigation — make endpoint optional (if import fails or HEALTH_ENABLED=0, skip server; log warning); or scope initial to bot cmd + terminal (0 dep), add HTTP in follow-up; Railway healthcheck remains commented until validated.
- Private access (_scheduler, _listeners): Add thin public get_*_status() methods (read-only) in SchedulerService + InternalEventBus if private ugly (small, read-only, non-breaking; document in decisions); or accept in HealthService (system domain) with comments. Low blast.
- Test flakiness / N806 / DB in tests: Follow precedents (TestSession/tmp_path in golds, N806 tol w/doc, fresh 777 TG, guards for get_service, ruff hygiene separate chore if needed).
- Handler rule violation: Enforce "exactly 1 svc" + no logic in all new/updated handlers (use get_service context); update tests accordingly.
- GSD/LOC/logging/naming: Pre every modify (echo >> gsd log), ruff/LOC checks in phases, explicit logs in health methods.
- Stale health (e.g. scheduler jobs): Jobs are dynamic but health reflects current APScheduler state (jobs persist via SQLAlchemyJobStore).

**Safe points (revertable with 0 residual):**
- Delete services/health_service.py + test + script + 1-2 lines in __init__.py/bot/handlers/keyboards/voice/decisions/CLAUDEs = clean.
- Remove button + cb/cmd handler = no menu impact.
- Skip endpoint wiring = bot unaffected.
- Revert bot on_startup addition.
- Precedent: EventBus was "completely removable" per decisions.

**DoD (for gsd-executor phases + final; copy 28/27 style):**
- All 5-6 phases GSD pre-logs (counts 5-10+/fase + wc), self-check "PASSED" with full structure + critical tests list + pool phrase + "0 behavior/0 atomicity/0 risk to 3 systems" + "Item 11/29 closed. Third of new pool of 4..." + handoff to arch + testg + documentador + next.
- HealthService implements listed checks (read-only/best-effort with timeouts), logs in format, overall_status, lifecycle follows Analytics al pie, <50 LOC, uses get_service compat, closes properly, no mutation.
- Bot admin view (cmd or menu cb) + terminal script functional, use get_service + is_admin + LucienVoice, exactly 1 svc.
- Endpoint (if in scope) serves JSON on /health, non-blocking, no breakage to polling; optional/flag.
- 0 changes to critical flows/services (grep confirm 0 new writes/mutations in gamif/narrative/channel paths; 3 crit protected).
- Tests: new unit green; targeted -k re-runs of golds + scheduler + free_entry + event_bus + vip/story + analytics all green with 0 regressions; ruff clean; smokes (bot/terminal/curl) pass; greps/LOC verif pass.
- Docs: decisions entry, services/CLAUDE table+notes; (documentador handles ROADMAP + final persistence per mandate).
- Grep/LOC/verif in final GSD: "health_service |" logs present, get_service(HealthService), 1 btn, system_health + denied, no privates without comment or public wrapper, "Item 11" refs, 3 crit + contracts protected.
- Handoff: "Item 11/29 closed. Third of new pool of 4. Pool anterior de 4 cerrado (tests passing per user). Nuevo pool de 4 iniciado. Quedan ~2-4 clusters del análisis inicial después de este pool. ... Ready for arch-enforcer re-scan (enfocado en health checks + endpoint + admin views + no impact on 3 crit) + test-guardian (correr los tests críticos listados) + documentador (for final ROADMAP update) + gsd-executor del siguiente item del pool de 4."

---

## 6. Self-check (planner, after all exploration + reads + GSD pre + write)

**PASSED**

- All exploration + reads done (impact report full + gsd-impact log + HARDENING_ROADMAP + precedents 28/27/26/25/24/23 PLANs/SUMMARIES/gsd logs + current code via tools: bot.py on_startup+listeners+imports, analytics_service full, scheduler, event_bus, channel_service, vip_service, database, analytics_handlers full (1svc pattern), admin_handlers, inline_keyboards admin_menu, lucien_voice (analytics_dashboard + access_denied + error), services/__init__ get_service, utils/admin is_admin, services/CLAUDE, handlers/CLAUDE, decisions tail, settings, railway, Procfile, models/CLAUDE, architecture/rules/CLAUDE root + AGENTS, golds atomicity/free_entry/scheduler/event_bus/analytics/channel/vip, scripts patterns, greps for on_startup/jobs/logging/health(0)/Channel/VIP publics/get_service/is_admin; GSD pre logged 10+ before this write + prior).
- Scope respected (tight In/Out explicit; 0 other services/handlers/models/critical flows; 0 behavior/0 atomicity/0 prod; 3 crit + contracts + logging + GSD + <50 + 1svc + verb+ctx+res + documentador integration cited in every relevant section: Alcance, Fases, Copia, Instrucciones, Risks, Self-check).
- PLAN complete/actionable + mirrors precedents exactly (title with Item 11/29 + "third of new pool of 4" + pool phrase; Input principal + precedents list; GSD enforcement; Alcance 1 with exact files + methods from impact + In/Out; Archivos/Fuera; 6 phases F1-F6 with gates/DoD/safe points/copy al pie; Copia patrones section (Analytics + get_service/is_admin + EventBus MUST NOT + Scheduler/Channel/VIP publics + logging + Lucien + 1svc + GSD + self-check + pool + handoff + documentador); Instrucciones para gsd-executor (read first + GSD pre every + tight scope + copy al pie + phases strict + re-verify 3 crit + gates exact + documentador spawn at end + self-check full + risks); Risks + safe points + DoD; Self-check + handoff with verbatim pool + "Item 11/29 closed. Third of new pool of 4. ... Ready for arch-enforcer re-scan (enfocado en health checks + endpoint + admin views + no impact on 3 crit) + test-guardian (correr los tests críticos listados) + documentador (for final ROADMAP update) + gsd-executor del siguiente item del pool de 4."; commands with -q --tb=line -p no:cov --override-ini="addopts="; UI 1:1; 3 crit + contracts protected).
- Verbatim pool language repeated: "Pool anterior de 4 cerrado (tests passing per user). Nuevo pool de 4 iniciado. Quedan ~2-4 clusters del análisis inicial después de este pool." + "Item 11/29 closed..." etc.
- 0 scope creep. Follows user desire for friendly admin views (bot + terminal) + API endpoint + simple health review. Strict: no edits outside GSD (planner pre-log before write); documentador for docs.

**Pool phrase (verbatim, repeated in PLAN + will be in self-check/F6 log/SUMMARY):** "Pool anterior de 4 cerrado (tests passing per user). Nuevo pool de 4 iniciado. Quedan ~2-4 clusters del análisis inicial después de este pool."

**Item 11/29 closed (in F6 handoff after executor + arch + testg + documentador).** PLAN ready for gsd-executor (observability/health Item 11, after Item 10 in current flow; third of new pool of 4).

Self-check (planner): PASSED (all exploration + reads done, scope respected, 3 systems + rules + GSD + pool phrase + documentador integration cited in every relevant section; PLAN complete/actionable + mirrors precedents exactly).

---

**End of PLAN.** (GSD pre-write logged; wc tracked; ready for gsd-executor.)