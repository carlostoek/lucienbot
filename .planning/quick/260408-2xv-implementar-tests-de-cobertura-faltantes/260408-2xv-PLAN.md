---
name: Implementar tests de cobertura faltantes
description: Implementar 4 tests que faltan: e2e misiones (reacción→misión→besitos), alembic heads, ciclo VIP completo, límite de reacciones
status: pending
priority: high
must_haves:
  - tests/integration/test_mission_e2e.py con test de flujo reacción→misión→besitos
  - tests/integration/test_alembic_heads.py verificando no múltiples heads
  - tests/integration/test_vip_complete_cycle.py con entrada, recordatorio, expiración, expulsión
  - tests/integration/test_reaction_limit.py verificando/implementando límite diario de reacciones
created: 2026-04-08
---

## Tasks

### Task 1: Test e2e de misiones (reacción → misión → besitos)
**Files:** `tests/integration/test_mission_e2e.py`

**Action:**
- Crear test que simule una reacción de usuario
- Verificar que se dispara la misión correcta (tipo REACTION_COUNT)
- Verificar que se otorgan los besitos esperados al completar la misión

**Verify:**
- Test pasa exitosamente verificando el flujo completo

**Done:**
- test_mission_e2e.py creado en tests/integration/

### Task 2: Test de alembic heads (verificar no múltiples heads)
**Files:** `tests/integration/test_alembic_heads.py`

**Action:**
- Crear test que ejecute `alembic heads` mediante script
- Verificar que solo hay un head (no bifurcaciones)
- Documentar el resultado

**Verify:**
- Test pasa y confirma un único head

**Done:**
- test_alembic_heads.py creado en tests/integration/

### Task 3: Test e2e de ciclo VIP completo
**Files:** `tests/integration/test_vip_complete_cycle.py`

**Action:**
- Test flujo completo: token → suscripción activa → recordatorio 24h antes → expiración → expulsión
- Crear suscripción que vence en 25 horas
- Verificar que aparece en get_expiring_subscriptions (24h)
- Simular expiración y verificar is_active=False

**Verify:**
- Test pasa verificando todo el ciclo

**Done:**
- test_vip_complete_cycle.py creado en tests/integration/

### Task 4: Verificar/implementar límite de reacciones por usuario
**Files:** `tests/integration/test_reaction_limit.py`, potencial modificación en `services/broadcast_service.py`

**Action:**
- Revisar si existe implementación de límite diario de reacciones
- Si no existe, documentar como gap y crear test que verifique la ausencia
- Si existe, crear test que verifique el comportamiento

**Verify:**
- Test documenta el estado actual (existe o no existe)

**Done:**
- test_reaction_limit.py creado con documentación del estado actual